---
quick_id: 260522-s33
slug: scrub-cli-help-phase-stubs
description: Scrub stale phase-stub annotations from CLI help strings
date: 2026-05-22
status: complete
---

# Quick Task 260522-s33 — Summary

## What changed

Removed four stale `/// (Phase X implementation — Phase 1 stub.)` doc-comment lines
from `crates/lacon-cli/src/cli.rs` (on the `Init`, `Stats`, `Explain`, and `Doctor`
subcommand variants). Clap was rendering them verbatim into `lacon --help` and the
per-subcommand `--help` output even though all four commands shipped in v1.0.

`crates/lacon-cli/src/cli.rs` — 1 file changed, 4 deletions.

## Verification

- `cargo build -p lacon-cli` — clean.
- `lacon --help` — command descriptions now end at the period; no "Phase"/"stub" text.
- `grep -rn "Phase 1 stub\|Phase 3 implementation\|Phase 4 implementation" crates/ tests/`
  — no matches.
- `cargo test -p lacon-cli` — passes (including `tracking_coldstart` /
  `tracking_e2e` suites). Change is doc-comment-only, no logic touched.

## Notes / out of scope

Internal `// Phase N` comments elsewhere (e.g. `runtime/mod.rs`, `tracking/mod.rs`,
`benches/tracker_open.rs`) were left in place. They document cross-phase contracts and
historical evolution for developers and never reach a user-facing surface, so they
fall outside this report's intent ("strings the help shows").
