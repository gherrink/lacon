---
phase: quick-260522-v4a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/lacon-cli/src/commands/doctor.rs
  - crates/lacon-cli/tests/cli_doctor.rs
  - crates/lacon-cli/src/cli.rs
autonomous: true
requirements:
  - REQ-cli-doctor
user_setup: []

must_haves:
  truths:
    - "doctor checks BOTH project and user scope (hook + LACON.md + @import reference per scope), grouped by scope"
    - "A scope is 'configured' iff its settings.json carries the lacon hook fingerprint"
    - "Configured + complete scope shows [ ok ] for hook/instructions/reference"
    - "Configured + broken scope (hook present, LACON.md or @import missing) shows [fail] and exits 1"
    - "A not-configured scope WHILE the other scope IS configured renders NEUTRALLY (informational), not warn, not fail — exit unaffected"
    - "Neither scope configured (fresh machine) shows [warn] with `lacon init` hint, exit 0"
    - "IO/parse error on a present settings.json or CLAUDE.md is [fail] regardless"
    - "doctor tests are fully hermetic: run_doctor overrides HOME + XDG so user-scope checks never touch real ~/.claude"
  artifacts:
    - path: "crates/lacon-cli/src/commands/doctor.rs"
      provides: "scope-aware hook+instructions+reference checks via one parameterized code path + user_claude_dir() helper"
      contains: "fn user_claude_dir"
    - path: "crates/lacon-cli/tests/cli_doctor.rs"
      provides: "HOME-override run_doctor + user-scope/both-scope/one-scope-only/posture test cases"
      contains: ".env(\"HOME\""
  key_links:
    - from: "crates/lacon-cli/src/commands/doctor.rs"
      to: "HOOK_FINGERPRINT walk"
      via: "shared parameterized check_scope (project + user reuse one walk)"
      pattern: "HOOK_FINGERPRINT"
    - from: "crates/lacon-cli/src/commands/doctor.rs"
      to: "etcetera::home_dir().join(\".claude\")"
      via: "user_claude_dir() helper (NOT user_config_dir)"
      pattern: "home_dir\\(\\)"
---

<objective>
Rework `lacon doctor` to verify the FULL setup at BOTH scopes (project + user) — hook, `LACON.md`, and the scope-correct `@import` reference in `CLAUDE.md` — with the opt-in posture the user locked: a configured scope must be complete or it fails; a not-configured scope is only flagged when NEITHER scope is configured (fresh machine), and is shown neutrally when the other scope is configured. Then audit and fix the remaining single-scope assumptions left over from the old cwd-only model.

Purpose: Quick task 260522-tor made `lacon init` install at two scopes (project = cwd-relative, user = `~/.claude` + `~/.config/lacon/rules`). `doctor` still assumes the OLD single-scope model — its `check_hook` only inspects `<cwd>/.claude/settings.json` and never checks `LACON.md` or the `@import` reference line that init now writes per scope. This closes that gap so `doctor` accurately verifies what `init` produces.

Output: Reworked `doctor.rs` (scope-aware, three-check-per-scope, posture-correct); reworked `cli_doctor.rs` (HOME-isolated, new posture + scope test cases); updated `cli.rs:64` Doctor help; doctor.rs module + check rustdoc describing both scopes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260522-v4a-doctor-check-both-project-and-user-setup/260522-v4a-CONTEXT.md

# The file being reworked (full current state)
@crates/lacon-cli/src/commands/doctor.rs

# Current contract tests (HOME override + posture rework target)
@crates/lacon-cli/tests/cli_doctor.rs

# Init: the source of truth for scope paths + import tokens to MIRROR
@crates/lacon-cli/src/commands/init.rs

# Test isolation pattern to mirror (HOME + XDG tempdir; PROJECT_IMPORT/USER_IMPORT consts)
@crates/lacon-cli/tests/cli_init.rs

# Doctor help text to update (~line 64)
@crates/lacon-cli/src/cli.rs

<interfaces>
<!-- Key contracts the executor needs. Use these directly — do NOT re-explore. -->

From crates/lacon-cli/src/commands/init.rs (the per-scope setup doctor must verify):
- Project scope paths (cwd-relative):
    settings_path  = <cwd>/.claude/settings.json
    lacon_md_path  = <cwd>/.claude/LACON.md
    claude_md_path = <cwd>/CLAUDE.md
    import token   = "@.claude/LACON.md"   (const PROJECT_IMPORT_LINE)
- User scope paths (home-relative, via `etcetera::home_dir()` which reads $HOME):
    home           = etcetera::home_dir()?            // tests override $HOME
    claude_dir     = home.join(".claude")
    settings_path  = <claude_dir>/settings.json
    lacon_md_path  = <claude_dir>/LACON.md
    claude_md_path = <claude_dir>/CLAUDE.md
    import token   = "@LACON.md"            (const USER_IMPORT_LINE)
- Extensionless `@LACON` does NOT resolve and must never be the expected token.

From crates/lacon-cli/src/commands/doctor.rs (current surface — REUSE, do not duplicate):
- const HOOK_FINGERPRINT: &str = "lacon-claude-hook";
- enum Status { Pass, Fail, Warn }   // ADD a neutral/informational variant (e.g. Info)
- fn report(status: Status, label: &str, detail: &str) -> bool  // Pass/Warn/Info => true (no flip); Fail => false
- The PreToolUse(Bash) walk (doctor.rs:141-148):
    settings["hooks"]["PreToolUse"].as_array().into_iter().flatten()
      .filter(|g| g["matcher"] == "Bash")
      .flat_map(|g| g["hooks"].as_array().into_iter().flatten())
      .filter_map(|h| h["command"].as_str())
      .any(|c| c.starts_with(HOOK_FINGERPRINT))
- fn user_config_dir() -> Option<PathBuf>   // XDG ~/.config/lacon — DIFFERENT path; keep for the config.yaml check, do NOT conflate with ~/.claude
- Untouched checks stay as-is: check_configs, check_rules, check_db_perms, check_tracker_health, perms_of.

From crates/lacon-cli/tests/cli_init.rs (isolation pattern to mirror in run_doctor):
- const PROJECT_IMPORT: &str = "@.claude/LACON.md";
- const USER_IMPORT: &str = "@LACON.md";
- User-scope test isolation: `.env("HOME", home.path()).env("XDG_CONFIG_HOME", xdg.path())`
- On macOS etcetera resolves user rules under $HOME/Library/Application Support; HOME override covers it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework doctor.rs into scope-aware three-check-per-scope with the locked opt-in posture</name>
  <files>crates/lacon-cli/src/commands/doctor.rs</files>
  <action>
Replace the single project-only `check_hook` with a parameterized per-scope check that runs for BOTH project and user, sharing ONE settings.json walk (do not duplicate the PreToolUse(Bash) traversal).

1. Add a neutral status. Extend `enum Status` with an informational-but-not-warn variant (e.g. `Info`) and handle it in `report` — render as a neutral glyph line (e.g. `[ -- ] <label>: ...`) that returns `true` (never flips `all_ok`). Keep the existing `Pass`/`Warn` => true, `Fail` => false semantics. Greppable labels (`hook`, `rules`, `db-perms`, `tracker`) MUST remain — existing tests substring-match them; per-scope hook lines must still contain the substring `hook`.

2. Add a `user_claude_dir()` helper: `etcetera::home_dir()?.join(".claude")` returning `Option<PathBuf>` (mirror init.rs:211-212). Reads `$HOME` so tests can override it. DO NOT reuse or modify `user_config_dir()` — that resolves the XDG `~/.config/lacon` path used by the config.yaml check and is a DIFFERENT directory; conflating the two is the bug being avoided (per CONTEXT Discretion + Specifics).

3. Define a per-scope check function, e.g. `check_scope(label: &str, settings_path, lacon_md_path, claude_md_path, import_token) -> (configured: bool, ok: bool)`. Behavior per the LOCKED posture in CONTEXT.md `<decisions>`:
   - Read settings.json. `configured` = settings.json present AND parses AND carries the HOOK_FINGERPRINT PreToolUse(Bash) entry (reuse the existing walk). A settings.json that is absent OR present-without-hook => `configured = false` (NOT an immediate fail — the caller decides posture).
   - settings.json present but unreadable/unparseable => `[fail]` (IO/parse error on a present file, T-04-10), `configured = false`, `ok = false`, return early.
   - When `configured` is true, run the two completeness sub-checks and emit a line each:
       * instructions: `lacon_md_path` is `Path::is_file()` => `[ ok ]`, else `[fail]` (half-installed scope is positively broken).
       * reference: read `claude_md_path`; a whole-line substring match for `import_token` (tolerant of trailing whitespace, mirror init.rs install_reference_line's `l.trim_end() == import_line`) => `[ ok ]`, missing => `[fail]`. A present-but-unreadable CLAUDE.md => `[fail]` (T-04-10). A NotFound CLAUDE.md while configured => `[fail]` (the reference is missing).
       * also emit the `[ ok ] hook ...` line for this scope.
     `ok` = AND of all three sub-checks.
   - Return `(configured, ok)`. Do NOT emit the not-configured line here — the caller renders the cross-scope posture (it needs to know the OTHER scope's `configured` state first).

4. Rewrite `execute()` to drive the new posture:
   - Resolve project paths from `cwd` (settings `<cwd>/.claude/settings.json`, lacon_md `<cwd>/.claude/LACON.md`, claude_md `<cwd>/CLAUDE.md`, token `@.claude/LACON.md`).
   - Resolve user paths via `user_claude_dir()` (settings/lacon_md/claude_md under `~/.claude`, token `@LACON.md`). If `user_claude_dir()` is `None`, treat user as not-configured (do not panic).
   - First determine `project_configured` and `user_configured` (a cheap "is the hook present?" probe — you may compute it inside `check_scope` and return it, but you must know BOTH before rendering not-configured lines; structure the calls so configured-state is known before emitting the neutral/warn line for an un-configured scope). `any_configured = project_configured || user_configured`.
   - Render, grouped by scope ("Project setup" then "User setup" headers — exact wording is discretion, both scopes MUST be clearly labeled):
       * configured scope => emit its hook/instructions/reference lines (from `check_scope`); fold its `ok` into `all_ok`.
       * not-configured scope WHEN `any_configured` => NEUTRAL `Info` line (e.g. `[ -- ] <scope> setup: not configured (optional)`); does NOT flip `all_ok`. THIS IS THE USER'S EXPLICIT REQUIREMENT — never a `[warn]`, never a `[fail]`.
       * if `!any_configured` (NEITHER configured, fresh machine) => a single `[warn] hook: not installed (run \`lacon init\`)` posture line, exit 0. The substring `hook` and `run \`lacon init\`` MUST appear.
   - Keep the four global checks unchanged below the scope groups: `check_configs(&cwd)`, `check_rules(&cwd)`, `check_db_perms()`, `check_tracker_health()`; fold each into `all_ok`.
   - Keep the existing exit contract: `Ok(0)` iff `all_ok`, else `Ok(1)`; never propagate a raw error (T-04-10).

5. Update module rustdoc (lines ~1-31) and the reworked hook-check rustdoc: describe BOTH scopes and the three per-scope checks (hook / instructions / reference); drop the "fixed five-check" framing since the check shape changed. Document the opt-in posture (configured-or-fail; neutral-when-other-configured; warn-when-neither) and keep the T-04-10 error-posture note. Do NOT touch check_configs/check_rules/check_db_perms/check_tracker_health/perms_of logic.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5 && cargo clippy -p lacon-cli --all-targets 2>&1 | grep -E "warning|error" | grep -v "generated" | head; echo "build+clippy done"</automated>
  </verify>
  <done>doctor.rs builds clean (no new clippy warnings); `check_scope` runs for project AND user via one shared HOOK_FINGERPRINT walk; `user_claude_dir()` exists and is distinct from `user_config_dir()`; `execute()` renders grouped-by-scope output with the locked posture (configured+complete=ok, configured+broken=fail/exit1, not-configured+other-configured=neutral, neither-configured=warn/exit0, present-file IO/parse error=fail); module/check rustdoc describes both scopes and the new posture.</done>
</task>

<task type="auto">
  <name>Task 2: Rework cli_doctor.rs for HOME isolation + new posture + scope coverage; update cli.rs Doctor help</name>
  <files>crates/lacon-cli/tests/cli_doctor.rs, crates/lacon-cli/src/cli.rs</files>
  <action>
Make every doctor test hermetic against the real `~/.claude`, and lock the new scope-aware posture as the contract.

1. `run_doctor` HOME isolation (CRITICAL): add `.env("HOME", <home_tempdir>)` to the `run_doctor` helper so user-scope checks never read the developer's real `~/.claude`. The current helper only redirects `XDG_DATA_HOME`/`XDG_CONFIG_HOME`. Change its signature to also take a `home: &Path` (mirror cli_init.rs user-scope tests). Update EVERY existing call site to pass a fresh `home` tempdir. A simple pattern: each test creates `let home = tempdir().unwrap();` and passes `home.path()`. Existing project-scope-only tests (`doctor_all_green_*`, invalid config/rule, fresh machine) pass an EMPTY `home` tempdir so the user scope is correctly seen as not-configured. NOTE: the `doctor_all_green` test's `lacon init` call writes only the project scope (cwd) — confirm it still asserts `[ ok ] hook` for project; with an empty HOME the user scope renders neutral, which must not flip the green result.

2. Update the existing posture test. Rename/rewrite `doctor_settings_present_without_hook_is_a_hard_fail` to the NEW posture (INTENTIONAL CHANGE per CONTEXT): a project `.claude/settings.json` that parses but has no lacon hook, with an empty user HOME (neither scope configured), is now a `[warn]` ("run `lacon init`") with exit 0 — NOT a hard fail. Assert `.success()`, `[warn]` + `hook` + "run `lacon init`". (Add a comment pointing at the SUMMARY for the behavior-change rationale.)

3. Add new scope/posture test cases (all driving controlled HOME + XDG, never the real home):
   - user-scope configured + complete: seed `<home>/.claude/settings.json` with the lacon hook, `<home>/.claude/LACON.md`, and `<home>/.claude/CLAUDE.md` containing `@LACON.md`; empty project cwd. Assert `.success()` and the user-scope hook/instructions/reference all `[ ok ]`. (You may use `lacon init --user` with HOME+XDG overridden to produce these artifacts, mirroring cli_init.rs, OR hand-write the files — either is fine; prefer `lacon init --user` to avoid drift.)
   - configured + BROKEN scope flips exit 1: a scope with the hook installed but `LACON.md` missing (or the `@import` reference absent from CLAUDE.md) asserts `.failure()` + `[fail]` on the missing sub-check. Cover at least one broken sub-check (instructions OR reference).
   - one-scope-only neutrality (the user's explicit requirement): install ONLY the user scope (hook + LACON.md + reference under tempdir HOME), leave the project cwd empty/unconfigured. Assert `.success()` (exit 0) AND that the project scope is rendered NEUTRALLY — it must NOT contain `[warn] hook` or `[fail] hook` for the project group (assert absence of a project-scope warn/fail; the neutral marker e.g. `[ -- ]` or "not configured" is present). Mirror the inverse if cheap (project-only configured, user neutral).
   - keep `doctor_fresh_machine_*` but update it to the new neither-configured posture (empty cwd + empty HOME + empty XDG => `[warn] hook` + the existing `[warn] db-perms`/`[warn] tracker` informational lines + `all checks passed` + exit 0).

   Use the cli_init.rs helpers as a template (`bash_commands`, the `lacon init --user` HOME+XDG invocation, the PROJECT_IMPORT/USER_IMPORT consts — copy them into this test file). Update the module-level rustdoc at the top of cli_doctor.rs to describe the new scope-aware cases and the HOME-isolation requirement.

4. Update `cli.rs` Doctor help text (line ~64, the `/// Verify hooks installed, configs valid, rules parse.` doc comment). Reword to reflect that doctor checks BOTH project and user scope setup (hook + instructions + import reference per scope) plus configs/rules/DB health. Keep it concise (one or two lines, matching the clap doc-comment style of the Init variant just above).

5. Audit note (no code change unless a real break): `pnpm_e2e.rs::pnpm_e2e_real` calls bare `lacon init` via a builder that sets XDG but not HOME — under the new project-default non-interactive behavior bare `lacon init` writes only the project (cwd) scope, so it does NOT touch `~/.claude` and stays hermetic; `pnpm_e2e_hermetic` does not call init at all. No change required there, but confirm `cargo test -p lacon-cli` (which excludes the ignored real test) stays green. The substantive scan target was doctor.rs/cli.rs/cli_doctor.rs; docs/ wording (`docs/v1-scope.md:46`, `docs/architecture.md:66`) is generic and not materially wrong — leave it unless the SUMMARY author judges otherwise.
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test -p lacon-cli --test cli_doctor 2>&1 | tail -25</automated>
  </verify>
  <done>`cargo build --workspace` then `cargo test -p lacon-cli --test cli_doctor` passes; `run_doctor` sets `.env("HOME", ...)` and no test reads the real `~/.claude`; the old `doctor_settings_present_without_hook_is_a_hard_fail` is now the warn/exit-0 posture; new tests cover user-scope-complete, configured-broken-exit-1, and one-scope-only-neutrality (other scope NOT flagged); `cli.rs` Doctor help mentions both scopes; full `cargo test -p lacon-cli` (non-ignored) stays green incl. pnpm_e2e hermeticity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → doctor | `settings.json` / `CLAUDE.md` / `LACON.md` are user/third-party-written files doctor parses (untrusted JSON, untrusted text) |
| `$HOME` env → doctor | `etcetera::home_dir()` reads `$HOME`; in tests this is the isolation seam — a missed override would read the developer's real `~/.claude` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v4a-01 | Tampering | malformed/unparseable settings.json at either scope | mitigate | parse error on a PRESENT file maps to `[fail]` + exit 1 (T-04-10 posture); no raw `?` panic reaches the user |
| T-v4a-02 | Information Disclosure | doctor test reading the developer's real `~/.claude` | mitigate | `run_doctor` overrides `HOME` + `XDG_*` to tempdirs (the locked hermetic-test requirement); every doctor test drives a controlled HOME |
| T-v4a-03 | Tampering | unreadable/missing CLAUDE.md at a configured scope | mitigate | present-but-unreadable => `[fail]`; configured-but-reference-missing => `[fail]` (half-installed scope is positively broken) |
| T-v4a-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies added (reuses etcetera + serde_json already in lacon-cli); no install tasks in this plan |
</threat_model>

<verification>
- `cargo build --workspace` succeeds (CLAUDE.md build-before-test ordering).
- `cargo test -p lacon-cli --test cli_doctor` passes (new scope-aware posture contract).
- `cargo test -p lacon-cli` (full non-ignored suite) passes — proves cli_init.rs + pnpm_e2e hermetic tests still green.
- `cargo clippy -p lacon-cli --all-targets` adds no new warnings.
- Manual grep confirms: `doctor.rs` contains `fn user_claude_dir`, reuses `HOOK_FINGERPRINT` (single walk, not duplicated), and `cli_doctor.rs` `run_doctor` contains `.env("HOME"`.
</verification>

<success_criteria>
- `lacon doctor` checks hook + LACON.md + scope-correct `@import` reference for BOTH project and user, grouped by scope, with greppable `hook`/`rules`/`db-perms`/`tracker` labels intact.
- Posture matches the LOCKED CONTEXT decisions exactly:
  - configured + complete → ok;
  - configured + broken → fail (exit 1);
  - not-configured while other configured → NEUTRAL (not warn/fail, exit unaffected);
  - neither configured → warn (run `lacon init`), exit 0;
  - present-file IO/parse error → fail.
- The old `doctor_settings_present_without_hook_is_a_hard_fail` is updated to the new posture and the change is documented in the SUMMARY.
- All doctor tests are hermetic (HOME + XDG overridden); new user-scope, both-scope, and one-scope-only cases exist.
- Single-scope assumptions fixed: doctor.rs module + check rustdoc, `cli.rs` Doctor help; pnpm_e2e.rs hermeticity verified (no change needed); docs/ left unless materially wrong.
- One shared HOOK_FINGERPRINT walk (no duplication); `user_claude_dir()` distinct from `user_config_dir()`.
</success_criteria>

<output>
Create `.planning/quick/260522-v4a-doctor-check-both-project-and-user-setup/260522-v4a-SUMMARY.md` when done. In the SUMMARY, explicitly document the INTENTIONAL behavior change: the old `doctor_settings_present_without_hook_is_a_hard_fail` is now warn-when-neither-configured / neutral-when-the-other-scope-is-configured (no longer a hard fail), and the rationale (two opt-in scopes mean a bare Claude settings.json without a lacon hook is no longer "positively broken").
</output>
