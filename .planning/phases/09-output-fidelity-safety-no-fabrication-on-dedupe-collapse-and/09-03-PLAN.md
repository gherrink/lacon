---
phase: 09-output-fidelity-safety-no-fabrication-on-dedupe-collapse-and
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - bundled-rules/git-status.yaml
  - tests/fixtures/git-status/many-untracked/input.txt
  - tests/fixtures/git-status/many-untracked/expected.txt
  - tests/fixtures/git-status/many-untracked/meta.yaml
  - tests/fixtures/git-status/tabular-signal/input.txt
  - tests/fixtures/git-status/tabular-signal/expected.txt
  - tests/fixtures/git-status/tabular-signal/meta.yaml
  - docs/specs/filter-rule-schema.md
autonomous: true
requirements: [REQ-engine-streaming-primitives]
must_haves:
  truths:
    - "The git-status rule no longer collapses tabular per-file signal lines — every file line survives verbatim (D-08)."
    - "The git-status success fixture proves every surviving line is byte-identical to an input line; any elision is the `[lacon: …]` marker, never a substitution (D-11)."
    - "tsc.yaml's dedupe success fixture is confirmed to preserve signal (D-10) — unchanged."
    - "filter-rule-schema.md documents the standardized `[lacon: …]` collapse elision marker (D-12)."
  artifacts:
    - path: "bundled-rules/git-status.yaml"
      provides: "git-status pipeline with the signal-collapsing collapse_repeated stage removed/narrowed (D-08)"
      contains: "id: git-status"
    - path: "tests/fixtures/git-status/many-untracked/meta.yaml"
      provides: "regenerated meta — exempt_from_reduction_check decision + must_keep_lines + notes"
      contains: "exempt_from_reduction_check"
    - path: "tests/fixtures/git-status/tabular-signal/expected.txt"
      provides: "no-fabrication class fixture (tabular / repeated-prefix) proving verbatim survival"
    - path: "docs/specs/filter-rule-schema.md"
      provides: "updated collapse_repeated marker contract (D-12)"
      contains: "collapse_repeated"
  key_links:
    - from: "bundled-rules/git-status.yaml pipeline"
      to: "tests/fixtures/git-status/many-untracked/expected.txt"
      via: "bundled_rules.rs fixture walker byte-exact + reduction-floor assertions"
      pattern: "git-status"
    - from: "docs/specs/filter-rule-schema.md collapse_repeated entry"
      to: "the standardized [lacon: …] marker emitted by stages.rs (Plan 02)"
      via: "spec documents the marker behavior"
      pattern: "\\[lacon:"
---

<objective>
Re-audit the two bundled rules that use `dedupe`/`collapse_repeated` and update the user-facing spec (REQ-engine-streaming-primitives, success-criterion #3, D-08/D-10/D-11/D-12). This is the rule + fixture + spec layer; it depends on Plan 02 because the regenerated fixtures must encode the new `[lacon: …]` marker format that Plan 02 establishes.

Per D-08: remove/narrow `collapse_repeated` from `git-status.yaml` so tab-indented per-file lines (signal) survive verbatim. Per D-10: confirm `tsc.yaml`'s verbatim-only `dedupe` fixture already preserves signal (no change expected). Per D-11: regenerate the git-status success fixture and add a no-fabrication class fixture (tabular/repeated-prefix). Per D-12: update `docs/specs/filter-rule-schema.md` — a deliberate user-facing contract change to the `collapse_repeated` summary semantics.

Two RESEARCH open questions are settled as explicit tasks here (NOT silently): Open Q1 (the literal `table table table` string is NOT reproducible from git-status → gate on the D-07 class reinterpretation, not exact bytes) and Open Q2 (removing the collapse stage breaches the ≥50% reduction floor → record the explicit `exempt_from_reduction_check: true` choice in meta.yaml).

Purpose: No bundled success-path fixture loses verbatim signal to collapse; the contract is documented.
Output: edited `git-status.yaml`, regenerated + new git-status fixtures, confirmed tsc fixture, updated spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-output-fidelity-safety-no-fabrication-on-dedupe-collapse-and/09-CONTEXT.md
@.planning/phases/09-output-fidelity-safety-no-fabrication-on-dedupe-collapse-and/09-RESEARCH.md
@.planning/phases/09-output-fidelity-safety-no-fabrication-on-dedupe-collapse-and/09-PATTERNS.md
@.planning/phases/09-output-fidelity-safety-no-fabrication-on-dedupe-collapse-and/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the signal-collapsing stage from git-status.yaml + regenerate its success fixture</name>
  <read_first>
    - bundled-rules/git-status.yaml (the file being modified; the `collapse_repeated` stage on `^\t` max_kept 5 at lines 14-17 to remove/narrow per D-08; the `on_error` block at lines 19-25 is UNTOUCHED — ADR-0010)
    - bundled-rules/tsc.yaml (the exempt-from-reduction "the output IS the signal" template to mirror)
    - tests/fixtures/git-status/many-untracked/input.txt, expected.txt, meta.yaml (the triple to regenerate)
    - tests/fixtures/tsc/type-errors/meta.yaml (the `exempt_from_reduction_check: true` + `must_keep_lines` + notes template)
    - crates/lacon-core/tests/bundled_rules.rs (the fixture-walker contract around lines 104-143: byte-exact actual==expected trailing-newline tolerant, reduction ≤ 0.5 unless exempt, must_keep_lines substring survival, and the meta.yaml schema incl. exit_code)
    - .planning/phases/09-output-fidelity-safety-no-fabrication-on-dedupe-collapse-and/09-02-SUMMARY.md (the exact marker form Plan 02 settled on — needed only if a narrowed collapse is kept)
  </read_first>
  <files>bundled-rules/git-status.yaml, tests/fixtures/git-status/many-untracked/{input.txt,expected.txt,meta.yaml}</files>
  <action>
    Apply D-08 to `bundled-rules/git-status.yaml`: REMOVE the `collapse_repeated` stage (pattern `^\t`, max_kept 5, the tab-indented blending summary at lines 14-17) so every tab-indented per-file line survives verbatim. Keep `strip_ansi` and `drop_regex: '^\s*\(use '`. Leave the `on_error` block (lines 19-25) untouched (ADR-0010). EXPLICIT DECISION — RESEARCH Open Q2 (record the chosen option + rationale in the SUMMARY): removing the stage makes git-status output ≈ input, breaching the `bundled_rules.rs` ≥50% reduction floor. Recommended option = exempt: set `exempt_from_reduction_check: true` on the regenerated `many-untracked` fixture, mirroring `tsc` ("the output IS the signal"). If instead a narrowed collapse is kept, it MUST emit the Plan-02 standardized `[lacon: …]` marker and still pass the floor — but recommended path is removal+exempt. Regenerate `tests/fixtures/git-status/many-untracked/`: `input.txt` stays a representative `git status -uall` capture with many tab-indented file lines; `expected.txt` becomes those lines surviving VERBATIM after only strip_ansi + the one `(use …)` drop (NO summary line, NO substitution); `meta.yaml` set `command: git status -uall`, `exit_code: 0`, `exempt_from_reduction_check: true`, `must_keep_lines` listing representative tab-indented file path substrings that must survive, and `notes` documenting the D-08 decision and the Open Q2 exempt rationale. Reduction now comes only from ANSI strip + the `(use …)` drop, not from dropping signal — mirror the tsc meta notes wording. ORM n/a. Bundled YAML is rust-embed'd at build time → verification MUST `cargo build --workspace` before `cargo test`.
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test -p lacon-core --test bundled_rules git_status</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace && cargo test -p lacon-core --test bundled_rules` exits 0 (git-status fixtures pass the walker contract).
    - `bundled-rules/git-status.yaml` no longer contains a `collapse_repeated` stage in the success `pipeline` (grep filtered of comments: `grep -v '^\s*#' bundled-rules/git-status.yaml | grep -c collapse_repeated` returns 0), OR if narrowed, the stage's summary is the Plan-02 `[lacon: …]` marker.
    - Every line in `tests/fixtures/git-status/many-untracked/expected.txt` appears verbatim in `tests/fixtures/git-status/many-untracked/input.txt` (no summary/substituted line). Verifiable: each expected line is a substring-line of input (a `comm`/`grep -Fxf` check passes for all expected lines, modulo the dropped `(use …)` lines).
    - `tests/fixtures/git-status/many-untracked/meta.yaml` contains `exempt_from_reduction_check: true` (recommended) with `must_keep_lines` and a `notes` field recording the D-08 / Open Q2 decision.
    - The `git-status/not-a-repo` (on_error) fixture, if present, still passes unchanged.
  </acceptance_criteria>
  <done>git-status no longer collapses tabular signal; success fixture regenerated with verbatim survivors and the exempt-vs-narrow decision recorded; on_error path unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add a no-fabrication class fixture + confirm tsc dedupe fixture preserves signal</name>
  <read_first>
    - tests/fixtures/git-status/many-untracked/ (the sibling triple structure: input.txt / expected.txt / meta.yaml)
    - tests/fixtures/tsc/type-errors/meta.yaml (the exempt + must_keep_lines template; D-10 confirm-only target)
    - bundled-rules/tsc.yaml (the `dedupe: { max_kept: 1 }` verbatim-only stage — D-06/D-10, confirm no change needed)
    - crates/lacon-core/tests/bundled_rules.rs (the walker contract the new fixture must satisfy)
    - 09-RESEARCH.md (Pitfall 1 / Open Q1 — do NOT gate on the literal `table table table` string; reproduce the CLASS: aligned/tabular columns, repeated-prefix loop rows, grep hits)
  </read_first>
  <files>tests/fixtures/git-status/tabular-signal/{input.txt,expected.txt,meta.yaml}, tests/fixtures/tsc/type-errors/meta.yaml (confirm-only)</files>
  <action>
    Create `tests/fixtures/git-status/tabular-signal/` (sibling to `many-untracked/`) reproducing the fabrication CLASS per D-11 and RESEARCH Open Q1 — aligned/tabular columns and repeated-prefix loop rows (the structurally-similar-line shape the bug belonged to), NOT the literal `table table table` string (Open Q1: that came from a non-git-status loop and is explicitly NOT a gate). `input.txt` = a `git status` capture whose untracked/changed block is aligned/tabular and repeated-prefix; `expected.txt` = every signal line surviving VERBATIM after the git-status pipeline (strip_ansi + `(use …)` drop only); `meta.yaml` = `command`, `exit_code: 0`, `exempt_from_reduction_check: true` (tabular = signal), `must_keep_lines` covering representative aligned/repeated-prefix lines, and `notes` citing success-criteria #1/#3 and Open Q1 (class not literal). Then CONFIRM D-10 for tsc: verify `tests/fixtures/tsc/type-errors/` is already `exempt_from_reduction_check: true` with `must_keep_lines: ["error TS"]` and that its `input.txt` has no consecutive duplicates so `dedupe: { max_kept: 1 }` drops nothing (expected.txt == input.txt) — RESEARCH verified this; make NO change unless the confirmation fails, and record the confirmation outcome in the SUMMARY. ORM n/a. Bundled rules are rust-embed'd → `cargo build --workspace` before `cargo test`.
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test -p lacon-core --test bundled_rules</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace && cargo test -p lacon-core --test bundled_rules` exits 0 with the new `git-status/tabular-signal` fixture walked and passing.
    - Every line in `tests/fixtures/git-status/tabular-signal/expected.txt` appears verbatim in its `input.txt` (no fabricated/substituted line; `grep -Fxf` survival check passes for all expected lines modulo dropped noise).
    - `tests/fixtures/git-status/tabular-signal/expected.txt` contains NO `[lacon:` marker (the removed-collapse path drops nothing to elide) OR, if a narrowed collapse is retained from Task 1, any elision line matches `^\[lacon:` and never blends.
    - tsc confirmation: `tests/fixtures/tsc/type-errors/meta.yaml` has `exempt_from_reduction_check: true` and `must_keep_lines` includes `error TS`; the tsc fixture test still passes unchanged. Confirmation outcome recorded in SUMMARY.
  </acceptance_criteria>
  <done>No-fabrication class fixture added proving verbatim survival of tabular/repeated-prefix signal; tsc dedupe fixture confirmed signal-preserving and unchanged (D-10).</done>
</task>

<task type="auto">
  <name>Task 3: Update filter-rule-schema.md collapse_repeated marker contract (D-12)</name>
  <read_first>
    - docs/specs/filter-rule-schema.md (the file being modified; the `collapse_repeated: { pattern, max_kept, summary }` entry and the `{count}` substitution wording at lines 128-137; the sibling `dedupe` and `max_bytes` primitive entries are the format analogs in the same doc)
    - .planning/phases/09-output-fidelity-safety-no-fabrication-on-dedupe-collapse-and/09-02-SUMMARY.md (the exact marker form + the retain-vs-drop summary_template decision Plan 02 made — the spec MUST describe what was actually implemented)
    - 09-RESEARCH.md (Elision-Marker Convention table + the D-12 spec-change guidance)
  </read_first>
  <files>docs/specs/filter-rule-schema.md</files>
  <action>
    Update the `collapse_repeated` entry in `docs/specs/filter-rule-schema.md` (lines ~128-137) per D-12 to document the standardized `[lacon: …]` elision-marker behavior as actually implemented in Plan 02 (read 09-02-SUMMARY.md for the exact form and the retain-vs-drop `summary` decision). Describe that elided lines are replaced by a single standardized lacon-namespaced marker of the form Plan 02 chose (e.g. `[lacon: collapsed N lines]`) — NOT a free-form line that can blend into real tool output. If Plan 02 dropped the free-form `summary` body, document its removal (a deliberate user-facing contract change — explicitly call it out as such in the prose, since the spec is part of the user-facing contract per CLAUDE.md). If Plan 02 retained `summary` as an optional suffix inside the brackets, document that rendering. Keep the change additive/clarifying where possible (the primitive itself survives — D-07). Mirror the existing `[lacon: truncated, …]` documentation style for the `max_bytes` marker so the marker convention reads consistently across primitives. ORM n/a; doc-only change, no rebuild needed for this file but the workspace suite still runs at wave merge.
  </action>
  <verify>
    <automated>grep -q '\[lacon:' docs/specs/filter-rule-schema.md && cargo build --workspace && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `docs/specs/filter-rule-schema.md` `collapse_repeated` entry describes the standardized `[lacon: …]` elision marker and matches the form implemented in 09-02-SUMMARY.md (grep `\[lacon:` near the collapse_repeated entry returns a hit).
    - If the free-form `summary` body was dropped in Plan 02, the spec explicitly flags the removal as a user-facing contract change; if retained as an in-bracket suffix, the spec documents that.
    - `cargo build --workspace && cargo test --workspace` passes (full wave-merge suite; no validate/loader test regresses on the still-parsing `summary` field).
  </acceptance_criteria>
  <done>Spec updated to describe the standardized collapse elision marker consistent with the implementation; user-facing contract change documented (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled rule output → assistant context | The git-status filtered output is what the model ingests; collapsing tabular signal or emitting a blending summary is the fidelity threat. |
| Spec doc → users authoring rules | `filter-rule-schema.md` is the user-facing contract; a stale `summary`/`{count}` description would misrepresent the implemented marker behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Spoofing | git-status collapse summary (removed) | mitigate | The tab-indented `"\t… {count} more…"` summary blended into the file lines it replaced (the D-07 failure mode). Mitigated by removing the signal-collapsing stage entirely (D-08) so no synthetic line is emitted in the success path; survival proven by byte-exact fixtures. |
| T-09-07 | Repudiation | spec vs implementation drift | mitigate | Spec updated in the same plan as the rule/fixture change, sourced from the Plan-02 SUMMARY, so the documented contract matches the emitted marker (D-12). |
| T-09-08 | Information disclosure | reduction-floor exemption | accept | Exempting git-status from the ≥50% floor means less byte reduction on that one rule. Accepted: tabular file lists ARE signal (RESEARCH Open Q2 / tsc precedent); fidelity outranks the reduction target here. Recorded in meta.yaml notes. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase (RESEARCH Standard Stack: Package Legitimacy Audit N/A). No install tasks; bundled YAML rebuild is normal rust-embed cargo build, not a fetch. |
</threat_model>

<verification>
- `cargo build --workspace && cargo test -p lacon-core --test bundled_rules` — git-status + tsc fixtures pass the walker contract (build-first: YAML is rust-embed'd).
- `grep -Fxf` survival check — every expected line is a verbatim input line.
- `grep '\[lacon:' docs/specs/filter-rule-schema.md` — spec documents the marker.
- `cargo build --workspace && cargo test --workspace` — wave-merge full suite.
- `cargo clippy --workspace --all-targets` + `cargo fmt --check` — before verify.
</verification>

<success_criteria>
- git-status success fixtures lose no verbatim tabular/repeated-prefix signal to collapse (success-criterion #3, D-08/D-11).
- The exempt-vs-narrow reduction-floor decision (Open Q2) is made explicitly and recorded in meta.yaml.
- No-fabrication class fixture proves every survivor is byte-identical to an input line (class, not literal `table table table` — Open Q1).
- tsc dedupe fixture confirmed signal-preserving and unchanged (D-10).
- filter-rule-schema.md documents the standardized `[lacon: …]` collapse marker (D-12), matching the implementation.
</success_criteria>

<output>
Create `.planning/phases/09-output-fidelity-safety-no-fabrication-on-dedupe-collapse-and/09-03-SUMMARY.md` when done
</output>
