---
phase: 05-bundled-tier-1-rules
plan: 03
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - bundled-rules/pkg-install.yaml
  - tests/fixtures/pkg-install/npm-deprecated/input.txt
  - tests/fixtures/pkg-install/npm-deprecated/expected.txt
  - tests/fixtures/pkg-install/npm-deprecated/meta.yaml
  - tests/fixtures/pkg-install/npm-e404/input.txt
  - tests/fixtures/pkg-install/npm-e404/expected.txt
  - tests/fixtures/pkg-install/npm-e404/meta.yaml
autonomous: true
requirements: [REQ-bundled-rules-tier1, REQ-bundled-rules-format]
must_haves:
  truths:
    - "pkg-install loads via the resolver and matches npm/pnpm/yarn install + pnpm i + pnpm add (D-10) via an any: list"
    - "pkg-install has NO rewrite block (D-11: --silent deletes the E404 error on failure)"
    - "Success pipeline drops deprecation/progress/funding noise, keeps the added/Done summary, reducing ≥50%"
    - "Failure fixture (exit 1) routes through on_error and preserves npm error / E404 lines"
  artifacts:
    - path: "bundled-rules/pkg-install.yaml"
      provides: "npm/pnpm/yarn install rule with multi-manager match, no rewrite, context-preserving on_error"
      contains: "id: pkg-install"
    - path: "tests/fixtures/pkg-install/npm-e404/meta.yaml"
      provides: "Failure fixture metadata exit_code 1 + must_keep_lines on the error"
      contains: "must_keep_lines"
  key_links:
    - from: "bundled-rules/pkg-install.yaml"
      to: "loader match resolution"
      via: "match.any list of per-manager command+args_prefix"
      pattern: "any:"
    - from: "bundled-rules/pkg-install.yaml"
      to: "on_error error survival"
      via: "keep_around_match on (error|ERR_|fatal)"
      pattern: "on_error"
---

<objective>
Author the `pkg-install` rule (npm / pnpm / yarn install) and its two fixtures.
This is the most-common command and the rule with the locked D-11 verdict: NO
`rewrite` block, because the only cross-manager silent flag (`--silent`) deletes
the error output entirely on failure — violating the no-drop contract. Reduction
is pipeline-side only.

Purpose: Prove the multi-manager `match.any` resolution and that the success
pipeline strips the heavy npm/pnpm/yarn install noise while on_error preserves the
install error signal.
Output: pkg-install.yaml (no rewrite), 2 fixtures from real npm output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-bundled-tier-1-rules/05-CONTEXT.md
@.planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md
@.planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md
@docs/specs/filter-rule-schema.md
@crates/lacon-core/tests/bundled_rules.rs

<interfaces>
<!-- npm 11.12.1, pnpm 11.1.2, yarn 1.22.22 ALL installed — capture real output. -->

match with OR semantics (MatchSpec, schema.rs:69-91; filter-rule-schema.md:46-66):
```yaml
match:
  any:
    - { command: npm,  args_prefix: [install] }
    - { command: pnpm, args_prefix: [install] }
    - { command: pnpm, args_prefix: [i] }
    - { command: pnpm, args_prefix: [add] }
    - { command: yarn, args_prefix: [install] }
```

RESEARCH section 1 + Directive 2 give verbatim captured output and the recommended
no-rewrite pkg-install design (lines 650-679). Drop targets: `^npm warn deprecated `,
`^\[WARN\] ` (pnpm), `^Progress: ` (pnpm), `^\s*[╭│╰]` (pnpm update box), `^Packages: \+`,
`^\++$` (pnpm bar), `^\[[0-9]/4\] ` (yarn steps), `^info ` (yarn), `^\d+ packages are looking for funding`,
`^  run \`npm fund\``. KEEP `^added \d+ packages`, `^\d+ vulnerabilities`, `^Done in `.

D-11: NO rewrite block. on_error keep_around_match on `(?i)(error|ERR_|fatal)` { before:1, after:10 } + keep_tail { lines: 40 }.
NO max_bytes (D-07). regex RE2 — no look-around (Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author pkg-install.yaml (multi-manager match, no rewrite, on_error)</name>
  <files>bundled-rules/pkg-install.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (section "Research Directive 2: pkg-install rewrite (D-11 verdict)" — the recommended design block lines 650-679; section "1. pkg-install" for the drop/keep targets per manager)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (the rule field layout + stage YAML keys + cross-cutting regex constraints)
    - crates/lacon-core/tests/fixtures/rules/parent.yaml (on_error nesting shape)
    - docs/specs/filter-rule-schema.md:46-66 (match operators incl any:)
  </read_first>
  <action>
    Create `bundled-rules/pkg-install.yaml`: `id: pkg-install`, `description: npm / pnpm / yarn install`, a `match.any` list of the five per-manager sub-matches (npm install, pnpm install, pnpm i, pnpm add, yarn install — D-10). NO `rewrite` block (D-11 verdict — adding `--silent` deletes the E404 error on failure on npm and pnpm; pipeline-side filtering is strictly better).

    Success `pipeline`: `strip_ansi` then the `drop_regex` set from RESEARCH lines 650-672 covering npm deprecation/funding noise, pnpm Progress/box/Packages/plus-bar/WARN, and yarn step/info lines. Do not drop `^added `, `^\d+ vulnerabilities`, or `^Done in ` (those are the kept summary signal). NO max_bytes (D-07).

    `on_error.pipeline`: `strip_ansi` → `keep_around_match: { pattern: '(?i)(error|ERR_|fatal)', before: 1, after: 10 }` → `keep_tail: { lines: 40 }`. Verify no look-around tokens and that the pnpm update-box drop regex `^\s*[╭│╰]` compiles (multibyte glyphs are fine as literal class members in RE2).
  </action>
  <verify>
    <automated>cargo run -q -p lacon-cli -- validate bundled-rules/pkg-install.yaml 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `lacon validate bundled-rules/pkg-install.yaml` succeeds.
    - File has `match.any` with all five manager sub-matches (`grep -c 'args_prefix' bundled-rules/pkg-install.yaml` >= 5).
    - NO rewrite key (`! grep -q 'rewrite:' bundled-rules/pkg-install.yaml`) per D-11.
    - NO max_bytes hand-placed (`! grep -q 'max_bytes' bundled-rules/pkg-install.yaml`).
    - No look-around tokens (`! grep -E '\(\?[=!<]' bundled-rules/pkg-install.yaml`).
    - Has a non-empty `pipeline` and an `on_error.pipeline`.
  </acceptance_criteria>
  <done>pkg-install loads, matches all five manager invocations, has no rewrite, and preserves errors via on_error.</done>
</task>

<task type="auto">
  <name>Task 2: pkg-install fixtures (npm-deprecated success + npm-e404 failure)</name>
  <files>tests/fixtures/pkg-install/npm-deprecated/input.txt, tests/fixtures/pkg-install/npm-deprecated/expected.txt, tests/fixtures/pkg-install/npm-deprecated/meta.yaml, tests/fixtures/pkg-install/npm-e404/input.txt, tests/fixtures/pkg-install/npm-e404/expected.txt, tests/fixtures/pkg-install/npm-e404/meta.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (section "1. pkg-install" — verbatim npm chatty-success-with-deprecated-deps output AND the npm E404 failure capture with exit 1)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (fixture layout + meta.yaml shape with exit_code)
    - docs/testing-rules.md:85-110 (regeneration recipe)
  </read_first>
  <action>
    npm 11.12.1 IS installed — capture REAL output (D-03). For the success fixture, install a few packages with KNOWN-deprecated transitive deps (e.g. `request`, `uuid@3`, `har-validator`) in a throwaway dir so the output carries multiple `npm warn deprecated` lines (Pitfall 5 — the chatty case clears the 50% floor). Save merged `npm install 2>&1` to `tests/fixtures/pkg-install/npm-deprecated/input.txt`. For the failure fixture, `npm install this-package-truly-does-not-exist-lacon-xyz 2>&1`, observe exit 1, save the E404 output to `tests/fixtures/pkg-install/npm-e404/input.txt`. Anonymize the user home path in the debug-log line.

    Regenerate each `expected.txt` by replaying the pkg-install pipeline against its input (do not hand-author).

    meta.yaml: success → `command: npm install`, `exit_code: 0`, `tool_version: "npm 11.12.1"`, `os: linux`, `exempt_from_reduction_check: false`. failure → `command: npm install`, `exit_code: 1`, `exempt_from_reduction_check: true`, `must_keep_lines: ["npm error code E404", "404 Not Found"]` (the error MUST survive).
  </action>
  <verify>
    <automated>cargo test --test bundled_rules 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test bundled_rules` passes for both pkg-install scenarios.
    - npm-deprecated reduction `<= 0.5` (not exempt).
    - npm-e404 (exit 1) routes through on_error; both must_keep_lines substrings survive.
    - input.txt files are real npm output, not synthetic.
  </acceptance_criteria>
  <done>Both pkg-install fixtures pass: chatty success reduces ≥50%, E404 failure preserves the error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule YAML → loader | Declarative pkg-install rule validated at load; RE2 regex |
| install output → runner | Static captured npm output; no untrusted runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-04 | Info disclosure / signal loss | a --silent rewrite suppressing the install error | mitigate | D-11 verdict enforced: NO rewrite block; on_error preserves the E404/error block (V5). The no-rewrite design IS the security control |
| T-5-02 | DoS | regex in pkg-install pipeline | accept | RE2 linear-time — ReDoS impossible |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | The deprecated-dep capture install is a throwaway scratch dir; its node_modules never enters this repo |

No HIGH-severity threats.
</threat_model>

<verification>
- `lacon validate bundled-rules/pkg-install.yaml` succeeds; no rewrite key present.
- `cargo test --test bundled_rules` passes for both pkg-install scenarios.
- `cargo test` full workspace green.
</verification>

<success_criteria>
pkg-install loads, matches all five manager invocations, carries no rewrite block
(D-11), reduces chatty install success ≥50%, and preserves the install error via on_error.
</success_criteria>

<output>
Create `.planning/phases/05-bundled-tier-1-rules/05-03-SUMMARY.md` when done.
</output>
