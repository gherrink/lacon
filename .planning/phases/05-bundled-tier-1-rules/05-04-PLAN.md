---
phase: 05-bundled-tier-1-rules
plan: 04
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - bundled-rules/cargo-build.yaml
  - tests/fixtures/cargo-build/multi-dep-warning/input.txt
  - tests/fixtures/cargo-build/multi-dep-warning/expected.txt
  - tests/fixtures/cargo-build/multi-dep-warning/meta.yaml
  - tests/fixtures/cargo-build/compile-error/input.txt
  - tests/fixtures/cargo-build/compile-error/expected.txt
  - tests/fixtures/cargo-build/compile-error/meta.yaml
autonomous: true
requirements: [REQ-bundled-rules-tier1, REQ-bundled-rules-format]
must_haves:
  truths:
    - "cargo-build matches cargo build AND cargo check (D-10) and loads via the resolver"
    - "Success pipeline drops Compiling/Updating/Locking/Finished repeats, preserves warning blocks with file:line, reducing ≥50%"
    - "Failure fixture (exit 101) routes through on_error and preserves error[E0xxx] / error: + the --> file:line context"
  artifacts:
    - path: "bundled-rules/cargo-build.yaml"
      provides: "cargo build/check rule (drop status-line noise + context-preserving on_error)"
      contains: "id: cargo-build"
    - path: "tests/fixtures/cargo-build/compile-error/meta.yaml"
      provides: "Failure fixture metadata exit_code 101 + must_keep_lines on the error code"
      contains: "exit_code: 101"
  key_links:
    - from: "bundled-rules/cargo-build.yaml"
      to: "success reduction"
      via: "drop_regex on Compiling/Updating/Locking/Finished"
      pattern: "drop_regex"
    - from: "bundled-rules/cargo-build.yaml"
      to: "on_error error survival"
      via: "keep_around_match on ^error"
      pattern: "on_error"
---

<objective>
Author the `cargo-build` rule (cargo build / cargo check) and its two fixtures.
The headline reduction is dropping the repeated "Compiling foo v0.x" lines (one per
dependency) plus the Updating/Locking/Finished status lines, while preserving
warning and error diagnostic blocks WITH their `-->` file:line context.

Purpose: Prove a blacklist-drop success pipeline that keeps variable-shape warning
blocks intact, and an on_error path that preserves the full compile-error context.
Output: cargo-build.yaml, 2 fixtures from real cargo output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-bundled-tier-1-rules/05-CONTEXT.md
@.planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md
@.planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md
@docs/specs/filter-rule-schema.md
@crates/lacon-core/tests/bundled_rules.rs

<interfaces>
<!-- cargo 1.95.0 installed — capture real output. RESEARCH section 2 = verbatim. -->

match (D-10 — both subcommands):
```yaml
match:
  any:
    - { command: cargo, args_prefix: [build] }
    - { command: cargo, args_prefix: [check] }
```

Success drops (RESEARCH section 2): `^\s*Compiling ` (the repeats — headline target),
`^\s*Updating `, `^\s*Locking `, `^\s*Finished `. KEEP everything else so warning
blocks (`warning:` + `-->` + `|` + `= note:`) pass through unmodified (blacklist —
recommended over keep_around_match because warning blocks have variable shape).

on_error (exit 101): `strip_ansi` → `keep_around_match: { pattern: '^error', before: 0, after: 15 }`
→ `keep_tail: { lines: 40 }`. Error prefixes: `error[E0308]:` (coded), `error:` (uncoded).
NO max_bytes (D-07). regex RE2 — no look-around.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author cargo-build.yaml (drop status lines, preserve diagnostics)</name>
  <files>bundled-rules/cargo-build.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (section "2. cargo-build" — verbatim success-with-warning AND failure output, exit 101, the DROP/KEEP guidance)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (rule field layout + stage keys + regex constraints)
    - crates/lacon-core/tests/fixtures/rules/parent.yaml (on_error nesting)
  </read_first>
  <action>
    Create `bundled-rules/cargo-build.yaml`: `id: cargo-build`, `description: cargo build / cargo check`, `match.any` with `cargo build` and `cargo check` (D-10).

    Success `pipeline`: `strip_ansi` then `drop_regex` for `^\s*Compiling `, `^\s*Updating `, `^\s*Locking `, `^\s*Finished `. Use the blacklist approach (RESEARCH recommendation) — do NOT add keep_regex, so warning diagnostic blocks (`warning:`, `-->`, `|`, `= note:`) survive intact. NO max_bytes (D-07).

    `on_error.pipeline`: `strip_ansi` → `keep_around_match: { pattern: '^error', before: 0, after: 15 }` → `keep_tail: { lines: 40 }`. The `keep_around_match` on `^error` captures both `error[E0308]:` and `error:` forms plus the `-->`/`|` context lines that follow. Verify both before AND after are present (required, no defaults). No look-around.
  </action>
  <verify>
    <automated>cargo run -q -p lacon-cli -- validate bundled-rules/cargo-build.yaml 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `lacon validate bundled-rules/cargo-build.yaml` succeeds.
    - `match.any` covers cargo build and cargo check.
    - NO max_bytes (`! grep -q max_bytes bundled-rules/cargo-build.yaml`); NO look-around (`! grep -E '\(\?[=!<]' bundled-rules/cargo-build.yaml`).
    - keep_around_match has both before and after keys.
    - Non-empty pipeline + on_error.pipeline.
  </acceptance_criteria>
  <done>cargo-build loads, drops the status-line noise, and preserves diagnostics via blacklist + on_error.</done>
</task>

<task type="auto">
  <name>Task 2: cargo-build fixtures (multi-dep-warning success + compile-error failure)</name>
  <files>tests/fixtures/cargo-build/multi-dep-warning/input.txt, tests/fixtures/cargo-build/multi-dep-warning/expected.txt, tests/fixtures/cargo-build/multi-dep-warning/meta.yaml, tests/fixtures/cargo-build/compile-error/input.txt, tests/fixtures/cargo-build/compile-error/expected.txt, tests/fixtures/cargo-build/compile-error/meta.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (section "2. cargo-build" verbatim captures; Pitfall 5 — capture ≥4 deps so dropping N Compiling lines clears the floor; A6 exit code 101)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (fixture + meta.yaml shape)
    - docs/testing-rules.md:85-110 (regeneration recipe)
  </read_first>
  <action>
    cargo 1.95.0 IS installed — capture REAL output (D-03). For the success fixture, build a throwaway crate with ≥4 dependencies and ONE deliberate unused-variable warning (so the warning block is preserved while the ≥4 Compiling lines drop — Pitfall 5 clears the 50% floor). Save `cargo build 2>&1` to `tests/fixtures/cargo-build/multi-dep-warning/input.txt`. For the failure fixture, introduce a type mismatch (`let x: i32 = "not a number";`), run `cargo build 2>&1`, observe exit 101, save to `tests/fixtures/cargo-build/compile-error/input.txt`. Anonymize the tmp path in the Compiling line.

    Regenerate each `expected.txt` by replaying the cargo-build pipeline against its input.

    meta.yaml: success → `command: cargo build`, `exit_code: 0`, `tool_version: "cargo 1.95.0"`, `os: linux`, `exempt_from_reduction_check: false`, `must_keep_lines: ["warning:"]` (the warning must survive the success reduction). failure → `command: cargo build`, `exit_code: 101`, `exempt_from_reduction_check: true`, `must_keep_lines: ["error[E0308]", "mismatched types"]`.
  </action>
  <verify>
    <automated>cargo test --test bundled_rules 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test bundled_rules` passes for both cargo-build scenarios.
    - multi-dep-warning reduction `<= 0.5`; the `warning:` line survives (must_keep_lines).
    - compile-error (exit 101) routes through on_error; `error[E0308]` + `mismatched types` survive.
    - input.txt files are real cargo output (≥4 deps), not synthetic.
  </acceptance_criteria>
  <done>Both cargo-build fixtures pass: multi-dep success reduces ≥50% while keeping the warning, error path preserves the full diagnostic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule YAML → loader | Declarative cargo-build rule validated at load; RE2 regex |
| cargo output → runner | Static captured output; no untrusted runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02 | DoS | regex in cargo-build pipeline | accept | RE2 linear-time — ReDoS impossible |
| T-5-03 | Info disclosure | on_error/success dropping the diagnostic | mitigate | must_keep_lines asserts warning: (success) and error[E0308]/mismatched types (failure) survive (V5) |
| T-5-SC | Tampering | cargo installs | accept | Throwaway capture crate deps are scratch; never enter this repo's Cargo.toml |

No HIGH-severity threats.
</threat_model>

<verification>
- `lacon validate bundled-rules/cargo-build.yaml` succeeds.
- `cargo test --test bundled_rules` passes for both cargo-build scenarios.
- `cargo test` full workspace green.
</verification>

<success_criteria>
cargo-build loads, reduces multi-dep success ≥50% while preserving warnings, and
preserves the full compile-error context via on_error.
</success_criteria>

<output>
Create `.planning/phases/05-bundled-tier-1-rules/05-04-SUMMARY.md` when done.
</output>
