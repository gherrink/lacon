---
phase: 05-bundled-tier-1-rules
plan: 05
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - bundled-rules/git-status.yaml
  - tests/fixtures/git-status/many-untracked/input.txt
  - tests/fixtures/git-status/many-untracked/expected.txt
  - tests/fixtures/git-status/many-untracked/meta.yaml
  - tests/fixtures/git-status/not-a-repo/input.txt
  - tests/fixtures/git-status/not-a-repo/expected.txt
  - tests/fixtures/git-status/not-a-repo/meta.yaml
autonomous: true
requirements: [REQ-bundled-rules-tier1, REQ-bundled-rules-format]
must_haves:
  truths:
    - "git-status matches git status (any args) and loads via the resolver"
    - "Success pipeline collapses the tab-indented untracked-file block via collapse_repeated, reducing ≥50% on the chatty -uall case"
    - "Failure fixture (exit 128, not a repo) preserves the fatal: line"
  artifacts:
    - path: "bundled-rules/git-status.yaml"
      provides: "git status rule using collapse_repeated on the ^\\t file block"
      contains: "collapse_repeated"
    - path: "tests/fixtures/git-status/many-untracked/meta.yaml"
      provides: "Chatty -uall success fixture metadata exit_code 0"
      contains: "exit_code: 0"
  key_links:
    - from: "bundled-rules/git-status.yaml"
      to: "success reduction"
      via: "collapse_repeated on tab-indented file lines"
      pattern: "collapse_repeated"
---

<objective>
Author the `git-status` rule and its two fixtures. The reduction strategy is
distinct: instead of dropping lines, COLLAPSE the long tab-indented file block under
"Untracked files:" (which can be hundreds of lines in a monorepo) into a few
examples plus a summary, via `collapse_repeated`. git status rarely fails, so the
failure fixture is the "not a git repository" case.

Purpose: Exercise the collapse_repeated primitive in a bundled rule (the only Tier 1
rule that uses it as its primary reducer) and confirm the `summary` YAML key (NOT
summary_template — PATTERNS delta #1).
Output: git-status.yaml, 2 fixtures from real git output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-bundled-tier-1-rules/05-CONTEXT.md
@.planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md
@.planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md
@docs/specs/filter-rule-schema.md
@crates/lacon-core/tests/bundled_rules.rs

<interfaces>
<!-- git 2.53.0 installed — capture real output. RESEARCH section 9 = verbatim. -->

match: `{ command: git, args_prefix: [status] }` (D-10 — any args; collapse handles both default and -uall forms).

collapse_repeated YAML key is `summary` NOT `summary_template` (PATTERNS delta #1; schema.rs:244 CollapseArgs):
```yaml
- collapse_repeated:
    pattern: '^\t'
    max_kept: 5
    summary: '… {count} more changed/untracked files'
```
`{count}` is replaced with the number of dropped lines. The tab-indented file lines
under "Untracked files:" / "Changes not staged" are the bulk (RESEARCH section 9).

Optional drop of the hint lines: `drop_regex: '^\s*\(use '`. KEEP `^On branch`, `^Changes`, `^Untracked files:`.
on_error (exit 128, not a repo): keep_regex '^fatal:' (small — likely exempt). NO max_bytes (D-07). regex RE2 — no look-around.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author git-status.yaml (collapse_repeated on the file block)</name>
  <files>bundled-rules/git-status.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (section "9. git-status" — verbatim default AND -uall captures, the collapse_repeated reduction strategy, the fatal: failure note)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (Verification Notes delta #1 — summary NOT summary_template; the stage YAML keys table)
    - docs/specs/filter-rule-schema.md:128-137 (collapse_repeated semantics + {count} placeholder)
  </read_first>
  <action>
    Create `bundled-rules/git-status.yaml`: `id: git-status`, `description: git status`, `match: { command: git, args_prefix: [status] }`.

    Success `pipeline`: `strip_ansi` then `collapse_repeated: { pattern: '^\t', max_kept: 5, summary: '… {count} more changed/untracked files' }` (use the `summary` key, NOT `summary_template` — PATTERNS delta #1). Optionally `drop_regex: '^\s*\(use '` to remove the git hint lines. Do NOT drop the section headers (`^On branch`, `^Changes`, `^Untracked files:`). NO max_bytes (D-07).

    `on_error.pipeline` (exit 128 when run outside a repo): `strip_ansi` → `keep_regex: '^fatal:'`. This block is small; the failure fixture will be exempt from the reduction check.
  </action>
  <verify>
    <automated>cargo run -q -p lacon-cli -- validate bundled-rules/git-status.yaml 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `lacon validate bundled-rules/git-status.yaml` succeeds.
    - Uses `summary:` not `summary_template:` (`grep -q 'summary:' bundled-rules/git-status.yaml && ! grep -q 'summary_template' bundled-rules/git-status.yaml`).
    - NO max_bytes; NO look-around tokens.
    - Non-empty pipeline + on_error.pipeline.
  </acceptance_criteria>
  <done>git-status loads and collapses the tab-indented file block, preserving section headers and the fatal: error path.</done>
</task>

<task type="auto">
  <name>Task 2: git-status fixtures (many-untracked success + not-a-repo failure)</name>
  <files>tests/fixtures/git-status/many-untracked/input.txt, tests/fixtures/git-status/many-untracked/expected.txt, tests/fixtures/git-status/many-untracked/meta.yaml, tests/fixtures/git-status/not-a-repo/input.txt, tests/fixtures/git-status/not-a-repo/expected.txt, tests/fixtures/git-status/not-a-repo/meta.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (section "9. git-status" — the -uall 176-line chatty capture for the primary fixture; the fatal: not-a-repo failure)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (fixture + meta.yaml shape)
    - docs/testing-rules.md:85-110 (regeneration recipe)
  </read_first>
  <action>
    git 2.53.0 IS installed — capture REAL output (D-03). For the success fixture, in a throwaway git repo create many untracked files (≥100, e.g. `mkdir gen && touch gen/f{1..120}.rs`) and run `git status -uall 2>&1` so the tab-indented block is long (Pitfall 5 — the chatty case clears the 50% floor; default git status collapses dirs and would be too small). Save to `tests/fixtures/git-status/many-untracked/input.txt`. For the failure fixture, run `git status 2>&1` in a NON-repo dir (`/tmp/not-a-repo`), observe exit 128 + `fatal: not a git repository`, save to `tests/fixtures/git-status/not-a-repo/input.txt`.

    Regenerate each `expected.txt` by replaying the git-status pipeline against its input. Confirm the collapse summary line (`… N more …`) appears in the success expected output.

    meta.yaml: success → `command: git status -uall`, `exit_code: 0`, `tool_version: "git 2.53.0"`, `os: linux`, `exempt_from_reduction_check: false`. failure → `command: git status`, `exit_code: 128`, `exempt_from_reduction_check: true`, `must_keep_lines: ["fatal: not a git repository"]`.
  </action>
  <verify>
    <automated>cargo test --test bundled_rules 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test bundled_rules` passes for both git-status scenarios.
    - many-untracked reduction `<= 0.5`; the collapse summary line is present in expected.txt.
    - not-a-repo (exit 128) routes through on_error; `fatal: not a git repository` survives.
    - input.txt files are real git output, not synthetic.
  </acceptance_criteria>
  <done>Both git-status fixtures pass: -uall success collapses ≥50%, not-a-repo failure preserves the fatal: line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule YAML → loader | Declarative git-status rule validated at load; RE2 regex |
| git output → runner | Static captured output; no untrusted runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02 | DoS | regex in git-status pipeline | accept | RE2 linear-time — ReDoS impossible |
| T-5-03 | Info disclosure | on_error dropping the fatal: line | mitigate | must_keep_lines asserts fatal: not a git repository survives (V5) |

No HIGH-severity threats. No package installs in this plan.
</threat_model>

<verification>
- `lacon validate bundled-rules/git-status.yaml` succeeds (summary key correct).
- `cargo test --test bundled_rules` passes for both git-status scenarios.
- `cargo test` full workspace green.
</verification>

<success_criteria>
git-status loads, collapses the tab-indented file block ≥50% on the -uall case via
collapse_repeated, and preserves the fatal: error on the not-a-repo failure.
</success_criteria>

<output>
Create `.planning/phases/05-bundled-tier-1-rules/05-05-SUMMARY.md` when done.
</output>
