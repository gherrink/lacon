---
phase: 05-bundled-tier-1-rules
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/lacon-core/tests/bundled_rules.rs
  - docs/testing-rules.md
  - docs/bundled-rules-roadmap.md
autonomous: true
requirements: [REQ-bundled-rules-format]
must_haves:
  truths:
    - "cargo test --test bundled_rules compiles and runs with zero fixtures present (walks an empty tree green) — the runner is subprocess-free byte replay via RuleLoader::new(None).resolve -> Runner::new -> filter_bytes (D-01) and lives at crates/lacon-core/tests/bundled_rules.rs (D-09)"
    - "The runner reads meta.yaml's exit_code field and passes it to filter_bytes so failure fixtures hit the on_error branch (D-02)"
    - "Each fixture gets three assertions: byte-exact match via the join-with-newline vs expected.trim_end_matches newline idiom (D-04), plus the >=50% reduction threshold on non-exempt primary success fixtures and the must_keep_lines substring survival (D-05)"
    - "docs/testing-rules.md documents the exit_code meta.yaml field"
    - "docs/bundled-rules-roadmap.md carries a per-rule doc note for all 10 Tier 1 rules"
    - "docs/bundled-rules-roadmap.md pkg-install Notes no longer reference --reporter=silent/--silent; the note states NO rewrite block per D-11 (pipeline-side filtering only, because --silent deletes the error on failure)"
  artifacts:
    - path: "crates/lacon-core/tests/bundled_rules.rs"
      provides: "Fixture-walking integration test (replay via filter_bytes + 3 per-fixture assertions)"
      min_lines: 80
      contains: "filter_bytes"
    - path: "docs/testing-rules.md"
      provides: "meta.yaml schema documenting exit_code"
      contains: "exit_code"
    - path: "docs/bundled-rules-roadmap.md"
      provides: "Per-rule doc notes for the 10 Tier 1 rules"
      contains: "pkg-install"
  key_links:
    - from: "crates/lacon-core/tests/bundled_rules.rs"
      to: "lacon_core::rules::loader::RuleLoader"
      via: "RuleLoader::new(None).resolve(rule_id)"
      pattern: "RuleLoader::new\\(None\\)"
    - from: "crates/lacon-core/tests/bundled_rules.rs"
      to: "lacon_core::runtime::Runner::filter_bytes"
      via: "runner.filter_bytes(input, meta.exit_code, 0, &meta.command, None)"
      pattern: "filter_bytes"
---

<objective>
Build the Wave 0 foundation that every rule plan depends on: the fixture-walking
integration test runner (`crates/lacon-core/tests/bundled_rules.rs`), the
`exit_code` extension to the `meta.yaml` schema doc (D-02), and the per-rule doc
notes in the bundled-rules roadmap (REQ-bundled-rules-format). Nothing in this
phase can be asserted until this runner exists.

Purpose: The runner is data-driven — it walks `tests/fixtures/<rule-id>/<scenario>/`,
loads each rule from the embedded bundled layer, replays `input.txt` through
`Runner::filter_bytes` selecting the ADR-0010 branch from `meta.exit_code`, and
runs three assertions (byte-exact, ≥50% reduction, must_keep_lines). It must go
green on an EMPTY fixture tree so subsequent rule plans turn it green incrementally.
Output: one new Rust test file, two doc edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bundled-tier-1-rules/05-CONTEXT.md
@.planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md
@.planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md
@docs/testing-rules.md
@docs/bundled-rules-roadmap.md

<interfaces>
<!-- Verified public API the runner consumes. Use directly — no exploration. -->

From crates/lacon-core/src/rules/loader.rs:
```rust
impl RuleLoader {
    pub fn new(project_dir: Option<PathBuf>) -> Self;             // None → hermetic bundled-only (D-01)
    pub fn resolve(&mut self, rule_id: &str) -> Result<ResolvedRule, ValidationError>;
}
```

From crates/lacon-core/src/runtime/mod.rs:
```rust
pub struct RunOptions { /* Default exists */ }
impl Runner {
    pub fn new(resolved: ResolvedRule, options: RunOptions) -> Self;
    pub fn filter_bytes(
        &mut self,
        merged_bytes: &[u8],
        exit_code: i32,
        duration_ms: u64,
        command_raw: &str,
        project_path: Option<String>,
    ) -> Result<Vec<String>, RuntimeError>;
}
```
Branch logic (runtime/mod.rs:457-473): exit_code == 0 → success pipeline;
!= 0 with on_error present → on_error pipeline; != 0 with none → raw passthrough.

Test imports (verified from runtime_filter_bytes.rs:13-17):
`use lacon_core::rules::loader::RuleLoader;` and `use lacon_core::runtime::{RunOptions, Runner};`

`serde_saphyr 0.0.26` is the workspace YAML parser (same one the loader uses);
deserialize meta.yaml via `serde_saphyr::from_str::<FixtureMeta>(&s)` (mirror parse_one, loader.rs:439).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixture-walking runner with FixtureMeta + 3 assertions</name>
  <files>crates/lacon-core/tests/bundled_rules.rs</files>
  <read_first>
    - crates/lacon-core/tests/primitives.rs:1-44 (the fixture_path workspace-root idiom AND the `join("\n")` vs `trim_end_matches('\n')` byte-compare idiom — D-04)
    - crates/lacon-core/tests/runtime_filter_bytes.rs:1-45 (filter_bytes import path + call style + make_rule builder)
    - crates/lacon-cli/src/commands/explain.rs:116-159 (the resolve → Runner::new → filter_bytes call sequence)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (sections "crates/lacon-core/tests/bundled_rules.rs" + "tests/fixtures/<id>/<scenario>/meta.yaml" — the FixtureMeta struct and replay helper to copy verbatim)
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md ("Test runner skeleton" + "meta.yaml deserialize struct" code blocks)
  </read_first>
  <behavior>
    - With no fixture directories present, the test discovers zero fixtures and passes (does NOT panic on an empty/absent tree).
    - For each `tests/fixtures/<rule-id>/<scenario>/` containing input.txt + expected.txt + meta.yaml: replay returns the rule's filtered output for that exit_code branch.
    - exit_code from meta.yaml routes the branch: a meta with exit_code != 0 against a rule with on_error exercises the on_error pipeline (NOT the success pipeline).
    - Byte-exact assertion uses `out.join("\n")` vs `expected.trim_end_matches('\n')` (D-04) — a single trailing newline in expected.txt does not fail the test.
    - Reduction assertion `len(expected) as f64 / len(input) as f64 <= 0.5` runs ONLY when `exempt_from_reduction_check` is false (D-05).
    - Every substring in `must_keep_lines` must be present in the joined output, else the test fails with a message naming the missing substring (D-05).
  </behavior>
  <action>
    Create `crates/lacon-core/tests/bundled_rules.rs` (cargo auto-discovers `tests/*.rs`; this filename makes `cargo test --test bundled_rules` resolve, D-09).

    Define `struct FixtureMeta` exactly as in PATTERNS.md: required `command: String` and `exit_code: i32` (D-02), plus `#[serde(default)]` fields `tool_version: Option<String>`, `exempt_from_reduction_check: bool`, `must_keep_lines: Vec<String>`, `os: Option<String>`, `notes: Option<String>`, `tool_version`/`captured_at` optional. Deserialize with `serde_saphyr::from_str::<FixtureMeta>` (mirror parse_one, loader.rs:439). Do NOT add `#[serde(deny_unknown_fields)]` to FixtureMeta — fixtures may carry `captured_at` and future provenance keys the runner ignores.

    Path helper: copy `fixture_path` from primitives.rs but join `tests/fixtures` (NOT `tests/fixtures/primitives`) then `<rule-id>/<scenario>`, using `env!("CARGO_MANIFEST_DIR")/../..`.

    Replay helper exactly per PATTERNS.md: `RuleLoader::new(None)` (hermetic bundled-only, D-01) → `resolve(rule_id)` → `Runner::new(resolved, RunOptions::default())` → `filter_bytes(input_bytes, meta.exit_code, 0, &meta.command, None)`.

    Discovery: read `env!("CARGO_MANIFEST_DIR")/../../tests/fixtures`, iterate each subdir whose name is NOT `primitives` (skip the existing primitive fixtures), then iterate each scenario subdir that contains `meta.yaml`. For each, run the 3 assertions (D-05): (1) byte-exact via the D-04 idiom; (2) reduction `<= 0.5` gated on `!meta.exempt_from_reduction_check`; (3) every `must_keep_lines` substring present in the joined output. Use plain `assert_eq!`/`assert!` with descriptive messages (D-09 — do NOT introduce `insta`). On an absent or empty `tests/fixtures` rule set the test must still pass.

    Emit one logical sub-assertion per fixture with the `<rule-id>/<scenario>` slug in every panic message so failures are diagnosable. A single `#[test] fn all_bundled_rule_fixtures()` driver iterating the tree is acceptable (mirrors the data-driven shape; per-fixture `#[test]` is not required).
  </action>
  <verify>
    <automated>cargo test --test bundled_rules 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test bundled_rules` compiles and exits 0 with zero fixtures present (walks empty tree green).
    - The file imports `RuleLoader`, `Runner`, `RunOptions` from `lacon_core` and calls `filter_bytes` with `meta.exit_code` as the 2nd arg (`grep -n 'filter_bytes' crates/lacon-core/tests/bundled_rules.rs` shows the call).
    - `grep -n 'exit_code' crates/lacon-core/tests/bundled_rules.rs` shows exit_code is read from FixtureMeta and threaded to filter_bytes.
    - No `insta` usage (`! grep -q insta crates/lacon-core/tests/bundled_rules.rs`).
    - `cargo test` (full workspace) shows no regression in existing suites.
  </acceptance_criteria>
  <done>Runner compiles, walks the (currently empty) fixture tree green, branch-selects on meta.exit_code, and applies all 3 D-05 assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Document exit_code in meta.yaml schema + roadmap doc notes for all 10 rules</name>
  <files>docs/testing-rules.md, docs/bundled-rules-roadmap.md</files>
  <read_first>
    - docs/testing-rules.md:37-68 (the meta.yaml block at 39-45 to extend, and the "What each test verifies" section that already documents exempt_from_reduction_check + must_keep_lines)
    - docs/bundled-rules-roadmap.md:1-52 (the Tier 1 table — find the pkg-install row whose Notes cell currently says "Add `--reporter=silent` where supported" — plus "Format expectations for new rules")
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (section "docs/testing-rules.md (MODIFIED)" — the exact exit_code edit)
    - .planning/phases/05-bundled-tier-1-rules/05-CONTEXT.md (D-11 — the load-bearing decision for the pkg-install note edit)
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (per-rule reduction intent for the doc-note wording)
  </read_first>
  <action>
    Edit `docs/testing-rules.md` (NOTE: real path is `docs/testing-rules.md`, NOT `docs/specs/testing-rules.md`): add `exit_code: 0` to the `meta.yaml` example block (currently lines 39-45) with a one-line note that exit_code selects the filter branch per ADR-0010 (0 → success pipeline; nonzero + on_error → on_error pipeline; nonzero + none → raw passthrough), and that cargo build/test failures are exit 101 (RESEARCH A6), so fixtures must record the ACTUAL observed code. No change to the "What each test verifies" section (it already covers the other meta fields).

    Edit `docs/bundled-rules-roadmap.md` — TWO required edits:

    (1) REPLACE the pkg-install Notes text. The Tier 1 table's `pkg-install` row currently has a Notes cell reading "Most common. Add `--reporter=silent` where supported, drop deprecation warnings, collapse progress." DELETE the `--reporter=silent` reference entirely and substitute the D-11 rationale: pkg-install carries NO `rewrite` block — reduction is pipeline-side only (drop deprecation warnings, collapse progress) because `--silent`/`--reporter=silent` deletes the error (e.g. the E404) on a failed install, which would destroy the on_error signal. After this edit, the literal string `reporter=silent` MUST NOT appear anywhere in docs/bundled-rules-roadmap.md.

    (2) Add a per-rule trade-off doc note for each of the 10 (REQ-bundled-rules-format wants "a short doc note … describing the trade-off"). Either enrich the existing Notes column or append a short "## Tier 1 — implementation notes" subsection with one bullet per rule id, each naming the reduction strategy (e.g. pkg-install: drop deprecation/progress noise, NO rewrite block per D-11 because --silent deletes the error on failure — pipeline-side filtering only; cargo-build: drop Compiling/Updating/Locking/Finished, preserve warning/error file:line blocks; cargo-test/vitest/jest/pytest: drop per-test PASS lines, keep summary + on_error failures; tsc: ANSI-strip + dedupe + tail, failure path is primary; eslint: keep error/warning detail with file path, drop pure-pass summaries; git-status: collapse the tab-indented untracked-file block; docker-build: drop CACHED/DONE/sha256 progress, keep step headers + on_error framed ERROR block). All 10 rule ids must appear.
  </action>
  <verify>
    <automated>grep -q 'exit_code' docs/testing-rules.md && ! grep -q 'reporter=silent' docs/bundled-rules-roadmap.md && for r in pkg-install cargo-build cargo-test vitest jest pytest tsc eslint git-status docker-build; do grep -q "$r" docs/bundled-rules-roadmap.md || { echo "MISSING note: $r"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep exit_code docs/testing-rules.md` returns the new field with its ADR-0010 branch note.
    - The old pkg-install Notes text is gone: `! grep -q 'reporter=silent' docs/bundled-rules-roadmap.md` (and no `--silent` rewrite recommendation remains for pkg-install).
    - The pkg-install note now reflects D-11: NO rewrite block, pipeline-side filtering only because --silent deletes the error on failure.
    - All 10 rule ids appear in docs/bundled-rules-roadmap.md with a trade-off note (the verify loop prints OK).
    - The doc edit does NOT remove existing meta fields (command, tool_version, captured_at, os, notes) or the existing exempt_from_reduction_check / must_keep_lines documentation.
  </acceptance_criteria>
  <done>meta.yaml schema documents exit_code with its branch semantics; the pkg-install Notes reflect D-11 (no rewrite, no reporter=silent); all 10 Tier 1 rules carry a roadmap doc note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture files → test runner | Static checked-in text read by the test; no untrusted runtime input |
| rule YAML → loader | Declarative YAML validated at load (regex compiled via linear-time RE2 crate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Tampering | fixture path resolution in bundled_rules.rs | mitigate | Path is `env!(CARGO_MANIFEST_DIR)/../../tests/fixtures/<id>/<scenario>` — fixed prefix, no user input, no traversal (V12) |
| T-5-02 | DoS | regex in any rule pipeline | accept | Rust `regex` crate is linear-time RE2 by construction — ReDoS impossible (V5); no mitigation code needed |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO registry packages (RESEARCH Package Legitimacy Audit: N/A). npx fixture-capture in later plans uses throwaway scratch projects whose deps never enter this repo |

No HIGH-severity threats. Security surface is minimal (declarative YAML + static fixtures).
</threat_model>

<verification>
- `cargo test --test bundled_rules` compiles and passes with the empty fixture tree.
- `cargo test` full workspace green (no Phase 1-4 regression).
- `grep exit_code docs/testing-rules.md`, the `! grep reporter=silent` check, and the 10-rule-id roadmap loop all succeed.
</verification>

<success_criteria>
The fixture-walking runner exists and goes green on an empty tree, reads exit_code
from meta.yaml to select the ADR-0010 branch, applies the 3 D-05 assertions, the
meta.yaml schema doc documents exit_code, the pkg-install Notes reflect D-11 (no
rewrite / no reporter=silent), and all 10 Tier 1 rules have a roadmap doc note.
Every downstream rule plan can now drop fixtures and turn this test green.
</success_criteria>

<output>
Create `.planning/phases/05-bundled-tier-1-rules/05-01-SUMMARY.md` when done.
</output>
