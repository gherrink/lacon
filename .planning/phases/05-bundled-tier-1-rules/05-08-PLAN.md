---
phase: 05-bundled-tier-1-rules
plan: 08
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - bundled-rules/pytest.yaml
  - tests/fixtures/pytest/verbose-pass/input.txt
  - tests/fixtures/pytest/verbose-pass/expected.txt
  - tests/fixtures/pytest/verbose-pass/meta.yaml
  - tests/fixtures/pytest/assert-failure/input.txt
  - tests/fixtures/pytest/assert-failure/expected.txt
  - tests/fixtures/pytest/assert-failure/meta.yaml
autonomous: true
requirements: [REQ-bundled-rules-tier1, REQ-bundled-rules-format]
must_haves:
  truths:
    - "pytest matches pytest AND python -m pytest (D-10) and loads via the resolver"
    - "Success pipeline drops the verbose PASSED per-test lines + the session header, keeps the final summary banner, reducing ≥50%"
    - "Failure fixture (exit 1) routes through on_error and preserves the FAILURES block + E assertion lines + summary"
    - "The extends-vs-copy decision from plan 05-02 is applied consistently"
  artifacts:
    - path: "bundled-rules/pytest.yaml"
      provides: "pytest rule (drop verbose PASSED + header; on_error keeps tracebacks)"
      contains: "id: pytest"
    - path: "tests/fixtures/pytest/assert-failure/meta.yaml"
      provides: "Failure fixture metadata exit_code 1 + must_keep_lines on the assertion"
      contains: "must_keep_lines"
  key_links:
    - from: "bundled-rules/pytest.yaml"
      to: "success reduction"
      via: "drop_regex on PASSED per-test lines + session header"
      pattern: "drop_regex"
    - from: "bundled-rules/pytest.yaml"
      to: "on_error error survival"
      via: "keep on E / FAILED / FAILURES"
      pattern: "on_error"
---

<objective>
Author the `pytest` rule (member of the test-runner family) and its two fixtures.
Drop the verbose per-test `PASSED` lines and the session header, keep the final
`=== N passed ===` summary; on failure preserve the FAILURES block, `E ` assertion
lines, and the summary. Apply the extends-vs-copy verdict recorded by plan 05-02.

Purpose: Second member of the test-runner family. Depends on the 05-02 spike so the
extends-or-copy structure is consistent across cargo-test/pytest/vitest/jest.
Output: pytest.yaml, 2 fixtures from real pytest output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-bundled-tier-1-rules/05-CONTEXT.md
@.planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md
@.planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md
@.planning/phases/05-bundled-tier-1-rules/05-02-SUMMARY.md
@docs/specs/filter-rule-schema.md
@crates/lacon-core/tests/bundled_rules.rs

<interfaces>
<!-- pytest 9.0.2 installed — capture real output. RESEARCH "pytest" section = verbatim. -->

match (D-10):
```yaml
match:
  any:
    - { command: pytest }
    - { command: python, args_prefix: [-m, pytest] }
```

Success drops (RESEARCH pytest section): `PASSED\s+\[` (verbose per-test PASS),
`^platform `, `^cachedir:`, `^rootdir:`, `^plugins:`, `^collecting`, `^collected`,
the `=== test session starts ===` banner. KEEP `^=+ \d+ passed.* =+$` (final summary banner).

on_error (exit 1): keep the `=== FAILURES ===` block, `^E ` lines, `^>` lines,
`:\d+: \w+Error$`, `^FAILED `, the final `=== N failed ===` banner. Either keep_around_match
on `^FAILED|^E |^>` + keep_tail, OR adjacent keep_regex whitelist.

extends-vs-copy: APPLY the 05-02 verdict (read 05-02-SUMMARY). If extends kept,
pytest may `extends: bundled/test-base` and inherit shared stages. If copy fallback,
inline the full pipeline + on_error. NO max_bytes (D-07). regex RE2 — no look-around.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author pytest.yaml (apply 05-02 extends/copy verdict)</name>
  <files>bundled-rules/pytest.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-02-SUMMARY.md (THE extends-vs-copy verdict — apply it; if extends kept, see how cargo-test extends test-base)
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (the "pytest" subsection — verbatim verbose-pass AND assert-failure captures, exit 1, DROP/KEEP guidance)
    - bundled-rules/cargo-test.yaml (the sibling test-runner authored in 05-02 — mirror its structure for consistency)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (rule field layout + stage keys + Pitfall 1 adjacency)
  </read_first>
  <action>
    Create `bundled-rules/pytest.yaml`: `id: pytest`, `description: pytest / python -m pytest`, `match.any` with `pytest` and `python -m pytest` (D-10). Structure it consistently with the 05-02 verdict: if 05-02 kept `extends`, use `extends: bundled/test-base` and add only the pytest-specific stages; if 05-02 used copy-the-parent, inline the full success + on_error pipelines.

    Success `pipeline` (pytest-specific): `strip_ansi` (or inherited from base) then `drop_regex` for `PASSED\s+\[` (verbose per-test PASS — headline target), `^platform `, `^cachedir:`, `^rootdir:`, `^plugins:`, `^collecting`, `^collected`, and the session-start banner line. Do NOT drop the final `=== N passed ===` summary banner. NO max_bytes (D-07).

    `on_error.pipeline` (exit 1): keep the failure signal — `keep_around_match` on `^FAILED|^E |^>` (before/after both set) OR an adjacent `keep_regex` whitelist for `(=== FAILURES ===|^E |^>|^FAILED |Error$|passed.* ===)`, then `keep_tail: { lines: 60 }`. strip_ansi first (Pitfall 1); no look-around.
  </action>
  <verify>
    <automated>cargo run -q -p lacon-cli -- validate bundled-rules/pytest.yaml 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `lacon validate bundled-rules/pytest.yaml` succeeds (incl. extends resolution if used).
    - `match.any` covers pytest and python -m pytest.
    - Structure matches the 05-02 verdict (extends OR fully-inlined copy — consistent with cargo-test).
    - NO max_bytes; NO look-around tokens.
    - Non-empty pipeline (or inherited via extends) + on_error.pipeline (own or inherited).
  </acceptance_criteria>
  <done>pytest loads, drops verbose PASS noise, preserves the summary, and keeps tracebacks via on_error — structured consistently with the family.</done>
</task>

<task type="auto">
  <name>Task 2: pytest fixtures (verbose-pass success + assert-failure failure)</name>
  <files>tests/fixtures/pytest/verbose-pass/input.txt, tests/fixtures/pytest/verbose-pass/expected.txt, tests/fixtures/pytest/verbose-pass/meta.yaml, tests/fixtures/pytest/assert-failure/input.txt, tests/fixtures/pytest/assert-failure/expected.txt, tests/fixtures/pytest/assert-failure/meta.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (the "pytest" subsection — verbatim captures; the note that DEFAULT pytest is dot-progress and tiny, so capture `-v` for the reductive primary fixture, Pitfall 5)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (fixture + meta.yaml shape)
    - docs/testing-rules.md:85-110 (regeneration recipe)
  </read_first>
  <action>
    pytest 9.0.2 IS installed — capture REAL output (D-03). For the success fixture, write a throwaway test file with ≥8 passing tests and run `pytest -v 2>&1` (verbose so the per-test PASSED lines exist to drop — Pitfall 5; default dot-progress is too small) → `tests/fixtures/pytest/verbose-pass/input.txt`. For the failure fixture, write ≥2 failing tests (an assertion failure + a KeyError), run `pytest 2>&1`, observe exit 1, save the FAILURES output → `tests/fixtures/pytest/assert-failure/input.txt`. Anonymize the tmp rootdir path.

    Regenerate each `expected.txt` by replaying the pytest pipeline against its input.

    meta.yaml: success → `command: pytest -v`, `exit_code: 0`, `tool_version: "pytest 9.0.2"`, `os: linux`, `exempt_from_reduction_check: false`. failure → `command: pytest`, `exit_code: 1`, `exempt_from_reduction_check: true`, `must_keep_lines: ["assert 6 == 7", "FAILED"]` (or the actual assertion text captured).
  </action>
  <verify>
    <automated>cargo test --test bundled_rules 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test bundled_rules` passes for both pytest scenarios.
    - verbose-pass reduction `<= 0.5` (the per-test PASSED drops clear the floor).
    - assert-failure (exit 1) routes through on_error; the assertion + FAILED lines survive.
    - input.txt files are real pytest `-v` output, not synthetic.
  </acceptance_criteria>
  <done>Both pytest fixtures pass: verbose success reduces ≥50%, assertion failure preserves the traceback signal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule YAML → loader | Declarative pytest rule validated at load; RE2 regex |
| pytest output → runner | Static captured output; no untrusted runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02 | DoS | regex in pytest pipeline | accept | RE2 linear-time — ReDoS impossible |
| T-5-03 | Info disclosure | on_error dropping the traceback | mitigate | must_keep_lines asserts the assertion text + FAILED survive (V5) |

No HIGH-severity threats. No registry package installs.
</threat_model>

<verification>
- `lacon validate bundled-rules/pytest.yaml` succeeds.
- `cargo test --test bundled_rules` passes for both pytest scenarios.
- `cargo test` full workspace green.
</verification>

<success_criteria>
pytest loads (structured per the 05-02 verdict), reduces verbose success ≥50%, and
preserves the failure traceback via on_error.
</success_criteria>

<output>
Create `.planning/phases/05-bundled-tier-1-rules/05-08-SUMMARY.md` when done.
</output>
