---
phase: 05-bundled-tier-1-rules
plan: 09
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - bundled-rules/vitest.yaml
  - bundled-rules/jest.yaml
  - tests/fixtures/vitest/mixed-run/input.txt
  - tests/fixtures/vitest/mixed-run/expected.txt
  - tests/fixtures/vitest/mixed-run/meta.yaml
  - tests/fixtures/vitest/all-pass/input.txt
  - tests/fixtures/vitest/all-pass/expected.txt
  - tests/fixtures/vitest/all-pass/meta.yaml
  - tests/fixtures/jest/suite-failure/input.txt
  - tests/fixtures/jest/suite-failure/expected.txt
  - tests/fixtures/jest/suite-failure/meta.yaml
  - tests/fixtures/jest/all-pass/input.txt
  - tests/fixtures/jest/all-pass/expected.txt
  - tests/fixtures/jest/all-pass/meta.yaml
autonomous: true
requirements: [REQ-bundled-rules-tier1, REQ-bundled-rules-format]
must_haves:
  truths:
    - "vitest matches vitest / vitest run; jest matches jest / npx jest (D-10); both load via the resolver"
    - "Both drop per-file/per-suite PASS lines + timing, keep the Test Files/Tests summary, reducing ≥50%"
    - "Failure fixtures route through on_error and preserve the failure detail + summary"
    - "Real output captured via npx in throwaway node projects (vitest/jest NOT installed); one-shot, no --watch"
    - "The extends-vs-copy decision from plan 05-02 is applied consistently"
  artifacts:
    - path: "bundled-rules/vitest.yaml"
      provides: "vitest rule (drop per-file PASS + timing, keep summary)"
      contains: "id: vitest"
    - path: "bundled-rules/jest.yaml"
      provides: "jest rule (drop per-suite PASS, keep Test Suites/Tests summary)"
      contains: "id: jest"
  key_links:
    - from: "bundled-rules/vitest.yaml"
      to: "success reduction"
      via: "strip_ansi + drop per-file PASS lines"
      pattern: "drop_regex"
    - from: "bundled-rules/jest.yaml"
      to: "on_error error survival"
      via: "keep FAIL / ● / Expected/Received + summary"
      pattern: "on_error"
---

<objective>
Author the final two test-runner rules, `vitest` and `jest`, and their fixtures.
Both are NOT installed → real output MUST be captured via `npx` in throwaway node
projects (D-03, A1/A2), one-shot (no `--watch`). Both share the family shape: drop
per-file/per-suite PASS lines and timing, keep the `Test Files`/`Tests` (vitest) and
`Test Suites:`/`Tests:` (jest) summary; on failure preserve the failure detail.
Apply the extends-vs-copy verdict from plan 05-02.

Purpose: Complete the 10-rule set and the test-runner family (cargo-test, pytest,
vitest, jest) consistently. jest writes most output to stderr — the lacon merge
captures it (filter_bytes takes merged bytes).
Output: vitest.yaml, jest.yaml, 4 fixtures (2 per rule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-bundled-tier-1-rules/05-CONTEXT.md
@.planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md
@.planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md
@.planning/phases/05-bundled-tier-1-rules/05-02-SUMMARY.md
@docs/specs/filter-rule-schema.md
@crates/lacon-core/tests/bundled_rules.rs

<interfaces>
<!-- vitest/jest NOT installed (node 24.15.0 + npx 11.12.1 present). Capture via npx. -->

vitest match (D-10): `any: [{ command: vitest }, { command: vitest, args_prefix: [run] }]`.
jest match (D-10): `any: [{ command: jest }, { command: npx, args_prefix: [jest] }]`.

vitest (RESEARCH vitest section): strip_ansi MANDATORY (heavily colorized). DROP
`^ ✓ ` per-file PASS, Duration/Start at/Transform/Setup/Collect/Environment/Prepare
timing lines. KEEP `^ Test Files `, `^      Tests `. Match on surrounding ASCII
(`(tests)`, `failed`, `passed`) where the ✓/×/❯/→ glyphs are multibyte.
on_error: keep `❯`/`×`/`→` failure detail + Test Files summary.

jest (RESEARCH jest section, output on stderr — merged): DROP `^PASS ` per-suite,
`^Snapshots:`, `^Time:`, `^Ran all test suites`. KEEP `^Test Suites:`, `^Tests:`.
on_error: keep `^FAIL `, `^\s*● `, `^\s*✕ `, `Expected:`/`Received:`, `at .* \(.*:\d+:\d+\)`, summary.

extends-vs-copy: APPLY the 05-02 verdict (read 05-02-SUMMARY). NO max_bytes (D-07).
regex RE2 — no look-around. strip_ansi BEFORE any keep block (Pitfall 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author vitest.yaml and jest.yaml (apply 05-02 verdict)</name>
  <files>bundled-rules/vitest.yaml, bundled-rules/jest.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-02-SUMMARY.md (the extends-vs-copy verdict — apply consistently)
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (the "vitest" + "jest" subsections — line shapes, the glyph caveat, jest-on-stderr note, "D-06 shared base recommendation")
    - bundled-rules/cargo-test.yaml + bundled-rules/pytest.yaml (sibling family members for structural consistency)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (rule field layout + stage keys + Pitfall 1 adjacency)
  </read_first>
  <action>
    Create `bundled-rules/vitest.yaml`: `id: vitest`, `description: vitest / vitest run`, `match.any` with `vitest` and `vitest run` (D-10). Structure per the 05-02 verdict (extends bundled/test-base OR inlined copy). Success `pipeline`: `strip_ansi` (mandatory) then `drop_regex` for the per-file PASS line (match the ASCII `\(\d+ tests?\) \d+ms` shape with a leading-glyph-tolerant anchor rather than the multibyte ✓) and the timing lines (Duration/Start at/Transform/Setup/Collect/Environment/Prepare). KEEP `^\s*Test Files ` and `^\s*Tests `. `on_error.pipeline`: keep the failure detail (lines containing `failed`, the `×`/`❯`/`→` detail via their ASCII context) + the Test Files summary + keep_tail. NO max_bytes.

    Create `bundled-rules/jest.yaml`: `id: jest`, `description: jest / npx jest`, `match.any` with `jest` and `npx jest` (D-10). Success `pipeline`: `strip_ansi` then `drop_regex` for `^PASS `, `^Snapshots:`, `^Time:`, `^Ran all test suites`. KEEP `^Test Suites:`, `^Tests:`. `on_error.pipeline`: keep `^FAIL `, `^\s*● `, `^\s*✕ `, `Expected:`, `Received:`, `at .* \(.*:\d+:\d+\)`, the `^Test Suites:`/`^Tests:` summary, + keep_tail. strip_ansi first (Pitfall 1); no look-around in either rule.
  </action>
  <verify>
    <automated>cargo run -q -p lacon-cli -- validate bundled-rules/vitest.yaml 2>&1 | tail -2 && cargo run -q -p lacon-cli -- validate bundled-rules/jest.yaml 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - Both `lacon validate bundled-rules/vitest.yaml` and `bundled-rules/jest.yaml` succeed.
    - vitest match covers vitest + vitest run; jest match covers jest + npx jest.
    - Both structured per the 05-02 verdict (consistent with cargo-test/pytest).
    - NO max_bytes in either; NO look-around tokens in either.
    - Both have a non-empty pipeline (own/inherited) + on_error.pipeline.
  </acceptance_criteria>
  <done>vitest and jest load and validate with the family reduction shape, consistent with the 05-02 verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Capture vitest + jest fixtures via npx (4 fixtures, one-shot)</name>
  <files>tests/fixtures/vitest/mixed-run/input.txt, tests/fixtures/vitest/mixed-run/expected.txt, tests/fixtures/vitest/mixed-run/meta.yaml, tests/fixtures/vitest/all-pass/input.txt, tests/fixtures/vitest/all-pass/expected.txt, tests/fixtures/vitest/all-pass/meta.yaml, tests/fixtures/jest/suite-failure/input.txt, tests/fixtures/jest/suite-failure/expected.txt, tests/fixtures/jest/suite-failure/meta.yaml, tests/fixtures/jest/all-pass/input.txt, tests/fixtures/jest/all-pass/expected.txt, tests/fixtures/jest/all-pass/meta.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (the "vitest" + "jest" subsections — exact npx capture commands `npx -p vitest vitest run` / `npx -p jest jest --no-watchman`; the glyph + stderr notes)
    - .planning/phases/05-bundled-tier-1-rules/05-VALIDATION.md (Manual-Only Verifications — npx throwaway capture protocol; "No watch-mode flags" sign-off)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (fixture + meta.yaml shape)
    - docs/testing-rules.md:85-110 (regeneration recipe)
  </read_first>
  <action>
    vitest/jest are NOT installed — capture REAL output via npx in throwaway node projects (D-03, A1/A2), one-shot (NO --watch — VALIDATION sign-off). 

    vitest: scratch project with several test files, most passing plus ≥1 failing (so the all-pass success fixture is reductive AND a mixed-run shows failures). `npx -p vitest vitest run 2>&1` → save the all-pass run (make all tests pass, exit 0) to `tests/fixtures/vitest/all-pass/input.txt` (primary success — many ✓ lines to drop, Pitfall 5), and a run with ≥1 failing test (exit 1) to `tests/fixtures/vitest/mixed-run/input.txt`. Strip-ANSI happens in-pipeline, but capture raw (colorized) bytes so strip_ansi is exercised.

    jest: scratch project (babel/ts setup or plain JS) with several suites. `npx -p jest jest --no-watchman 2>&1` → all-pass run (exit 0) to `tests/fixtures/jest/all-pass/input.txt` (primary success), and a suite-failure run (exit 1) to `tests/fixtures/jest/suite-failure/input.txt`. jest writes to stderr — the `2>&1` merge captures it.

    Regenerate each `expected.txt` by replaying the matching rule pipeline against its input.

    meta.yaml — the PRIMARY (reduction-checked) fixture is the all-pass success for each (many PASS lines to drop); the failure fixture preserves context (exempt):
    - vitest/all-pass: `command: vitest run`, `exit_code: 0`, `exempt_from_reduction_check: false`, notes record vitest version + npx capture.
    - vitest/mixed-run: `command: vitest run`, `exit_code: 1`, `exempt_from_reduction_check: true`, `must_keep_lines: ["failed"]` (plus a failing test name).
    - jest/all-pass: `command: jest`, `exit_code: 0`, `exempt_from_reduction_check: false`, notes record jest version + that output is stderr-merged.
    - jest/suite-failure: `command: jest`, `exit_code: 1`, `exempt_from_reduction_check: true`, `must_keep_lines: ["FAIL", "Expected:"]` (or the captured detail).
  </action>
  <verify>
    <automated>cargo test --test bundled_rules 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test bundled_rules` passes for all four vitest/jest scenarios.
    - vitest/all-pass and jest/all-pass (primary success) reduce `<= 0.5`.
    - vitest/mixed-run and jest/suite-failure (exit 1) route through on_error; their must_keep_lines survive.
    - input.txt files are genuine npx-captured output (raw/colorized), not synthetic; meta notes record npx capture + version.
    - No --watch flags used.
    - The full 10-rule set is now present in bundled-rules/ (verify all 10 ids resolve).
  </acceptance_criteria>
  <done>vitest and jest each have a passing primary success fixture (≥50% reduction) and an exempt failure fixture preserving the error — all from real npx output. All 10 Tier 1 rules complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule YAML → loader | Declarative vitest/jest rules validated at load; RE2 regex |
| npx-captured output → runner | Static captured text checked in; no untrusted runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02 | DoS | regex in vitest/jest pipelines | accept | RE2 linear-time — ReDoS impossible |
| T-5-03 | Info disclosure | on_error dropping the failure detail | mitigate | must_keep_lines asserts failed / FAIL / Expected: survive (V5) |
| T-5-SC | Tampering | npx package fetch during capture | accept | vitest/jest fetched into throwaway scratch projects for capture only; deps never enter this repo (RESEARCH Package Legitimacy Audit: N/A) |

No HIGH-severity threats.
</threat_model>

<verification>
- `lacon validate` succeeds for both vitest.yaml and jest.yaml.
- `cargo test --test bundled_rules` passes for all four scenarios + the full 10-rule suite.
- `cargo test` full workspace green.
</verification>

<success_criteria>
vitest and jest load and validate (per the 05-02 verdict); each has a primary success
fixture reducing ≥50% and an exempt failure fixture preserving the error — from genuine
npx output. With this plan the full 10-rule Tier 1 set and all fixtures are complete.
</success_criteria>

<output>
Create `.planning/phases/05-bundled-tier-1-rules/05-09-SUMMARY.md` when done.
</output>
