---
phase: 05-bundled-tier-1-rules
plan: 07
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - bundled-rules/tsc.yaml
  - bundled-rules/eslint.yaml
  - tests/fixtures/tsc/type-errors/input.txt
  - tests/fixtures/tsc/type-errors/expected.txt
  - tests/fixtures/tsc/type-errors/meta.yaml
  - tests/fixtures/tsc/clean/input.txt
  - tests/fixtures/tsc/clean/expected.txt
  - tests/fixtures/tsc/clean/meta.yaml
  - tests/fixtures/eslint/lint-errors/input.txt
  - tests/fixtures/eslint/lint-errors/expected.txt
  - tests/fixtures/eslint/lint-errors/meta.yaml
  - tests/fixtures/eslint/clean/input.txt
  - tests/fixtures/eslint/clean/expected.txt
  - tests/fixtures/eslint/clean/meta.yaml
autonomous: true
requirements: [REQ-bundled-rules-tier1, REQ-bundled-rules-format]
must_haves:
  truths:
    - "tsc matches tsc / tsc --noEmit and eslint matches eslint; both load via the resolver"
    - "tsc reduces via strip_ansi + dedupe + keep_tail (output IS the signal); the failure path is the primary reduction-checked fixture"
    - "eslint preserves error/warning detail with the file-path header; failure path is primary"
    - "Both tools emit nothing on clean success → the clean fixture is exempt_from_reduction_check"
    - "Real output captured via npx in a throwaway node project (tsc/eslint are NOT installed)"
  artifacts:
    - path: "bundled-rules/tsc.yaml"
      provides: "tsc rule (ANSI strip + dedupe + tail; failure-primary)"
      contains: "id: tsc"
    - path: "bundled-rules/eslint.yaml"
      provides: "eslint rule (keep error/warning + path header)"
      contains: "id: eslint"
  key_links:
    - from: "bundled-rules/tsc.yaml"
      to: "success reduction"
      via: "strip_ansi + dedupe + keep_tail"
      pattern: "dedupe"
    - from: "bundled-rules/eslint.yaml"
      to: "error survival"
      via: "keep_around_match on detail line preserving path header"
      pattern: "keep_around_match"
---

<objective>
Author the `tsc` and `eslint` rules and their fixtures. These two are grouped
because both share the same constraints: (a) NOT installed locally → real output
MUST be captured via `npx` in a throwaway node project during execution (D-03, A3/A4);
(b) both emit NOTHING on a clean run → the success fixture is near-empty and exempt,
so the FAILURE path is the primary reduction-checked fixture (RESEARCH Open Question 3).

Purpose: Cover the two TypeScript-toolchain rules where "the output IS the signal"
(tsc) and where reduction comes from keeping only the error/warning detail with its
path header (eslint), both validated against genuine npx-captured output.
Output: tsc.yaml, eslint.yaml, 4 fixtures (2 per rule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-bundled-tier-1-rules/05-CONTEXT.md
@.planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md
@.planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md
@docs/specs/filter-rule-schema.md
@crates/lacon-core/tests/bundled_rules.rs

<interfaces>
<!-- tsc/eslint NOT installed (node 24.15.0 + npx 11.12.1 present). Capture via npx. -->

tsc match (D-10): `any: [{ command: tsc, args_prefix: [] }, { command: tsc, args_prefix: ['--noEmit'] }]`
(or a single `{ command: tsc }`). tsc error format: `file(line,col): error TSxxxx:`.
Success pipeline (RESEARCH Pattern 3 — output IS the signal): `strip_ansi` → `dedupe: { max_kept: 1 }` → `keep_tail: { lines: 100 }`.
tsc emits NOTHING on clean success (exit 0) → clean fixture is exempt.
on_error (exit 1/2): same shape works; keep all `\(\d+,\d+\): error TS\d+:` + `Found N errors`.

eslint match: `{ command: eslint }`. stylish formatter: file-path header line then
indented `line:col error/warning rule` detail, then `✖ N problems`.
on_error: keep file-path header + detail. Use `keep_around_match: { pattern: ' (error|warning) ', before: 1, after: 0 }`
to preserve the path header above each detail line (RESEARCH section 8). strip_ansi MANDATORY first.

NO max_bytes (D-07). regex RE2 — no look-around. strip_ansi BEFORE any keep block (Pitfall 1).
Capture tsc with `--pretty false` for a stable one-line form OR pretty + strip_ansi (record which in meta notes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tsc.yaml and eslint.yaml</name>
  <files>bundled-rules/tsc.yaml, bundled-rules/eslint.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (section "7. tsc" + "8. eslint" — formats, the empty-on-success note, the npx-capture mandate, RESEARCH Pattern 3 for tsc)
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (rule field layout + stage keys; the dedupe/keep_tail/keep_around_match YAML forms)
    - docs/specs/filter-rule-schema.md (dedupe, keep_tail, keep_around_match semantics)
    - crates/lacon-core/tests/fixtures/rules/parent.yaml (on_error nesting)
  </read_first>
  <action>
    Create `bundled-rules/tsc.yaml`: `id: tsc`, `description: tsc / tsc --noEmit`, match for `tsc` and `tsc --noEmit` (D-10). Success `pipeline` (RESEARCH Pattern 3 — the output IS the signal, reduce only via ANSI/dedup/cap): `strip_ansi` → `dedupe: { max_kept: 1 }` → `keep_tail: { lines: 100 }`. `on_error.pipeline`: same three stages (tsc failure output is all errors; dedupe + tail caps it without dropping the TSxxxx lines). NO max_bytes (D-07).

    Create `bundled-rules/eslint.yaml`: `id: eslint`, `description: eslint`, `match: { command: eslint }`. Success `pipeline`: `strip_ansi` → `keep_around_match: { pattern: ' (error|warning) ', before: 1, after: 0 }` (keeps each detail line plus the file-path header above it). `on_error.pipeline`: `strip_ansi` → `keep_around_match: { pattern: ' (error|warning) ', before: 1, after: 0 }` → `keep_tail: { lines: 60 }`. Verify both before AND after present; strip_ansi is first (Pitfall 1); no look-around.
  </action>
  <verify>
    <automated>cargo run -q -p lacon-cli -- validate bundled-rules/tsc.yaml 2>&1 | tail -2 && cargo run -q -p lacon-cli -- validate bundled-rules/eslint.yaml 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - Both `lacon validate bundled-rules/tsc.yaml` and `bundled-rules/eslint.yaml` succeed.
    - tsc uses dedupe + keep_tail (`grep -q dedupe bundled-rules/tsc.yaml`).
    - eslint keep_around_match has both before and after keys.
    - NO max_bytes in either; NO look-around tokens in either.
    - Both have a non-empty pipeline + on_error.pipeline.
  </acceptance_criteria>
  <done>tsc and eslint rules load and validate with the failure-aware reduction shapes.</done>
</task>

<task type="auto" type-note="checkpoint-free; npx capture is a Claude action via Bash">
  <name>Task 2: Capture tsc + eslint fixtures via npx (4 fixtures)</name>
  <files>tests/fixtures/tsc/type-errors/input.txt, tests/fixtures/tsc/type-errors/expected.txt, tests/fixtures/tsc/type-errors/meta.yaml, tests/fixtures/tsc/clean/input.txt, tests/fixtures/tsc/clean/expected.txt, tests/fixtures/tsc/clean/meta.yaml, tests/fixtures/eslint/lint-errors/input.txt, tests/fixtures/eslint/lint-errors/expected.txt, tests/fixtures/eslint/lint-errors/meta.yaml, tests/fixtures/eslint/clean/input.txt, tests/fixtures/eslint/clean/expected.txt, tests/fixtures/eslint/clean/meta.yaml</files>
  <read_first>
    - .planning/phases/05-bundled-tier-1-rules/05-RESEARCH.md (section "7. tsc" + "8. eslint" — the exact npx capture commands `npx -p typescript tsc --noEmit` / `npx -p eslint eslint .`; the empty-on-success behavior; Open Question 3 — failure is primary)
    - .planning/phases/05-bundled-tier-1-rules/05-VALIDATION.md (Manual-Only Verifications row — the npx throwaway-project capture protocol; "No watch-mode flags")
    - .planning/phases/05-bundled-tier-1-rules/05-PATTERNS.md (fixture + meta.yaml shape)
    - docs/testing-rules.md:85-110 (regeneration recipe)
  </read_first>
  <action>
    tsc/eslint are NOT installed — capture REAL output via npx in a throwaway node project (D-03, RESEARCH A3/A4). Create a scratch dir with a tiny `tsconfig.json` and a `.ts` file with ≥3 deliberate type errors; run `npx -p typescript tsc --noEmit 2>&1` (use `--pretty false` for a stable one-line form, OR keep pretty and rely on strip_ansi — record which in meta notes), observe exit 1/2, save to `tests/fixtures/tsc/type-errors/input.txt`. Then make the .ts file clean and run again (exit 0, zero output) → `tests/fixtures/tsc/clean/input.txt` (may be empty). For eslint, create a `.eslintrc`/flat config + a `.js` file with `no-unused-vars` + `no-console` violations; `npx -p eslint eslint . 2>&1`, observe exit 1, save to `tests/fixtures/eslint/lint-errors/input.txt`; a clean file run (exit 0, zero output) → `tests/fixtures/eslint/clean/input.txt`. Do NOT use any --watch flag (VALIDATION sign-off: one-shot only).

    Regenerate each `expected.txt` by replaying the matching rule pipeline against its input.

    meta.yaml — the FAILURE fixture is PRIMARY (reduction-checked); the CLEAN fixture is exempt (empty/near-empty):
    - tsc/type-errors: `command: tsc --noEmit`, `exit_code: <observed 1 or 2>`, `exempt_from_reduction_check: false`, `must_keep_lines: ["error TS"]`, notes record `--pretty` state + that tsc isn't installed (npx-captured).
    - tsc/clean: `command: tsc --noEmit`, `exit_code: 0`, `exempt_from_reduction_check: true`, notes "tsc emits nothing on clean success".
    - eslint/lint-errors: `command: eslint .`, `exit_code: 1`, `exempt_from_reduction_check: false`, `must_keep_lines: ["error", "no-unused-vars"]`.
    - eslint/clean: `command: eslint .`, `exit_code: 0`, `exempt_from_reduction_check: true`, notes "eslint emits nothing on clean run".

    Note for the executor: if a CLEAN fixture's input is empty, the reduction check is skipped (exempt) and byte-exact compares empty-vs-empty — that is acceptable; the clean fixture documents the zero-output success path. If preferred, use an eslint "warnings present, exit 0" scenario instead of an empty clean run (RESEARCH section 8) — either satisfies the success-fixture requirement.
  </action>
  <verify>
    <automated>cargo test --test bundled_rules 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test bundled_rules` passes for all four tsc/eslint scenarios.
    - tsc/type-errors and eslint/lint-errors (the primary failure fixtures) reduce `<= 0.5`; their must_keep_lines survive.
    - The two clean fixtures are exempt and pass byte-exact.
    - input.txt files are genuine npx-captured tool output (not hand-synthesized); meta notes record npx capture + tool version.
    - No --watch flags used.
  </acceptance_criteria>
  <done>tsc and eslint each have a passing failure-primary fixture (≥50% reduction, error survives) and an exempt clean fixture, all from real npx output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule YAML → loader | Declarative tsc/eslint rules validated at load; RE2 regex |
| npx-captured output → runner | Static captured text checked in; no untrusted runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02 | DoS | regex in tsc/eslint pipelines | accept | RE2 linear-time — ReDoS impossible |
| T-5-03 | Info disclosure | success pipeline dropping the error/warning | mitigate | must_keep_lines asserts error TS / no-unused-vars survive (V5) |
| T-5-SC | Tampering | npx package fetch during capture | accept | typescript/eslint are fetched into a throwaway scratch project for capture only; their deps never enter this repo's Cargo.toml or node deps (RESEARCH Package Legitimacy Audit: N/A) |

No HIGH-severity threats.
</threat_model>

<verification>
- `lacon validate` succeeds for both tsc.yaml and eslint.yaml.
- `cargo test --test bundled_rules` passes for all four scenarios.
- `cargo test` full workspace green.
</verification>

<success_criteria>
tsc and eslint load and validate; each has a failure-primary fixture reducing ≥50%
while preserving the error signal, plus an exempt clean fixture — all from genuine
npx-captured output.
</success_criteria>

<output>
Create `.planning/phases/05-bundled-tier-1-rules/05-07-SUMMARY.md` when done.
</output>
