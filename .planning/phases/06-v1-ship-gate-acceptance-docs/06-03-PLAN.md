---
phase: 06-v1-ship-gate-acceptance-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/worked-example.md
  - docs/primitive-reference.md
autonomous: true
requirements:
  - REQ-docs-readme
  - REQ-docs-worked-example
  - REQ-docs-primitive-reference

must_haves:
  truths:
    - "README ships with install + quickstart (no longer a design-status stub) and links the worked example and primitive reference from its Documentation section."
    - "A worked-example doc walks through writing a project-specific filter rule, derived from the existing schema-spec material (not green-fielded)."
    - "A primitive-reference doc gives at least one worked input→output example for every one of the ten native primitives, with examples consistent with the tested golden fixtures."
  artifacts:
    - path: "README.md"
      provides: "install + quickstart + Documentation links to the two new docs (D-10)"
      contains: "Quickstart"
    - path: "docs/worked-example.md"
      provides: "project-specific filter-rule walkthrough (D-10)"
      contains: "our-monorepo-pnpm"
    - path: "docs/primitive-reference.md"
      provides: "one input→output example per primitive, all 10 (D-10)"
      contains: "keep_around_match"
  key_links:
    - from: "README.md"
      to: "docs/worked-example.md"
      via: "Documentation-section link"
      pattern: "worked-example"
    - from: "README.md"
      to: "docs/primitive-reference.md"
      via: "Documentation-section link"
      pattern: "primitive-reference"
    - from: "docs/primitive-reference.md"
      to: "tests/fixtures/primitives/<name>"
      via: "examples derived from golden fixtures (drift-prevention)"
      pattern: "strip_ansi"
---

<objective>
Ship the three user-facing docs (D-10): rewrite the 24-line design-status README into install + quickstart, extract a worked example for writing a project-specific filter rule, and author a primitive reference with one example per native primitive. Source all content from the canonical schema spec and the tested golden fixtures to prevent drift (Pitfall 5). Pure documentation — independent of all code work, runs fully parallel to Plans 01 and 02.

Purpose: SC5 of the Phase 6 ship gate (README + worked example + primitive reference ship and link from the project root).
Output: a rewritten README and two new Markdown docs, cross-linked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-v1-ship-gate-acceptance-docs/06-CONTEXT.md
@.planning/phases/06-v1-ship-gate-acceptance-docs/06-RESEARCH.md
@.planning/phases/06-v1-ship-gate-acceptance-docs/06-PATTERNS.md

<interfaces>
<!-- Concrete source-of-truth references verified at plan time. -->

README.md (current, 24 lines): a design-status stub. KEEP the `## Documentation` link list (lines 9-21) and `## License` (lines 22-24); REPLACE line 5 (`> **Status:** in design. No installable artifact yet.`) with install + quickstart.

Six CLI commands (REQ-cli-surface-cap): init, run, stats, explain, doctor, validate. `lacon init` wires the Claude Code PreToolUse hook + `.lacon/` skeleton + a CLAUDE.md instruction line. `lacon run [--rule <id>] -- <cmd>` is the production wrapper. (Surface is hard-capped at six; do not document a seventh.)

docs/specs/filter-rule-schema.md is the CONTRACT and source of truth:
- Lines 98-152: the ten native primitives with canonical behavior — strip_ansi, drop_regex, keep_regex (whitelist/OR mode), replace_regex {pattern,replacement}, dedupe (max_kept default 1), collapse_repeated {pattern,max_kept,summary} with the `{count}` placeholder, keep_head {lines|bytes}, keep_tail {lines|bytes} (ring buffer), keep_around_match {pattern,before,after} (grep -B/-A semantics), max_bytes N with the `[lacon: truncated, N more bytes dropped]` marker (must be the LAST stage).
- Lines 213-233: the existing "Worked example" — the `our-monorepo-pnpm` rule that `extends: bundled/pkg-install` and adds two drop_regex stages, plus three explanatory bullets about inheritance/prepend/resolution-precedence (ADR-0012).

tests/fixtures/primitives/<name>/{input.txt,expected.txt} exist for ALL TEN primitives (collapse_repeated, dedupe, drop_regex, keep_around_match, keep_head, keep_regex, keep_tail, max_bytes, replace_regex, strip_ansi). These are the byte-exact tested behavior (driven by crates/lacon-core/tests/primitives.rs). Derive each primitive-reference example's input→output FROM these fixtures so the doc cannot drift from tested behavior.

Drift-prevention mandate (Pitfall 5): do NOT green-field doc content; extract from the schema spec and verify examples against the fixtures.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/primitive-reference.md — one example per primitive, fixture-verified (D-10)</name>
  <files>docs/primitive-reference.md</files>
  <read_first>
    - docs/specs/filter-rule-schema.md (lines 98-152: the canonical behavior of all ten primitives)
    - tests/fixtures/primitives/strip_ansi/{input.txt,expected.txt}
    - tests/fixtures/primitives/drop_regex/{input.txt,expected.txt}
    - tests/fixtures/primitives/keep_regex/{input.txt,expected.txt}
    - tests/fixtures/primitives/replace_regex/{input.txt,expected.txt}
    - tests/fixtures/primitives/dedupe/{input.txt,expected.txt}
    - tests/fixtures/primitives/collapse_repeated/{input.txt,expected.txt}
    - tests/fixtures/primitives/keep_head/{input.txt,expected.txt}
    - tests/fixtures/primitives/keep_tail/{input.txt,expected.txt}
    - tests/fixtures/primitives/keep_around_match/{input.txt,expected.txt}
    - tests/fixtures/primitives/max_bytes/{input.txt,expected.txt}
    - crates/lacon-core/tests/primitives.rs (lines 16-44: how fixtures map to primitive runs, to confirm the YAML config each fixture uses)
    - .planning/phases/06-v1-ship-gate-acceptance-docs/06-PATTERNS.md (primitive-reference.md section: drift-prevention via fixtures)
  </read_first>
  <action>
    Create `docs/primitive-reference.md`. One section per native primitive, in this order: strip_ansi, drop_regex, keep_regex, replace_regex, dedupe, collapse_repeated, keep_head, keep_tail, keep_around_match, max_bytes. Each section contains: a one-line behavior summary sourced from filter-rule-schema.md:98-152, the YAML config form (args/shape), and ONE worked input→output example whose input and output are taken from the matching `tests/fixtures/primitives/<name>/{input.txt,expected.txt}` so the example IS the tested behavior. Reproduce these spec semantics exactly (do not paraphrase loosely): keep_regex is whitelist/OR mode (only matching lines kept; multiple keep_regex OR'd), dedupe `max_kept` defaults to 1, collapse_repeated's `summary` uses the `{count}` placeholder for the dropped-line count, keep_around_match uses grep -B/-A (before/after) semantics, max_bytes appends the `[lacon: truncated, N more bytes dropped]` marker and must be the LAST stage. Add a short intro noting these are streaming line-by-line transformers (ADR-0005) and a link back to the schema spec as the contract. Do NOT invent any example output that the primitive would not actually produce.
  </action>
  <verify>
    <automated>test -f docs/primitive-reference.md && for p in strip_ansi drop_regex keep_regex replace_regex dedupe collapse_repeated keep_head keep_tail keep_around_match max_bytes; do grep -q "$p" docs/primitive-reference.md || { echo "MISSING primitive: $p"; exit 1; }; done && grep -q 'lacon: truncated' docs/primitive-reference.md && echo "all 10 primitives present"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/primitive-reference.md` exists and contains a section for each of the ten primitives (grep finds all ten primitive names).
    - Each primitive section has at least one input→output example (REQ-docs-primitive-reference: "at least one example each").
    - The max_bytes section includes the literal truncation marker `[lacon: truncated, N more bytes dropped]` and notes it must be the last stage.
    - Examples are consistent with `tests/fixtures/primitives/<name>/{input.txt,expected.txt}` (no fabricated outputs).
    - keep_regex (whitelist/OR), dedupe (max_kept default 1), collapse_repeated (`{count}`), and keep_around_match (grep -B/-A) semantics match the schema spec.
  </acceptance_criteria>
  <done>A user-facing primitive reference exists with one fixture-verified worked example for every native primitive, consistent with the schema contract.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/worked-example.md — writing a project-specific filter rule (D-10)</name>
  <files>docs/worked-example.md</files>
  <read_first>
    - docs/specs/filter-rule-schema.md (lines 213-233: the existing "Worked example" — our-monorepo-pnpm extends bundled/pkg-install + two drop_regex stages + the three explanatory bullets)
    - docs/specs/filter-rule-schema.md (the extends/inheritance section, to keep prepend + scalar-inheritance semantics accurate per ADR-0012)
    - bundled-rules/ (confirm the pkg-install rule id and shape that the example extends)
    - .planning/phases/06-v1-ship-gate-acceptance-docs/06-PATTERNS.md (worked-example.md section: extract, do NOT green-field; preserve the three explanatory bullets)
  </read_first>
  <action>
    Create `docs/worked-example.md` by extracting and polishing the existing worked-example material at filter-rule-schema.md:213-233 (Pitfall 5: extract, do not green-field). Walk a reader through writing a project-specific rule end-to-end: where the rule file lives (`.lacon/rules/our-monorepo-pnpm.yaml`), the rule YAML (`id`, `description`, `extends: bundled/pkg-install`, the two `drop_regex` stages for the verbose lockfile lines), and WHY it behaves as it does — preserve the three explanatory bullets about inheriting `match`/`rewrite`/`on_error` from the parent, prepending the bundled pipeline before the extra stages, and project rules winning resolution over bundled (first-match-wins, project > user > bundled, ADR-0012/ADR-0007). Add a short closing pointer: validate the rule with `lacon validate .lacon/rules/our-monorepo-pnpm.yaml` and inspect a filtered invocation with `lacon explain <id>`. Keep the inheritance semantics byte-consistent with the schema spec — do not describe remove/reorder/insert operations (not in v1).
  </action>
  <verify>
    <automated>test -f docs/worked-example.md && grep -q 'our-monorepo-pnpm' docs/worked-example.md && grep -q 'extends' docs/worked-example.md && grep -q 'drop_regex' docs/worked-example.md && grep -qi 'validate' docs/worked-example.md && echo "worked example present"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/worked-example.md` exists and walks through writing a project-specific filter rule (REQ-docs-worked-example).
    - It contains the `our-monorepo-pnpm` rule with `extends: bundled/pkg-install` and the two `drop_regex` stages from the schema spec.
    - It preserves the three explanatory bullets (inheritance of match/rewrite/on_error; pipeline prepend; project-rule resolution precedence) consistent with ADR-0012/ADR-0007.
    - It references `lacon validate` for the rule (and ideally `lacon explain` for inspecting output).
    - It describes no remove/reorder/insert inheritance operation (out of v1 scope).
  </acceptance_criteria>
  <done>A worked-example doc shows a reader how to write a project-specific filter rule, derived from the canonical schema material and consistent with the extends/resolution contract.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite README into install + quickstart and link the new docs (D-10)</name>
  <files>README.md</files>
  <read_first>
    - README.md (the full current 24-line stub: line 5 status line to replace; lines 9-21 Documentation list to keep + extend; lines 22-24 License to keep)
    - docs/worked-example.md (the doc created in Task 2, to link)
    - docs/primitive-reference.md (the doc created in Task 1, to link)
    - .planning/REQUIREMENTS.md (REQ-cli-init, REQ-cli-run, and the six-command surface — to describe the quickstart accurately)
    - docs/architecture.md (the install/usage shape: lacon init wires the PreToolUse hook; lacon run is the wrapper)
    - .planning/phases/06-v1-ship-gate-acceptance-docs/06-PATTERNS.md (README REWRITE section: keep Documentation + License; replace the status stub)
  </read_first>
  <action>
    Rewrite `README.md`. REMOVE the design-status line (`> **Status:** in design. No installable artifact yet.`). ADD an Install section (build from source via `cargo build --release`; note the produced `lacon` + `lacon-claude-hook` binaries; macOS + Linux only per v1 scope) and a Quickstart section walking a new user through: run `lacon init` in a project (wires the Claude Code PreToolUse hook + `.lacon/` skeleton + CLAUDE.md line), then from then on matched Bash commands are auto-wrapped as `lacon run --rule <id> -- <cmd>` and filtered; mention the six commands (init, run, stats, explain, doctor, validate) briefly and `lacon doctor` to verify the install. EXTEND the existing `## Documentation` section to add links to `docs/worked-example.md` (Worked example — writing a project-specific filter rule) and `docs/primitive-reference.md` (Primitive reference — one example per primitive). KEEP the existing Documentation links and the License section. Do not promise features outside v1 scope (no Windows, no registry, no purge). Keep claims consistent with the actual six-command surface.
  </action>
  <verify>
    <automated>grep -qi 'Quickstart' README.md && grep -qi 'Install' README.md && grep -q 'worked-example' README.md && grep -q 'primitive-reference' README.md && ! grep -q 'No installable artifact yet' README.md && grep -q 'lacon init' README.md && echo "README updated + links present"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` no longer contains `No installable artifact yet`.
    - It has an Install section and a Quickstart section (REQ-docs-readme: install + quickstart).
    - The Documentation section links to BOTH `docs/worked-example.md` and `docs/primitive-reference.md` (relative links), and the prior Documentation links + License section are preserved.
    - The quickstart references `lacon init` and the production `lacon run` wrapper and does not document any command outside the six-command surface.
  </acceptance_criteria>
  <done>The README is a real install + quickstart and links both new docs from its Documentation section, closing SC5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → user actions | Docs instruct users to run commands and write config; inaccurate docs could mislead but execute no code themselves. |
| doc examples → schema contract | Examples that diverge from `filter-rule-schema.md` or actual primitive behavior create false expectations (drift). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-DOC-01 | Information Disclosure (misleading docs) | primitive-reference / worked-example accuracy | mitigate | Examples are extracted from the schema spec and verified against the tested golden fixtures (Pitfall 5); the verify greps assert every primitive and the canonical truncation marker are present. |
| T-06-DOC-02 | Tampering (scope creep in docs) | README promising out-of-v1 features | accept (low) | The action constrains README claims to the six-command surface and v1 scope; reviewer confirms no Windows/registry/purge promises. No code or runtime surface is touched. |

This plan ships Markdown only — it registers no runtime state, runs no network calls, and adds no dependency. No high-severity threats apply; the only real risk is documentation drift, which is mitigated by sourcing from the contract and fixtures.
</threat_model>

<verification>
- `docs/primitive-reference.md` exists with a section for all ten primitives and the truncation marker (D-10).
- `docs/worked-example.md` exists with the `our-monorepo-pnpm` extends example and the three explanatory bullets (D-10).
- `README.md` has Install + Quickstart and links both new docs; the design-status stub is gone (D-10).
- All three docs are internally consistent with `docs/specs/filter-rule-schema.md` (no drift).
</verification>

<success_criteria>
- README (install + quickstart), worked example (project-specific filter rule), and primitive reference (one example per primitive) all ship and link from the project root (SC5).
- Docs are sourced from the canonical schema spec and tested fixtures, so they describe real, tested behavior.
</success_criteria>

<output>
Create `.planning/phases/06-v1-ship-gate-acceptance-docs/06-03-SUMMARY.md` when done.
</output>
