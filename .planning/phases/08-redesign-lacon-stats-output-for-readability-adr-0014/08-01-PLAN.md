---
phase: 08-redesign-lacon-stats-output-for-readability-adr-0014
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/lacon-core/src/tracking/query.rs
  - crates/lacon-core/tests/tracking_query.rs
autonomous: true
requirements: [ADR-0014]

must_haves:
  truths:
    - "The stats headline can be backed by a single read-only aggregate over all bypassed=0 invocations."
    - "A --since/--project-filtered counterpart returns a zeroed (not error) headline when the filter matches nothing on a populated DB."
    - "A bypassed=1 row is excluded from every headline total."
    - "Decision trace — D-01: no-migration fence (no view/migration/write-path change); D-02: exactly one new SQL aggregate behind the lacon-core query boundary, rusqlite stays lacon-cli dev-only; D-05: overall headline aggregate `overall_totals` + its `filtered_overall_totals` counterpart over bypassed=0 rows."
  artifacts:
    - path: "crates/lacon-core/src/tracking/query.rs"
      provides: "OverallTotals struct + overall_totals(conn) + filtered_overall_totals(conn, since, project) behind the D-01/D-02 SQL boundary"
      contains: "pub fn overall_totals"
    - path: "crates/lacon-core/tests/tracking_query.rs"
      provides: "lacon-core test proving bypassed exclusion + zeroed filtered-empty result"
      contains: "overall_totals"
  key_links:
    - from: "crates/lacon-core/src/tracking/query.rs::overall_totals"
      to: "invocations base table (bypassed = 0)"
      via: "COALESCE(SUM(...),0) scalar aggregate via query_row"
      pattern: "COALESCE\\(SUM"
    - from: "crates/lacon-core/src/tracking/query.rs::filtered_overall_totals"
      to: "--since / --project binds"
      via: "?N placeholders in a Vec<&dyn rusqlite::ToSql>"
      pattern: "binds.push"
---

<objective>
Add exactly ONE new read-only aggregate behind the `lacon-core::tracking::query`
boundary (D-02): a `query::overall_totals(conn)` plus its `--since`/`--project`-
filtered counterpart `query::filtered_overall_totals(conn, since, project)`,
returning a new `OverallTotals` row struct. This is the data source for the stats
headline (ADR 0014 §1 / D-05): total runs, distinct projects (pre-canonicalization),
`raw → kept` bytes, and `bytes_saved`, over `bypassed = 0` rows.

Purpose: The headline (planned in 08-03) needs a scalar aggregate that does NOT
exist today. Per D-01/D-02 ALL SQL lives in `lacon-core`; `lacon-cli` keeps
`rusqlite` dev-only and never inlines a query. Adding the reader first lets the
headline task in 08-03 consume a typed function rather than exploring SQL.

Output: `OverallTotals` struct + two reader functions in `query.rs`; one lacon-core
integration test in `tracking_query.rs`. No migration, no view, no field rename,
no write-path change (D-01 fence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-redesign-lacon-stats-output-for-readability-adr-0014/08-CONTEXT.md
@.planning/phases/08-redesign-lacon-stats-output-for-readability-adr-0014/08-RESEARCH.md
@.planning/phases/08-redesign-lacon-stats-output-for-readability-adr-0014/08-PATTERNS.md
@docs/decisions/0014-stats-read-time-presentation.md

<interfaces>
<!-- The exact template the executor mirrors. filtered_project_savings is the
     filtered-counterpart analog (verbatim below); project_savings is the
     unfiltered analog; ProjectSaving is the row-struct analog. -->

From crates/lacon-core/src/tracking/query.rs:66-74 (ProjectSaving — mirror its derives + doc-above-struct style):
```rust
/// Row of `v_project_savings`: per-project raw-vs-filtered byte totals.
#[derive(Debug, Clone, PartialEq)]
pub struct ProjectSaving {
    pub project_path: Option<String>,
    pub total_runs: i64,
    pub raw_total: i64,
    pub filtered_total: i64,
    pub bytes_saved: i64,
}
```

From crates/lacon-core/src/tracking/query.rs:324-365 (filtered_project_savings — THE filtered template; copy the binds-vec + ?N pattern verbatim, then collapse to scalars):
```rust
pub fn filtered_project_savings(
    conn: &Connection,
    since_cutoff_ms: Option<i64>,
    project: Option<&str>,
) -> Result<Vec<ProjectSaving>, TrackingError> {
    let mut sql = String::from(
        "SELECT project_path,
                COUNT(*) AS total_runs,
                SUM(raw_stdout_bytes + raw_stderr_bytes) AS raw_total,
                SUM(filtered_bytes) AS filtered_total,
                SUM(raw_stdout_bytes + raw_stderr_bytes - filtered_bytes) AS bytes_saved
         FROM invocations
         WHERE bypassed = 0",
    );
    let mut binds: Vec<&dyn rusqlite::ToSql> = Vec::new();
    let mut n = 0;
    if let Some(cut) = since_cutoff_ms.as_ref() {
        n += 1;
        sql.push_str(&format!(" AND ts >= ?{n}"));
        binds.push(cut);
    }
    if let Some(p) = project.as_ref() {
        n += 1;
        sql.push_str(&format!(" AND project_path = ?{n}"));
        binds.push(p);
    }
    sql.push_str(" GROUP BY project_path ORDER BY bytes_saved DESC");
    // ... conn.prepare(&sql)? + query_map into ProjectSaving + collect ...
}
```

From crates/lacon-core/src/tracking/query.rs:155-172 (project_savings — unfiltered, no-arg shape):
```rust
pub fn project_savings(conn: &Connection) -> Result<Vec<ProjectSaving>, TrackingError> {
    let mut stmt = conn.prepare(
        "SELECT project_path, total_runs, raw_total, filtered_total, bytes_saved
         FROM v_project_savings",
    )?;
    // ... query_map + collect ...
}
```

`?` propagation works because `TrackingError` has `#[from] rusqlite::Error` on its
`Sqlite { source }` variant (crates/lacon-core/src/error.rs:142-146) — every
existing reader relies on this; do the same.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OverallTotals struct + overall_totals + filtered_overall_totals readers</name>
  <read_first>
    - crates/lacon-core/src/tracking/query.rs (the file being modified — read the row-struct block at lines 36-92 and the reader bodies at 155-172 + 324-365)
    - 08-PATTERNS.md "Unit 2 — New SQL aggregate reader" (lines 109-203) — the analog mapping with the three deltas spelled out
    - 08-RESEARCH.md "overall_totals reader design" (lines 200-284) — the verbatim target bodies for both functions + the three load-bearing details
    - docs/decisions/0014-stats-read-time-presentation.md §1 (Overall headline) — the data the aggregate backs
    - CONTEXT D-02 (one new aggregate behind the query boundary), D-05 (bypassed=0 floor)
  </read_first>
  <behavior>
    - overall_totals over a DB with matched + unmatched + one bypassed=1 row: total_runs, raw_total, kept_total, bytes_saved ALL exclude the bypassed row.
    - filtered_overall_totals with a --project filter that matches zero rows on a populated DB: returns an all-zero OverallTotals (no NULL panic, no Err).
    - kept_total == SUM(filtered_bytes); bytes_saved == raw_total - kept_total.
  </behavior>
  <action>
    Add a `pub struct OverallTotals` near the other row structs (after `ProjectSaving`, around query.rs:74), mirroring `ProjectSaving`'s `#[derive(Debug, Clone, PartialEq)]` and doc-comment-above-struct style. Fields (all `pub`, all `i64`): `total_runs`, `distinct_projects`, `raw_total`, `kept_total`, `bytes_saved`. Doc-comment that `distinct_projects` is `COUNT(DISTINCT project_path)` PRE-canonicalization (canonicalization is a lacon-cli presentation concern; SQL has no FS access) and that the headline's displayed "after canonicalization" count is computed in stats.rs from the rolled-up map, not from this field — keep it available regardless.

    Add `pub fn overall_totals(conn: &Connection) -> Result<OverallTotals, TrackingError>`: a static SQL string selecting `COUNT(*) AS total_runs`, `COUNT(DISTINCT project_path) AS distinct_projects`, `COALESCE(SUM(raw_stdout_bytes + raw_stderr_bytes), 0) AS raw_total`, `COALESCE(SUM(filtered_bytes), 0) AS kept_total`, `COALESCE(SUM(raw_stdout_bytes + raw_stderr_bytes - filtered_bytes), 0) AS bytes_saved` `FROM invocations WHERE bypassed = 0`. Use `stmt.query_row([], |r| ...)` — NOT `query_map().collect()` — because a scalar aggregate always returns exactly one row (all-zeros on empty); add a one-line code comment saying so since it differs from every existing reader. Read base table `invocations` directly (no `v_overall` view exists; D-01 forbids adding one).

    Add `pub fn filtered_overall_totals(conn: &Connection, since_cutoff_ms: Option<i64>, project: Option<&str>) -> Result<OverallTotals, TrackingError>`: copy `filtered_project_savings`' binds-vec + `?{n}` placeholder pattern VERBATIM (the `Vec<&dyn rusqlite::ToSql>` + the two `if let Some(...)` blocks with `n += 1`), but with the scalar SELECT from `overall_totals` (every SUM wrapped in `COALESCE(..., 0)`), `WHERE bypassed = 0` as the floor, NO `GROUP BY`/`ORDER BY`, and `query_row(binds.as_slice(), |r| ...)`. Do NOT add the `rule_id` predicates the offender views use — the headline spans matched + unmatched runs (D-05). Filter values go ONLY through `?{n}` placeholders — never string-interpolate `--since`/`--project` into SQL (threat T-08-02). Both readers use bare `?` propagation.
  </action>
  <verify>
    <automated>cargo build -p lacon-core && cargo test -p lacon-core query</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p lacon-core` succeeds (proves the readers compile against the existing TrackingError `#[from]`).
    - `grep -c 'COALESCE(SUM' crates/lacon-core/src/tracking/query.rs` returns ≥ 6 (three COALESCE'd SUMs × two readers).
    - `grep -n 'query_row' crates/lacon-core/src/tracking/query.rs` shows query_row used in BOTH new readers (not query_map for the aggregate).
    - No `GROUP BY` appears in the two new functions' SQL (the scalar collapse): inspect the diff — only the per-project readers retain GROUP BY.
    - `grep -c 'pub struct OverallTotals' crates/lacon-core/src/tracking/query.rs` == 1.
  </acceptance_criteria>
  <done>OverallTotals + overall_totals + filtered_overall_totals exist in query.rs, compile, and use COALESCE(SUM,0) + query_row + ?N binds with no GROUP BY and no field/view renames.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lacon-core test — bypassed exclusion + zeroed filtered-empty result</name>
  <read_first>
    - crates/lacon-core/tests/tracking_query.rs (the file being modified — read the seeding helper `seed_realistic_db` at lines 155-296 and an existing reader test like `project_savings_excludes_bypassed_and_groups_by_project` at lines 330-345 for the open_readonly + assert shape)
    - 08-VALIDATION.md row "overall headline (overall_totals over bypassed=0)" (line 59) and "literal-path fallback" boundary framing — this task owns the bypassed-exclusion boundary
    - 08-RESEARCH.md Pitfall 6 (lines 405-408) — the scalar NULL-on-empty failure mode the zeroed-filtered test guards
  </read_first>
  <behavior>
    - Seed a DB with ≥1 matched (rule_id set), ≥1 unmatched (rule_id NULL), and exactly one bypassed=1 row. overall_totals(conn): total_runs equals the count of bypassed=0 rows ONLY; raw_total / kept_total / bytes_saved sum bypassed=0 rows only (the bypassed row's bytes are absent).
    - filtered_overall_totals(conn, None, Some("/path-that-matches-nothing")) on that same populated DB: returns OverallTotals with all fields == 0 (proves COALESCE(SUM,0), no panic, no Err).
  </behavior>
  <action>
    Add two `#[test]` functions to `tracking_query.rs`, reusing the existing `seed_realistic_db()` helper (or `setup_db_path` + the `meta`/insert helpers if `seed_realistic_db`'s fixed row mix does not include a bypassed row — read it first; if it lacks a bypassed=1 row, seed a small purpose-built DB the same way `project_savings_excludes_bypassed_and_groups_by_project` does). Open with `tracking::open_readonly(&db_path)`.

    Test 1 (`overall_totals_excludes_bypassed_rows`): assert `total_runs` equals the number of seeded bypassed=0 rows, and that `raw_total`/`kept_total`/`bytes_saved` match the hand-computed sums over the bypassed=0 rows only. Assert `bytes_saved == raw_total - kept_total`.

    Test 2 (`filtered_overall_totals_empty_filter_returns_zeroed_row`): call `filtered_overall_totals(&conn, None, Some("/nope-no-such-project"))` against the populated DB; assert the returned struct equals `OverallTotals { total_runs: 0, distinct_projects: 0, raw_total: 0, kept_total: 0, bytes_saved: 0 }` (use the derived `PartialEq`). This is the regression guard for the scalar-NULL-on-empty pitfall.

    Import `lacon_core::tracking::query::OverallTotals` (or refer to it via `tracking::query::`). Do NOT modify any existing test or the seeding helper's existing rows beyond adding a bypassed row if genuinely required.
  </action>
  <verify>
    <automated>cargo build -p lacon-core && cargo test -p lacon-core --test tracking_query overall</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lacon-core --test tracking_query overall` runs both new tests and they PASS.
    - The bypassed-exclusion test fails if the `WHERE bypassed = 0` floor is removed from `overall_totals` (boundary is real, not vacuous) — confirm by reasoning that the seeded bypassed row's bytes are non-zero and distinct.
    - The empty-filter test asserts all five fields == 0 via `assert_eq!` on the whole struct (PartialEq), not a panic-free run alone.
    - `cargo test -p lacon-core` full crate suite still green (no regression to existing query tests).
  </acceptance_criteria>
  <done>Two new tracking_query.rs tests pass: bypassed rows are excluded from all totals, and a zero-match filter returns an all-zero OverallTotals (no NULL panic).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → SQL (`--since`/`--project` reaching `filtered_overall_totals`) | User-supplied filter values cross into a SQL statement. |
| stored DB row → reader | `project_path`/byte columns read from `history.db` (written by the local-only write path). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | scalar aggregate over `bypassed=0` | mitigate | `WHERE bypassed = 0` floor matches `v_project_savings`/D-05; bypassed rows (user-suppressed) never enter the headline totals. Asserted by Task 2. |
| T-08-02 | Tampering (SQL injection) | `filtered_overall_totals` `--since`/`--project` binds | mitigate | Filter values bound ONLY via `?{n}` placeholders in a `Vec<&dyn rusqlite::ToSql>` (copied verbatim from `filtered_project_savings`); only static SQL fragments are concatenated. No string interpolation of user values. Severity without mitigation: high; mitigated → low. |
| T-08-03 | Denial of Service | scalar NULL on empty/filtered-empty result | mitigate | `COALESCE(SUM(...), 0)` + `query_row` returns an all-zero row instead of erroring on a NULL `SUM` over zero rows; a filter matching nothing yields a zeroed headline, not exit-1. Asserted by Task 2. |

No threat in this plan rises above `low` after mitigation. The reader is read-only
(`open_readonly`), adds no new filesystem surface (that lives in 08-02/08-03), and
introduces no migration or write-path change.
</threat_model>

<verification>
- `cargo build -p lacon-core && cargo test -p lacon-core query` — both new readers compile and the new tests pass.
- `grep -rn 'GROUP BY' crates/lacon-core/src/tracking/query.rs` confirms the two new aggregate functions contain no GROUP BY (scalar collapse).
- No diff to `crates/lacon-core/src/tracking/migrations/`, no diff to view DDL, no rename of `filtered_bytes`/`avg_keep_ratio` (D-01/D-15 fence).
- `grep -n 'rusqlite' crates/lacon-cli/Cargo.toml` still shows it ONLY under `[dev-dependencies]` (this plan does not touch lacon-cli, but the gate is cheap insurance for the D-02 boundary).
</verification>

<success_criteria>
- `OverallTotals` struct + `overall_totals(conn)` + `filtered_overall_totals(conn, since, project)` exist in `query.rs` behind the D-01/D-02 boundary.
- Both readers use `COALESCE(SUM,0)` + `query_row` + `?N` placeholder binds; no GROUP BY; bare `?` propagation.
- lacon-core tests prove (a) bypassed=1 exclusion from all totals, (b) a zero-match filter returns an all-zero row with no panic.
- Zero changes to the write path, the four views, stored field names, or the migration set.
</success_criteria>

<output>
Create `.planning/phases/08-redesign-lacon-stats-output-for-readability-adr-0014/08-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
