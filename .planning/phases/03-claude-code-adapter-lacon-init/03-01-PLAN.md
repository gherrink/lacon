---
phase: 03-claude-code-adapter-lacon-init
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/lacon-adapter-claudecode/Cargo.toml
  - crates/lacon-adapter-claudecode/src/lib.rs
  - crates/lacon-adapter-claudecode/src/protocol.rs
  - crates/lacon-core/src/rules/loader.rs
  - crates/lacon-core/src/rules/mod.rs
autonomous: true
requirements:
  - REQ-adapter-pretooluse-only
tags: [phase-03, adapter, scaffolding, protocol]

must_haves:
  truths:
    - "D-01: Hook ships as a separate binary `lacon-claude-hook` (not a `lacon hook` subcommand); `cli_surface.rs` continues to lock the 6-command CLI surface."
    - "D-02: Adapter binary depends only on lacon-core + serde + serde_json + anyhow — no rusqlite/starlark/os_pipe — to honour the ≤10ms cold-start budget on the hot path."
    - "D-04: Workspace root `Cargo.toml` exposes `serde_json` in `[workspace.dependencies]`; adapter `Cargo.toml` adds `[[bin]] lacon-claude-hook` at `src/bin/hook.rs` with serde/serde_json/anyhow as workspace deps."
    - "D-05: Adapter follows the `bin/test_emitter/Cargo.toml` precedent for declaring a `[[bin]]` target inside a non-CLI crate."
    - "Workspace builds clean with new serde_json workspace dep."
    - "`crates/lacon-adapter-claudecode` declares `[[bin]] lacon-claude-hook` and depends on serde + serde_json only (no rusqlite/starlark/os_pipe)."
    - "D-03: Typed protocol structs HookInput + BashToolInput round-trip a real Claude Code PreToolUse payload via serde without losing description/timeout/run_in_background, and `build_rewrite_response` emits `hookEventName: PreToolUse` + `permissionDecision: allow` + `updatedInput`."
    - "A reusable rule-resolution helper exists in lacon-core so the adapter does not depend on lacon-cli."
    - "The 6-command CLI surface test continues to pass (no new subcommand)."
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace dep serde_json"
      contains: "serde_json"
    - path: "crates/lacon-adapter-claudecode/Cargo.toml"
      provides: "binary target + serde/serde_json/anyhow + dev deps"
      contains: "lacon-claude-hook"
    - path: "crates/lacon-adapter-claudecode/src/protocol.rs"
      provides: "HookInput, BashToolInput, build_rewrite_response"
      exports: ["HookInput", "BashToolInput", "build_rewrite_response"]
    - path: "crates/lacon-adapter-claudecode/src/lib.rs"
      provides: "module wiring (chain/tui/quote/protocol declarations as pub mods, run_hook skeleton)"
      contains: "pub mod protocol"
    - path: "crates/lacon-core/src/rules/loader.rs"
      provides: "pub fn match_argv_via_load_all(loader, argv) — promoted from lacon-cli try_match_via_load_all"
      contains: "pub fn match_argv_via_load_all"
  key_links:
    - from: "crates/lacon-adapter-claudecode/src/lib.rs"
      to: "lacon_core::rules::loader::match_argv_via_load_all"
      via: "use statement"
      pattern: "lacon_core::rules.*match_argv_via_load_all"
    - from: "crates/lacon-adapter-claudecode/Cargo.toml"
      to: "workspace.dependencies.serde_json"
      via: "serde_json = { workspace = true }"
      pattern: "serde_json\\s*=\\s*\\{\\s*workspace\\s*=\\s*true"
---

<objective>
Land the workspace-level scaffolding Phase 3 needs before any feature work: add `serde_json` as a workspace dependency, configure `crates/lacon-adapter-claudecode` with the `lacon-claude-hook` binary target + minimal dep set (per D-02), introduce typed stdin/stdout protocol structs (per D-03), and promote the rule-matching helper currently inside `lacon-cli::commands::run` (`try_match_via_load_all` + `rule_matches_argv`) into `lacon-core::rules::loader` so the adapter never imports from `lacon-cli`.

Purpose: every downstream Plan 2/3/4 task imports from this Plan's surface. Adding the workspace dep, binary target, and protocol module here keeps Plans 2/3/4 on independent files. Promoting the matcher avoids the adapter taking a `lacon-cli` dep — a cold-start regression (D-02) and a layering violation.

Output: workspace + adapter Cargo.toml changes, `protocol.rs` (typed structs with full echo-back round-trip), `lib.rs` skeleton with `pub mod` declarations and a placeholder `run_hook` returning `HookOutcome::PassThrough` (Plan 4 fills in the body), and a public `match_argv_via_load_all` function in `lacon-core::rules::loader` that `lacon-cli::commands::run::try_match_via_load_all` then delegates to.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-CONTEXT.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-PATTERNS.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-VALIDATION.md
@Cargo.toml
@crates/lacon-adapter-claudecode/Cargo.toml
@crates/lacon-adapter-claudecode/src/lib.rs
@crates/lacon-cli/Cargo.toml
@crates/lacon-cli/src/commands/run.rs
@crates/lacon-core/src/rules/loader.rs
@crates/lacon-core/src/rules/schema.rs
@crates/lacon-core/src/rules/mod.rs
@bin/test_emitter/Cargo.toml
@crates/lacon-cli/tests/cli_surface.rs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From crates/lacon-core/src/rules/schema.rs (RewriteSpec, RuleFile):
```rust
#[derive(Deserialize, Debug, Clone, Default)]
#[serde(deny_unknown_fields)]
pub struct RewriteSpec {
    #[serde(default)] pub add_flags: Vec<String>,
    #[serde(default)] pub remove_flags: Vec<String>,
    #[serde(default)] pub replace_flags: BTreeMap<String, String>,
}
```

From crates/lacon-core/src/rules/loader.rs (current public surface):
```rust
pub struct RuleLoader { /* ... */ }
impl RuleLoader {
    pub fn new(project_dir: Option<PathBuf>) -> Self;
    pub fn resolve(&mut self, rule_id: &str) -> Result<ResolvedRule, ValidationError>;
    pub fn load_all(&mut self) -> Result<Vec<ResolvedRule>, Vec<ValidationError>>;
}
pub struct ResolvedRule {
    pub id: String,
    pub source: RuleSource,
    pub rule: lacon_core::rules::schema::RuleFile, // verified at crates/lacon-core/src/rules/loader.rs:65
    pub success_pipeline: lacon_core::pipeline::Pipeline,
    pub on_error_pipeline: Option<lacon_core::pipeline::Pipeline>,
    pub post_process: Option<lacon_core::starlark_host::StarlarkScript>,
    pub on_error_post_process: Option<lacon_core::starlark_host::StarlarkScript>,
}
#[derive(Clone, Debug)] pub enum RuleSource { Project, User, Bundled }
```

From crates/lacon-cli/src/commands/run.rs (to be promoted to lacon-core):
```rust
fn try_match_via_load_all(loader: &mut RuleLoader, argv: &[String])
    -> Result<Option<ResolvedRule>, Vec<ValidationError>>;

fn rule_matches_argv(r: &ResolvedRule, prog_basename: &str, args: &[String])
    -> Result<bool, ValidationError>;
```

Target adapter dependency set (D-02 — minimal):
```toml
[dependencies]
lacon-core = { path = "../lacon-core" }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
```
NOT included: clap, rusqlite, starlark, os_pipe, regex, etcetera, signal-hook, nix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add serde_json workspace dep + adapter Cargo.toml scaffolding</name>
  <files>Cargo.toml, crates/lacon-adapter-claudecode/Cargo.toml</files>
  <read_first>
    - Cargo.toml (workspace root — current [workspace.dependencies] block at lines 13–33)
    - crates/lacon-adapter-claudecode/Cargo.toml (current 10-line shape)
    - bin/test_emitter/Cargo.toml (canonical analog for [[bin]] outside lacon-cli — lines 9–11)
    - crates/lacon-cli/Cargo.toml (canonical analog for [dependencies] + [dev-dependencies] block shape)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-CONTEXT.md (D-01, D-02, D-04, D-05)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md (Standard Stack, lines 124–187)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-PATTERNS.md (`crates/lacon-adapter-claudecode/Cargo.toml` section)
  </read_first>
  <behavior>
    - cargo build --workspace succeeds with the new serde_json workspace dep and the adapter crate's new [[bin]] target compiling (empty hook.rs is added in Task 3; this task only adds the manifest entry).
    - cargo check -p lacon-adapter-claudecode succeeds.
    - serde_json appears exactly once in [workspace.dependencies] in the root Cargo.toml, pinned to "1.0.149" (or later 1.x).
    - lacon-adapter-claudecode/Cargo.toml [dependencies] lists exactly: lacon-core (path), serde (workspace), serde_json (workspace), anyhow (workspace). No clap/rusqlite/starlark/os_pipe/regex/etcetera/signal-hook/nix (D-02 cold-start).
    - lacon-adapter-claudecode/Cargo.toml [dev-dependencies] lists exactly: assert_cmd, predicates, tempfile (all workspace = true).
    - The [[bin]] table for `lacon-claude-hook` points to `src/bin/hook.rs` (path, not main.rs).
  </behavior>
  <action>
    Edit `Cargo.toml` (workspace root) and `crates/lacon-adapter-claudecode/Cargo.toml` per D-01, D-02, D-04, D-05 and 03-PATTERNS.md "Modified files" section.

    For workspace Cargo.toml: add a single line `serde_json = "1.0.149"` to `[workspace.dependencies]`, grouped next to the existing `serde = { version = "1", features = ["derive"] }` line (line 15 region). Do not modify any other workspace deps. Do not change the `[profile.*]` blocks.

    For `crates/lacon-adapter-claudecode/Cargo.toml`: keep the existing `[package]` block (5 inherited keys). Replace the existing `[dependencies]` block with the four entries listed in <behavior> above (lacon-core path, serde workspace, serde_json workspace, anyhow workspace). Add a `[[bin]]` table with `name = "lacon-claude-hook"` and `path = "src/bin/hook.rs"` (matching `bin/test_emitter/Cargo.toml:9-11` shape). Add a `[dev-dependencies]` block with assert_cmd, predicates, tempfile (all `{ workspace = true }`).

    Per D-02 cold-start budget: do NOT add clap, rusqlite, starlark, os_pipe, regex, etcetera, signal-hook, or nix to the adapter crate. Per CONTEXT "Internal module organization" — `bin/hook.rs` is the binary entry, `lib.rs` is the orchestration library. Do not flatten the binary into `src/main.rs`.

    Per the binary target path "src/bin/hook.rs": this Task does NOT create that file (Task 3 introduces the skeleton). For this Task, `cargo build` will fail with a missing binary path error — that's expected; the next two Tasks land in this same Wave 1 plan, so do all three Tasks atomically and only run `cargo build --workspace` at the end of Task 3.
  </action>
  <verify>
    <automated>grep -n 'serde_json' /home/maurice/Projects/gherrink-lacon/Cargo.toml | grep -v '^#' | grep -c 'workspace.dependencies\|serde_json = "1' &gt;= 1 || true; grep -c 'serde_json' /home/maurice/Projects/gherrink-lacon/crates/lacon-adapter-claudecode/Cargo.toml; grep -c 'lacon-claude-hook' /home/maurice/Projects/gherrink-lacon/crates/lacon-adapter-claudecode/Cargo.toml; ! grep -E 'clap|rusqlite|starlark|os_pipe|regex|etcetera' /home/maurice/Projects/gherrink-lacon/crates/lacon-adapter-claudecode/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - Workspace root Cargo.toml `[workspace.dependencies]` contains a line beginning `serde_json = "1.` (version 1.0.149 or later 1.x).
    - `grep -c 'serde_json' crates/lacon-adapter-claudecode/Cargo.toml` returns 1 (exactly one workspace=true reference).
    - `grep -c 'name = "lacon-claude-hook"' crates/lacon-adapter-claudecode/Cargo.toml` returns 1.
    - `grep -c 'path = "src/bin/hook.rs"' crates/lacon-adapter-claudecode/Cargo.toml` returns 1.
    - `grep -E '^(clap|rusqlite|starlark|os_pipe|regex|etcetera|signal-hook|nix)\s*=' crates/lacon-adapter-claudecode/Cargo.toml` returns no matches (exit code 1).
    - `[dev-dependencies]` block contains assert_cmd, predicates, tempfile each on its own line with `{ workspace = true }`.
  </acceptance_criteria>
  <done>
    Cargo manifests express the new dep graph and binary target exactly per D-01/D-02/D-04. `cargo build` will be green after Task 3 lands the empty bin/hook.rs file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote rule-matching helper into lacon-core::rules::loader</name>
  <files>
    crates/lacon-core/src/rules/loader.rs,
    crates/lacon-core/src/rules/mod.rs,
    crates/lacon-cli/src/commands/run.rs
  </files>
  <read_first>
    - crates/lacon-cli/src/commands/run.rs (lines 1–153 — the current `try_match_via_load_all` + `rule_matches_argv` pair to promote)
    - crates/lacon-core/src/rules/loader.rs (current public surface — `RuleLoader::new` at line 110, `resolve` at line 127, `load_all` at line 156)
    - crates/lacon-core/src/rules/mod.rs (current 12-line re-export block)
    - crates/lacon-core/src/rules/schema.rs (MatchSpec definition — `any` / `all` / `command` / `args_prefix` / `args_contain` / `command_regex` fields)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md (research recommendation that the matcher promotion is a small additive change — Primary Recommendation section)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-PATTERNS.md (`crates/lacon-adapter-claudecode/src/lib.rs` Analog section, lines describing the `try_match_via_load_all` pattern)
  </read_first>
  <behavior>
    - Existing test `crates/lacon-cli/tests/cli_run.rs::run_with_rule_filters_output` still passes (the matcher behavior under `lacon run` is unchanged — only the function's home moves).
    - All cli_run.rs tests still pass with no behavioral drift.
    - A new doctest on the promoted function in loader.rs documents its first-match-wins contract.
    - Test: a single new unit test in `crates/lacon-core/src/rules/loader.rs` covers the trivial case (`load_all` returns one matching rule → `match_argv_via_load_all` returns Some) and the empty-argv guard (returns Ok(None) or panics with a clear message — choose Ok(None) to match the new function's caller contract).
  </behavior>
  <action>
    Move `try_match_via_load_all` and `rule_matches_argv` from `crates/lacon-cli/src/commands/run.rs` (lines 56–153) into `crates/lacon-core/src/rules/loader.rs` as `pub fn match_argv_via_load_all(loader: &mut RuleLoader, argv: &[String]) -> Result<Option<ResolvedRule>, Vec<ValidationError>>` plus a private `fn rule_matches_argv(r: &ResolvedRule, prog_basename: &str, args: &[String]) -> Result<bool, ValidationError>` helper.

    Per the existing `commands/run.rs:61` basename pattern, preserve `let prog_basename = argv[0].rsplit('/').next().unwrap_or(&argv[0]).to_owned();`. Preserve the spec_matches inner closure exactly — `any`/`all`/`command`/`args_prefix`/`args_contain`/`command_regex` predicates with the WR-02 fix for command_regex compile-error propagation.

    Update `crates/lacon-core/src/rules/mod.rs` to add `pub use loader::match_argv_via_load_all;` next to the existing `RuleLoader, ResolvedRule, RuleSource` re-export.

    In `crates/lacon-cli/src/commands/run.rs`: replace the local `try_match_via_load_all` (lines 56–69) and `rule_matches_argv` (lines 78–153) with a thin delegate — `commands::run::execute` calls `lacon_core::rules::match_argv_via_load_all(&mut loader, &argv)` directly (drop the local wrapper entirely OR keep a 1-line wrapper for symmetry; planner discretion — recommendation is to drop the local wrapper to avoid two names for one thing).

    Empty argv: when `argv` is empty, return `Ok(None)` rather than indexing into argv[0]. (The current code at run.rs:20-22 already gates `argv.is_empty()` at the CLI boundary, but the promoted function must be safe to call independently from the adapter.)

    Add a rustdoc comment on `match_argv_via_load_all` citing first-match-wins (ADR-0007) and the project > user > bundled order. Include a `/// # Examples` doctest showing a one-rule loader returning `Some(resolved)` (use an existing test fixture pattern if available; otherwise skip the doctest — unit tests in this Task cover the contract).

    Add one inline unit test in loader.rs verifying empty-argv returns Ok(None). The matcher's per-spec predicates are already exercised by cli_run.rs integration tests — no need to duplicate.
  </action>
  <verify>
    <automated>cd /home/maurice/Projects/gherrink-lacon && cargo test -p lacon-core --lib rules::loader::tests::match_argv_via_load_all 2>&amp;1 | tail -20; cargo test -p lacon-cli --test cli_run 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn match_argv_via_load_all' crates/lacon-core/src/rules/loader.rs` returns 1.
    - `grep -c 'pub use loader::match_argv_via_load_all' crates/lacon-core/src/rules/mod.rs` returns 1.
    - `grep -E '^fn (try_match_via_load_all|rule_matches_argv)' crates/lacon-cli/src/commands/run.rs` returns no matches (function bodies removed; only the helper may remain as a thin delegate).
    - `cargo test -p lacon-cli --test cli_run` passes (all existing tests still green).
    - `cargo test -p lacon-core --lib rules::loader::tests::match_argv_empty_argv_returns_none` passes (new unit test).
    - `cargo check --workspace` is clean (no warnings introduced beyond the existing baseline).
  </acceptance_criteria>
  <done>
    The first-match-wins rule resolver is reachable from `lacon-core::rules::match_argv_via_load_all`. The adapter (Plan 4) imports it via that path. `lacon-cli` no longer owns this logic privately. Existing `lacon run` behavior is preserved byte-for-byte.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Protocol structs + lib.rs skeleton + bin/hook.rs stub</name>
  <files>
    crates/lacon-adapter-claudecode/src/protocol.rs,
    crates/lacon-adapter-claudecode/src/lib.rs,
    crates/lacon-adapter-claudecode/src/bin/hook.rs
  </files>
  <read_first>
    - crates/lacon-adapter-claudecode/src/lib.rs (current 12-line stub — `ClaudeCodeAdapterStub` to be replaced)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-CONTEXT.md (D-03 stdin/stdout schema — fields verbatim)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md (Pattern 1: protocol.rs typed structs; Pattern 2: bin/hook.rs entry point; lines 290–372)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-PATTERNS.md (`crates/lacon-adapter-claudecode/src/protocol.rs` section + `bin/hook.rs` section)
    - crates/lacon-core/src/rules/schema.rs (lines 118–133 — RewriteSpec serde derive shape as the analog struct pattern)
    - crates/lacon-cli/src/main.rs (canonical workspace-binary main; anyhow::Result<()> return)
  </read_first>
  <behavior>
    - `cargo build --workspace` is clean — both `lacon` and `lacon-claude-hook` binaries compile.
    - `lacon-claude-hook` runs end-to-end with a minimal stdin payload and exits 0 with empty stdout (the lib's `run_hook` returns `HookOutcome::PassThrough` for now — Plan 4 fills in real orchestration).
    - Round-trip test: a `HookInput` deserialized from a fixture JSON payload (with description/timeout/run_in_background set) re-serializes to JSON that has every field of `tool_input` present and equal to the input (proves the echo-back contract per D-03).
    - Round-trip test: a payload WITHOUT description/timeout/run_in_background re-serializes WITHOUT those keys (proves `skip_serializing_if = "Option::is_none"` works correctly).
  </behavior>
  <action>
    Create `crates/lacon-adapter-claudecode/src/protocol.rs` containing:
    - `pub struct HookInput` with fields `session_id: String, transcript_path: String, cwd: String, permission_mode: String, hook_event_name: String, tool_name: String, tool_input: BashToolInput, tool_use_id: String`, all `#[derive(Deserialize, Debug, Clone)]`. Do NOT use `#[serde(deny_unknown_fields)]` on HookInput (Claude Code may add fields).
    - `pub struct BashToolInput { pub command: String, pub description: Option<String>, pub timeout: Option<u64>, pub run_in_background: Option<bool> }` with `#[derive(Deserialize, Serialize, Debug, Clone)]` and `#[serde(skip_serializing_if = "Option::is_none")]` on each `Option` field. This struct must round-trip via serde lossless when all fields present AND drop the Option fields from output when absent.
    - `pub fn build_rewrite_response(updated_input: &BashToolInput) -> serde_json::Value` using `serde_json::json!({ "hookSpecificOutput": { "hookEventName": "PreToolUse", "permissionDecision": "allow", "updatedInput": updated_input } })` — locks the D-03 output shape (hookEventName + permissionDecision: "allow" + updatedInput).
    - One inline `#[cfg(test)] mod tests` block with: (a) `deserialize_full_payload_round_trips` — parse a fixture JSON with all fields, re-serialize tool_input, assert no field lost; (b) `deserialize_minimal_payload_skips_optionals` — parse a fixture JSON without description/timeout/run_in_background, re-serialize tool_input, assert those keys are ABSENT in output; (c) `build_rewrite_response_has_required_fields` — assert hookEventName == "PreToolUse" AND permissionDecision == "allow" AND updatedInput.command is present.

    Replace `crates/lacon-adapter-claudecode/src/lib.rs` entirely (drop ClaudeCodeAdapterStub). New content:
    - `//!` module docstring naming this as the Claude Code adapter library.
    - `pub mod protocol;` declaration.
    - Placeholder `pub mod chain; pub mod tui; pub mod quote;` declarations gated behind `#[cfg(any(test, feature = "_internal"))]` OR (preferred) leave them commented out with a TODO referencing Plans 2/3 — DO NOT declare modules whose files don't yet exist (compile error).
    - `pub enum HookOutcome { PassThrough, Rewrite(serde_json::Value) }` — what `run_hook` returns.
    - `pub fn run_hook(_input: protocol::HookInput) -> anyhow::Result<HookOutcome>` returning `Ok(HookOutcome::PassThrough)` unconditionally (Plan 4 fills in the body — bypass-detect → split → tui → resolve → wrap → reassemble → emit).
    - DO NOT add `#![allow(dead_code)]` — every symbol declared in this skeleton is reachable from `bin/hook.rs` or the test module.

    Create `crates/lacon-adapter-claudecode/src/bin/hook.rs`:
    - `//!` docstring naming this the `lacon-claude-hook` binary entry, citing D-01 and the cold-start budget.
    - `fn main() -> anyhow::Result<()>`. Read stdin via `serde_json::from_reader(std::io::stdin().lock())`. Dispatch on `run_hook(input)?`. For `HookOutcome::PassThrough` return Ok(()) (exit 0, no stdout per D-03). For `HookOutcome::Rewrite(value)` lock stdout, write the value via `serde_json::to_writer(&mut handle, &value)`, then write a trailing `b"\n"` (tooling-friendliness — D-13 mentions trailing newline as the project's settings.json convention; same here for symmetry).
    - Mirror `crates/lacon-cli/src/main.rs:1-20` shape for imports and the `anyhow::Result<()>` return (Phase 1 D-03 — anyhow at the binary boundary).
  </action>
  <verify>
    <automated>cd /home/maurice/Projects/gherrink-lacon && cargo build --workspace 2>&amp;1 | tail -20; cargo test -p lacon-adapter-claudecode --lib protocol::tests 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0 — both `target/debug/lacon` and `target/debug/lacon-claude-hook` exist.
    - `target/debug/lacon-claude-hook` exists after build: `test -x target/debug/lacon-claude-hook && echo OK`.
    - Running `echo '{"session_id":"s","transcript_path":"/t","cwd":"/c","permission_mode":"default","hook_event_name":"PreToolUse","tool_name":"Bash","tool_input":{"command":"echo hi"},"tool_use_id":"u"}' | target/debug/lacon-claude-hook` exits 0 and writes nothing to stdout.
    - `cargo test -p lacon-adapter-claudecode --lib protocol::tests::deserialize_full_payload_round_trips` passes.
    - `cargo test -p lacon-adapter-claudecode --lib protocol::tests::deserialize_minimal_payload_skips_optionals` passes.
    - `cargo test -p lacon-adapter-claudecode --lib protocol::tests::build_rewrite_response_has_required_fields` passes.
    - `grep -c 'hookEventName' crates/lacon-adapter-claudecode/src/protocol.rs` returns at least 1 (D-03 shape is locked in code, not derived elsewhere).
    - `grep -c 'permissionDecision' crates/lacon-adapter-claudecode/src/protocol.rs` returns at least 1, and the value is "allow" (D-03).
    - `grep -c 'ClaudeCodeAdapterStub' crates/lacon-adapter-claudecode/src/lib.rs` returns 0 (stub removed).
  </acceptance_criteria>
  <done>
    `lacon-claude-hook` binary builds and runs. The stdin/stdout shape is locked in typed Rust (D-03). Plans 2/3/4 plug into this Plan's public surface without re-litigating the protocol.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code → `lacon-claude-hook` stdin | Claude Code writes a JSON document the hook MUST parse safely. Untrusted input. |
| Hook stdout → Claude Code parser | Hook writes JSON Claude Code reads. The shape is locked by D-03; emitting the wrong shape silently drops the rewrite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T (Tampering) | `protocol::HookInput` deserialize | mitigate | Typed `#[derive(Deserialize)]` struct without `deny_unknown_fields`; unknown fields tolerated (Claude Code may add fields), required fields strictly typed (missing field → serde error → hook exits non-zero → Claude Code surfaces the error). |
| T-03-01-02 | I (Information disclosure) | `BashToolInput` echo-back | mitigate | `#[serde(skip_serializing_if = "Option::is_none")]` on every optional → no `null` injected into `updatedInput`. Round-trip tests in protocol::tests lock this behavior. |
| T-03-01-03 | T (Tampering) | adapter dep graph | mitigate | D-02 strict dep set — adapter depends ONLY on lacon-core, serde, serde_json, anyhow. No rusqlite/starlark/os_pipe pulls in transitive supply-chain risk on the hook hot path. Cargo.toml grep gate in Task 1 acceptance criteria enforces this. |
| T-03-01-04 | E (Elevation of privilege) | `run_hook` skeleton | accept | Task 3 returns PassThrough unconditionally — no decision logic to attack. Real privilege boundary lands in Plan 4. |
</threat_model>

<verification>
- `cargo build --workspace` is clean (both binaries compile).
- `cargo test -p lacon-core --lib rules::loader::tests::match_argv_empty_argv_returns_none` passes.
- `cargo test -p lacon-cli --test cli_run` is fully green (no regression from the matcher promotion).
- `cargo test -p lacon-adapter-claudecode --lib protocol::tests` passes (all 3 round-trip tests).
- `cargo test -p lacon-cli --test cli_surface` continues to pass (6-command CLI surface unchanged — REQ-cli-surface-cap).
- `grep -E '^(clap|rusqlite|starlark|os_pipe|regex|etcetera|signal-hook|nix)\s*=' crates/lacon-adapter-claudecode/Cargo.toml` returns no matches (D-02 dep budget enforced).
</verification>

<success_criteria>
- Workspace `serde_json` dep present and inherited by lacon-adapter-claudecode (and prepared for lacon-cli's Plan 5 consumption).
- `lacon-claude-hook` binary target builds; minimal stdin → exit 0 / empty stdout works end-to-end.
- `lacon_core::rules::match_argv_via_load_all` is callable from any crate that depends on lacon-core.
- `lacon-cli::commands::run` continues to behave identically (cli_run.rs tests green) while delegating to the promoted helper.
- Adapter dep graph respects D-02 (no rusqlite/starlark/os_pipe).
- 6-command CLI surface intact (no `lacon hook` subcommand introduced).
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-code-adapter-lacon-init/03-01-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Note: per phase planning guidance, Plans 2 and 3 begin Wave 2 immediately after this Plan's commit.
</output>
