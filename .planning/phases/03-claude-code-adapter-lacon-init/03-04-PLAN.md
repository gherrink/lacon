---
phase: 03-claude-code-adapter-lacon-init
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - crates/lacon-adapter-claudecode/src/lib.rs
  - crates/lacon-adapter-claudecode/src/bin/hook.rs
  - crates/lacon-adapter-claudecode/tests/hook_e2e.rs
  - benches/cold_start.rs
  - benches/Cargo.toml
autonomous: true
requirements:
  - REQ-adapter-pretooluse-only
  - REQ-adapter-bypass-detection
  - REQ-adapter-chained-commands
  - REQ-adapter-tui-bypass
  - REQ-adapter-pipes-passthrough
tags: [phase-03, adapter, orchestration, e2e, cold-start]

must_haves:
  truths:
    - "lacon-claude-hook reads a PreToolUse JSON payload from stdin and emits either empty stdout (exit 0) for pass-through OR a hookSpecificOutput JSON envelope for rewrite."
    - "If `input.tool_name != \"Bash\"`, the hook returns empty stdout + exit 0 (non-Bash pass-through; RESEARCH Q4 RESOLVED 2026-05-16)."
    - "D-23: Bypass via `!!` prefix (LSTRIP whitespace, then `starts_with(\"!!\")` on `tool_input.command`) produces empty stdout + exit 0."
    - "D-24: Bypass via `LACON_DISABLE=1` env var (exact string `\"1\"`; other values like empty/`\"0\"`/`\"true\"` do NOT bypass) produces empty stdout + exit 0 — mirrors `crates/lacon-core/src/runtime/mod.rs:157` precedent via `as_deref() == Ok(\"1\")`."
    - "D-25: Bypass is whole-command granularity — when `!!` is detected OR `LACON_DISABLE=1` is set, the entire input bypasses with no chain splitting, no rule resolution, no rewrites (cheapest hot path)."
    - "Any segment matching is_tui (after chain split) triggers whole-chain bypass (entire input → pass-through)."
    - "Matched segments are wrapped as `LACON_ASSISTANT=claude-code LACON_SESSION_ID=<id> LACON_TOOL_USE_ID=<id> lacon run --rule <id> -- <quoted argv>` per D-26 (RESEARCH Q3 RESOLVED 2026-05-16)."
    - "D-27: Phase 2 D-17 env-var contract is satisfied here — every wrapped command inlines `LACON_ASSISTANT=claude-code LACON_SESSION_ID=<id>` (plus `LACON_TOOL_USE_ID=<id>`) so the Phase 2 tracker rows populate without further work."
    - "Unmatched segments are preserved byte-exact (using segment.text from the splitter)."
    - "Chain is reassembled byte-exact: joining wrapped/unwrapped segments with their original trailing_op_span reproduces input semantics."
    - "description/timeout/run_in_background fields are echoed back in updatedInput when present (D-03)."
    - "hookSpecificOutput.hookEventName == 'PreToolUse' AND permissionDecision == 'allow' on every rewrite-path emit."
    - "Cold-start: pass-through path ≤2ms median; rewrite path ≤5ms median (measured via benches/cold_start.rs in release build — soft target, documented in PHASE-BENCH.md if breached)."
  artifacts:
    - path: "crates/lacon-adapter-claudecode/src/lib.rs"
      provides: "run_hook orchestration: detect_bypass → split_chain → tui-check → load_all+match → apply_rewrite → quote_for_shell → wrap → reassemble → emit"
      contains: "pub fn run_hook"
      min_lines: 150
    - path: "crates/lacon-adapter-claudecode/src/bin/hook.rs"
      provides: "thin binary entry: stdin read → run_hook → stdout write OR empty"
      contains: "serde_json::from_reader"
    - path: "crates/lacon-adapter-claudecode/tests/hook_e2e.rs"
      provides: "assert_cmd fixtures: pass-through, bypass-!!, bypass-env, match-single, match-in-chain, TUI-in-chain, description-echo-back"
      contains: "run_hook_with_input"
      min_lines: 250
    - path: "benches/cold_start.rs"
      provides: "hook-passthrough + hook-rewrite scenarios + median table"
      contains: "measure_hook"
  key_links:
    - from: "crates/lacon-adapter-claudecode/src/lib.rs"
      to: "lacon_adapter_claudecode::chain::split_chain"
      via: "use crate::chain"
      pattern: "crate::chain::split_chain|chain::split_chain"
    - from: "crates/lacon-adapter-claudecode/src/lib.rs"
      to: "lacon_adapter_claudecode::tui::is_tui"
      via: "use crate::tui::is_tui"
      pattern: "tui::is_tui"
    - from: "crates/lacon-adapter-claudecode/src/lib.rs"
      to: "lacon_adapter_claudecode::quote::quote_for_shell"
      via: "use crate::quote::quote_for_shell"
      pattern: "quote::quote_for_shell"
    - from: "crates/lacon-adapter-claudecode/src/lib.rs"
      to: "lacon_core::rules::apply_rewrite"
      via: "use lacon_core::rules::apply_rewrite"
      pattern: "lacon_core::rules::apply_rewrite"
    - from: "crates/lacon-adapter-claudecode/src/lib.rs"
      to: "lacon_core::rules::match_argv_via_load_all"
      via: "use lacon_core::rules::match_argv_via_load_all"
      pattern: "match_argv_via_load_all"
    - from: "crates/lacon-adapter-claudecode/src/lib.rs (rewritten command)"
      to: "crates/lacon-cli/src/commands/run.rs:270-272 (tracker env-var consumption)"
      via: "LACON_ASSISTANT=claude-code LACON_SESSION_ID=<id> prefix in wrapped command"
      pattern: "LACON_ASSISTANT=claude-code"
---

<objective>
Replace the `run_hook` skeleton from Plan 1 with full orchestration: bypass-detect (D-23/24/25) → chain-split (Plan 2) → per-segment TUI check (Plan 3 + D-15) → per-segment rule resolve via `match_argv_via_load_all` (Plan 1) → per-segment `apply_rewrite` (Plan 3) → per-segment `quote_for_shell` (Plan 3) → wrap matched segments as `lacon run --rule <id> -- <quoted argv>` with the D-26 env-var prefix → reassemble chain byte-exact using `trailing_op_span` → emit `hookSpecificOutput` JSON via `build_rewrite_response` (Plan 1) OR exit 0 with empty stdout. Land the bin/hook.rs body. Add end-to-end tests in `tests/hook_e2e.rs` and two new cold-start scenarios in `benches/cold_start.rs`.

Purpose: this is the glue plan — it does no novel algorithm, only composition. Every piece exists in Plans 1/2/3. Plan 4 closes REQ-adapter-pretooluse-only, REQ-adapter-bypass-detection, REQ-adapter-chained-commands (the orchestration half), REQ-adapter-tui-bypass (the whole-chain enforcement), REQ-adapter-pipes-passthrough (proven by S10 + a hook_e2e fixture). It also closes T-hook-output-shape, T-bypass-failsafe, T-tui-whole-chain, T-cold-start-regression via the e2e fixtures and bench.

Output: the orchestration `run_hook` body in lib.rs, the bin/hook.rs that writes the response, ≥7 e2e tests covering the rewrite + bypass + chain + TUI matrix, extended cold-start probe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-CONTEXT.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-PATTERNS.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-VALIDATION.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-01-PLAN.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-02-PLAN.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-03-PLAN.md
@.planning/intel/constraints.md
@docs/specs/chained-commands.md
@docs/decisions/0013-filter-via-pretooluse-wrapper.md
@docs/architecture.md
@crates/lacon-adapter-claudecode/src/lib.rs
@crates/lacon-adapter-claudecode/src/bin/hook.rs
@crates/lacon-adapter-claudecode/src/protocol.rs
@crates/lacon-adapter-claudecode/src/chain.rs
@crates/lacon-adapter-claudecode/src/tui.rs
@crates/lacon-adapter-claudecode/src/quote.rs
@crates/lacon-core/src/rules/rewrite.rs
@crates/lacon-core/src/rules/loader.rs
@crates/lacon-cli/tests/cli_run.rs
@crates/lacon-cli/src/commands/run.rs
@benches/cold_start.rs

<interfaces>
<!-- Composed from Plans 1, 2, 3. Plan 4 wires only. -->

From Plan 1 (lacon-adapter-claudecode):
```rust
pub mod protocol;
pub enum HookOutcome { PassThrough, Rewrite(serde_json::Value) }
pub fn run_hook(_input: protocol::HookInput) -> anyhow::Result<HookOutcome>; // skeleton
```

From Plan 1 (lacon-core::rules):
```rust
pub fn match_argv_via_load_all(loader: &mut RuleLoader, argv: &[String])
    -> Result<Option<ResolvedRule>, Vec<ValidationError>>;
```

From Plan 2 (chain.rs):
```rust
pub fn split_chain(input: &str) -> Vec<Segment>;
pub struct Segment { pub text: String, pub trailing_op: Option<ChainOp>, pub trailing_op_span: Option<String> }
pub enum ChainOp { AndAnd, OrOr, Semi }
```

From Plan 3 (tui.rs, quote.rs, lacon-core::rules):
```rust
pub fn is_tui(command: &str, args: &[String]) -> bool;
pub fn quote_for_shell(arg: &str) -> std::borrow::Cow<'_, str>;
pub fn apply_rewrite(argv: &[String], rewrite: &RewriteSpec) -> Vec<String>;
```

From Phase 2 (tracker env-var consumption — adapter MUST satisfy):
```rust
// crates/lacon-cli/src/commands/run.rs:270-272
let assistant = std::env::var("LACON_ASSISTANT").unwrap_or_else(|_| "claude-code".to_owned());
let session_id = std::env::var("LACON_SESSION_ID").ok();
```
The adapter MUST inline-prepend `LACON_ASSISTANT=claude-code LACON_SESSION_ID=<session_id>` to every wrapped command per D-26.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire run_hook orchestration in lib.rs + bin/hook.rs response emit</name>
  <files>
    crates/lacon-adapter-claudecode/src/lib.rs,
    crates/lacon-adapter-claudecode/src/bin/hook.rs
  </files>
  <read_first>
    - crates/lacon-adapter-claudecode/src/lib.rs (Plan 1 skeleton — `run_hook` returns PassThrough; Plans 2/3 added `pub mod chain; pub mod tui; pub mod quote;`)
    - crates/lacon-adapter-claudecode/src/bin/hook.rs (Plan 1 minimal entry — already reads stdin and dispatches; needs rewrite-path stdout write)
    - crates/lacon-adapter-claudecode/src/protocol.rs (HookInput, BashToolInput, build_rewrite_response from Plan 1)
    - crates/lacon-adapter-claudecode/src/chain.rs (split_chain + Segment from Plan 2)
    - crates/lacon-adapter-claudecode/src/tui.rs (is_tui from Plan 3)
    - crates/lacon-adapter-claudecode/src/quote.rs (quote_for_shell from Plan 3)
    - crates/lacon-core/src/rules/rewrite.rs (apply_rewrite from Plan 3)
    - crates/lacon-core/src/rules/loader.rs (match_argv_via_load_all from Plan 1 Task 2)
    - crates/lacon-cli/src/commands/run.rs (orchestration analog — lines 19–54 + 56–69 — for the "load_all once, dispatch per segment" idiom)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-CONTEXT.md (D-08, D-21, D-25, D-26)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md (System Architecture Diagram lines 191–246, bypass detection patterns lines 906–936)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-PATTERNS.md (lib.rs orchestration analog section + LACON_DISABLE pattern + RuleLoader::new pattern)
    - crates/lacon-core/src/runtime/mod.rs (lines 173–177 — the LACON_DISABLE precedent, `as_deref() == Ok("1")`)
  </read_first>
  <behavior>
    - Compiles clean: `cargo build --workspace`.
    - Running `lacon-claude-hook` with a stdin payload where `tool_input.command == "echo hi"` and no rule file present in cwd → exits 0 with empty stdout.
    - Running with `tool_input.command == "!! pnpm test"` → exits 0 with empty stdout (bypass via !!).
    - Running with env `LACON_DISABLE=1` and any command → exits 0 with empty stdout (bypass via env).
    - Running with a matching rule and chain `echo hi && ls -la` → stdout is JSON with `hookSpecificOutput.updatedInput.command` containing the wrapped first segment and the literal " && ls -la" remainder.
    - Running with `tool_input.command == "vim file && ls"` (TUI in chain, with a rule for ls) → exits 0 with empty stdout (whole-chain bypass per D-15).
    - description/timeout/run_in_background fields are echoed back into updatedInput when present in the source payload.
  </behavior>
  <action>
    Replace the `run_hook` body in `crates/lacon-adapter-claudecode/src/lib.rs` with the full orchestration per 03-RESEARCH.md "System Architecture Diagram" (lines 191–246) and the lib.rs analog from 03-PATTERNS.md.

    Orchestration steps:
    0. **Non-Bash pass-through (defensive)** — if `input.tool_name != "Bash"` return `Ok(HookOutcome::PassThrough)` immediately. The hook is registered under `matcher: "Bash"` (D-domain) but a user could widen the matcher manually; defensive guard prevents the hook from rewriting Write/Edit/Read tool calls. Per RESEARCH Q4 (RESOLVED 2026-05-16).
    1. **Bypass-detect** — per 03-RESEARCH.md lines 906–922. Inline `fn detect_bypass(command: &str) -> bool`:
       - LSTRIP whitespace from `input.tool_input.command`, then check `starts_with("!!")` (D-23).
       - Check `std::env::var("LACON_DISABLE").as_deref() == Ok("1")` (D-24, mirrors `crates/lacon-core/src/runtime/mod.rs:173-177` exactly).
       - Return true if either fires.
       If bypassed → return `Ok(HookOutcome::PassThrough)` immediately (D-25 — no chain split, no resolve).
    2. **Chain split** — `let segments = crate::chain::split_chain(&input.tool_input.command);`. If `segments.is_empty()` (empty command) → return `Ok(HookOutcome::PassThrough)`.
    3. **Per-segment TUI check (BEFORE resolve, per D-15)** — for each segment compute `argv_for_resolution(&segment.text)` (helper below) and call `crate::tui::is_tui(&argv[0], &argv[1..])`. If ANY segment is TUI → return `Ok(HookOutcome::PassThrough)` (whole-chain bypass per CON-chained-tui-bypass-whole-chain).
    4. **Per-segment resolve + rewrite + wrap**:
       - Create ONE `lacon_core::rules::loader::RuleLoader::new(Some(PathBuf::from(&input.cwd)))` (D-14 cache amortization — one loader per hook invocation).
       - For each segment: tokenize its `.text` via the `argv_for_resolution` helper. If argv is empty → keep segment.text verbatim (degenerate).
       - Call `lacon_core::rules::match_argv_via_load_all(&mut loader, &argv)`. If `Ok(Some(resolved))` → apply rewrite + wrap; if `Ok(None)` → preserve `segment.text` byte-exact; if `Err(errors)` → log to stderr (`eprintln!("lacon-claude-hook: rule load error: {}", e)` per pattern) and treat as unmatched (best-effort: don't fail the hook because one rule file is bad).
       - When matched: `let rewritten_argv = lacon_core::rules::apply_rewrite(&argv, &resolved.rule.rewrite_unwrapped_or_default());` (use the resolved rule's RewriteSpec; if rule has no rewrite block, use `&RewriteSpec::default()`). Then quote each argv element: `let quoted: Vec<String> = rewritten_argv.iter().map(|a| crate::quote::quote_for_shell(a).into_owned()).collect();`. Build the wrapped string `format!("LACON_ASSISTANT=claude-code LACON_SESSION_ID={} LACON_TOOL_USE_ID={} lacon run --rule {} -- {}", input.session_id, input.tool_use_id, resolved.id, quoted.join(" "))` — this is the D-26 env-var prefix (extended with LACON_TOOL_USE_ID per RESEARCH Q3 RESOLVED 2026-05-16) + D-21 wrap form. Session ID is taken verbatim from `input.session_id`; quote it via `quote_for_shell` if it contains metachars (defensive — Claude Code session IDs are UUIDs in practice but treat the field as untrusted).
    5. **Reassemble**:
       - Build the output command string by iterating segments in order. For each segment i: push the segment's text (either the wrapped form for matched or the original `segment.text` for unmatched). Then push `segment.trailing_op_span.as_deref().unwrap_or("")` to preserve the original operator + whitespace.
       - If NO segment was rewritten (all unmatched, no TUI, no bypass) → return `Ok(HookOutcome::PassThrough)`. This is the cheapest hot path for the common case (most commands won't have a rule until Phase 5).
       - If at least one segment was rewritten → construct a new `BashToolInput` cloning input.tool_input but replacing `command` with the reassembled string. Call `crate::protocol::build_rewrite_response(&new_input)` and return `Ok(HookOutcome::Rewrite(value))`.

    Helper `fn argv_for_resolution(text: &str) -> Vec<String>` per D-08 (revised 2026-05-16 in CONTEXT.md):
    - Tokenize the segment string into argv. Handles single + double quotes (drops them in the output). `$(...)` is treated as part of the surrounding token — NOT opaquely state-machine-tracked. This is an explicit v1 scope reduction from D-08's original "$(...) opacity" scope, recorded in 03-CONTEXT.md's "Revised 2026-05-16" paragraph under D-08. The chain splitter (chain.rs, per D-06) retains full `$(...)` opacity for top-level operator detection — that DFA is unchanged. Only the secondary resolver-input tokenizer is scope-reduced. Implementation: a whitespace-split that respects single + double quotes (mirror the pattern from `chain.rs` quote-state tracking but without operator emission). Promoting to full `$(...)` opacity is deferred to v1.5+ if a real-world rule predicate needs it. Per CONTEXT.md D-08 (Revised 2026-05-16).
    - This helper lives as a private function inside lib.rs (not exported). 5–8 inline `#[cfg(test)] mod tests` covering: simple argv (`"echo hi"` → `["echo","hi"]`), single-quoted arg (`"echo 'hi there'"` → `["echo","hi there"]`), double-quoted arg, empty input → empty Vec.

    The `RewriteSpec::default()` fallback: 03-PATTERNS.md notes RewriteSpec implements `Default` via derive (schema.rs:119). Use `resolved.rule.rewrite.as_ref().unwrap_or(&RewriteSpec::default())` (or the equivalent — check the actual rule struct field name; the ResolvedRule shape may store rewrite as Option<RewriteSpec> or as a direct field). Read crates/lacon-core/src/rules/loader.rs and schema.rs to confirm the exact field path before writing the call.

    Update `crates/lacon-adapter-claudecode/src/bin/hook.rs`:
    - The skeleton from Plan 1 Task 3 already reads stdin via `serde_json::from_reader`. Replace the `HookOutcome::Rewrite(response) => { ... write_all(&b"\n") }` arm with the full implementation: lock stdout, `serde_json::to_writer(&mut handle, &value)?`, then `handle.write_all(b"\n")?`. The `PassThrough` arm remains `Ok(())` (empty stdout, exit 0).
    - Add 2–3 inline tests in lib.rs `#[cfg(test)] mod tests` covering: empty stdin command → PassThrough, "!!" prefix → PassThrough, LACON_DISABLE=1 → PassThrough. These are unit tests on `run_hook` (no subprocess spawn — that's hook_e2e's job).
  </action>
  <verify>
    <automated>cd /home/maurice/Projects/gherrink-lacon && cargo build --workspace 2>&amp;1 | tail -5; cargo test -p lacon-adapter-claudecode --lib 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0.
    - `cargo test -p lacon-adapter-claudecode --lib` is fully green (Plan 2's chain tests, Plan 3's tui/quote tests, Plan 1's protocol tests, plus new lib-level run_hook + argv_for_resolution unit tests).
    - `grep -c 'detect_bypass' crates/lacon-adapter-claudecode/src/lib.rs` returns at least 1.
    - `grep -c 'LACON_ASSISTANT=claude-code' crates/lacon-adapter-claudecode/src/lib.rs` returns 1 (literal substring lock — D-26 prefix is in source).
    - `grep -c 'LACON_SESSION_ID' crates/lacon-adapter-claudecode/src/lib.rs` returns at least 1.
    - `grep -c 'LACON_TOOL_USE_ID' crates/lacon-adapter-claudecode/src/lib.rs` returns at least 1 (RESEARCH Q3 RESOLVED 2026-05-16 — env-var prefix extension).
    - `grep -c 'tool_name != "Bash"' crates/lacon-adapter-claudecode/src/lib.rs` returns at least 1 (RESEARCH Q4 RESOLVED 2026-05-16 — non-Bash pass-through guard).
    - `grep -c 'lacon run --rule' crates/lacon-adapter-claudecode/src/lib.rs` returns 1 (D-21 wrap form).
    - `grep -c 'split_chain' crates/lacon-adapter-claudecode/src/lib.rs` returns at least 1.
    - `grep -c 'is_tui' crates/lacon-adapter-claudecode/src/lib.rs` returns at least 1.
    - `grep -c 'apply_rewrite' crates/lacon-adapter-claudecode/src/lib.rs` returns at least 1.
    - `grep -c 'quote_for_shell' crates/lacon-adapter-claudecode/src/lib.rs` returns at least 1.
    - `grep -c 'match_argv_via_load_all' crates/lacon-adapter-claudecode/src/lib.rs` returns at least 1.
    - `grep -c 'as_deref() == Ok("1")' crates/lacon-adapter-claudecode/src/lib.rs` returns at least 1 (LACON_DISABLE pattern mirrors runtime/mod.rs:175 exactly).
    - Running `echo '{"session_id":"s1","transcript_path":"/t","cwd":"/tmp","permission_mode":"default","hook_event_name":"PreToolUse","tool_name":"Bash","tool_input":{"command":"!! pnpm test"},"tool_use_id":"u"}' | target/debug/lacon-claude-hook` exits 0 with empty stdout.
  </acceptance_criteria>
  <done>
    `run_hook` composes all Plan 1/2/3 pieces. Bin/hook.rs writes the JSON response correctly. Unit tests cover bypass paths. Task 2 adds integration coverage via the binary spawn.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hook_e2e integration tests + Phase 2 env-var verification</name>
  <files>crates/lacon-adapter-claudecode/tests/hook_e2e.rs</files>
  <read_first>
    - crates/lacon-cli/tests/cli_run.rs (canonical assert_cmd + tempdir + write_rule pattern — lines 1–44)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md (lines 938–1046 — full hook_e2e fixture shape + 4 example tests)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-PATTERNS.md (`tests/hook_e2e.rs` section — assert_cmd::cargo_bin + write_stdin pattern)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-CONTEXT.md (D-03 — every echo-back field must round-trip; D-25 — bypass = empty stdout)
    - crates/lacon-adapter-claudecode/src/lib.rs (just-written run_hook)
    - crates/lacon-adapter-claudecode/src/protocol.rs (BashToolInput field set)
  </read_first>
  <behavior>
    - At least 7 e2e tests cover the full matrix per 03-VALIDATION.md per-task verification map for Plan 04:
      - `pass_through_unmatched_command_exits_zero_empty_stdout` (REQ-adapter-pretooluse-only, base behavior).
      - `matched_single_command_emits_rewrite_json` (REQ-adapter-pretooluse-only, T-hook-output-shape).
      - `bypass_via_bang_prefix_emits_empty_stdout` (REQ-adapter-bypass-detection, T-bypass-failsafe).
      - `bypass_via_lacon_disable_env_emits_empty_stdout` (REQ-adapter-bypass-detection, T-bypass-failsafe).
      - `tui_segment_in_chain_triggers_whole_chain_bypass` (REQ-adapter-tui-bypass, T-tui-whole-chain).
      - `chain_with_one_matched_one_unmatched_emits_chain_rewrite` (REQ-adapter-chained-commands).
      - `description_and_timeout_echoed_back_in_updated_input` (D-03 echo-back property, T-hook-output-shape).
      - `rewritten_command_contains_lacon_assistant_and_session_id` (D-26 + Phase 2 integration — the env-var prefix is in the wrapped command).
      - `pipe_in_segment_preserved_not_split` (REQ-adapter-pipes-passthrough — input `echo hi | grep h && ls` produces 2 segments, the first contains the pipe verbatim).
      - `non_bash_tool_passes_through` (RESEARCH Q4 RESOLVED 2026-05-16 — fixture with `tool_name: "Write"` → empty stdout + exit 0; defensive pass-through).
      - `rewritten_command_contains_lacon_tool_use_id` (RESEARCH Q3 RESOLVED 2026-05-16 — D-26 extended; the wrapped command carries `LACON_TOOL_USE_ID=<id>` for Phase 4 cross-correlation).
    - All tests pass via `cargo test -p lacon-adapter-claudecode --test hook_e2e`.
  </behavior>
  <action>
    Create `crates/lacon-adapter-claudecode/tests/hook_e2e.rs` per 03-RESEARCH.md lines 938–1046 and 03-PATTERNS.md `tests/hook_e2e.rs` section.

    Helper at top of file (per 03-RESEARCH.md lines 947–953):
    - `fn run_hook_with_input(input_json: &str) -> std::process::Output` calling `Command::cargo_bin("lacon-claude-hook").unwrap().write_stdin(input_json).output().expect("hook binary runs")`.
    - Optional: `fn run_hook_with_input_and_env(input_json: &str, env: &[(&str, &str)]) -> Output` for the LACON_DISABLE bypass test.
    - `fn write_rule(dir: &Path, rule_yaml: &str)` helper mirroring `crates/lacon-cli/tests/cli_run.rs:9-13` exactly (creates `.lacon/rules/test.yaml`).

    Tests (≥7, each with explicit fixture JSON):

    1. `pass_through_unmatched_command_exits_zero_empty_stdout` — empty tempdir, command `"echo hello"`, no rules → assert `output.status.success()` AND `output.stdout.is_empty()`. Verbatim per 03-RESEARCH.md:957-974.

    2. `matched_single_command_emits_rewrite_json` — write rule `{ id: echo-rule, match: { command: echo }, pipeline: [strip_ansi] }`, command `"echo hi"` → assert exit success, parse stdout as `serde_json::Value`, assert `value["hookSpecificOutput"]["hookEventName"] == "PreToolUse"`, `value["hookSpecificOutput"]["permissionDecision"] == "allow"`, `value["hookSpecificOutput"]["updatedInput"]["command"].as_str().unwrap().contains("lacon run --rule echo-rule")`.

    3. `description_and_timeout_echoed_back_in_updated_input` — fixture includes `tool_input.description == "say hi"`, `tool_input.timeout == 30000`, `tool_input.run_in_background == false` → assert all three appear unchanged in `updatedInput` (D-03 echo-back; T-hook-output-shape mitigation).

    4. `chain_with_one_matched_one_unmatched_emits_chain_rewrite` — write rule for `echo`, command `"echo hi && ls -la"` → assert exit success, parse stdout, `updatedInput.command` contains BOTH the wrapped `lacon run --rule echo-rule -- echo hi` AND ` && ls -la` (preserving the unmatched second segment AND the operator). Per 03-RESEARCH.md:1015-1043.

    5. `bypass_via_bang_prefix_emits_empty_stdout` — command `"!! pnpm test"`, no env override → assert `output.status.success()` AND `output.stdout.is_empty()`. T-bypass-failsafe.

    6. `bypass_via_lacon_disable_env_emits_empty_stdout` — command `"echo hi"` with a rule that WOULD match, but env `LACON_DISABLE=1` set on the Command builder → assert empty stdout + exit 0. Use `Command::cargo_bin("lacon-claude-hook").env("LACON_DISABLE", "1").write_stdin(input).output()`. T-bypass-failsafe.

    7. `tui_segment_in_chain_triggers_whole_chain_bypass` — write rule for `ls`, command `"vim file && ls -la"` (vim is pure TUI per D-16) → assert empty stdout + exit 0 (whole-chain bypass per D-15 — the matched `ls` segment is NOT wrapped because vim triggers bypass). T-tui-whole-chain.

    8. `rewritten_command_contains_lacon_assistant_and_session_id` — fixture `session_id == "session-abc-123"`, write rule for `echo`, command `"echo hi"` → assert `updatedInput.command` contains the substring `"LACON_ASSISTANT=claude-code"` AND `"LACON_SESSION_ID=session-abc-123"`. D-26 + Phase 2 integration property.

    9. `pipe_in_segment_preserved_not_split` — write rule for `echo`, command `"echo hi | grep h && ls"` → assert `updatedInput.command` contains the substring `"echo hi | grep h"` (NOT split on `|`) AND ` && ls`. REQ-adapter-pipes-passthrough at the orchestration boundary (chain_split.rs already proves it at the splitter boundary; this proves the whole hook respects it).

    10. `non_bash_tool_passes_through` — fixture JSON with `tool_name: "Write"` and `tool_input: { file_path: "/tmp/x", content: "hi" }` (any non-Bash shape; the hook never inspects tool_input fields for non-Bash names) → assert `output.status.success()` AND `output.stdout.is_empty()`. RESEARCH Q4 RESOLVED 2026-05-16 — defensive guard for matcher-widened settings.json. Use a `HookInput`-compatible fixture; the struct does not `deny_unknown_fields` so a different tool_input shape parses as long as the outer envelope is valid.

    11. `rewritten_command_contains_lacon_tool_use_id` — fixture `session_id == "session-abc-123"`, `tool_use_id == "toolu_01abc"`, write rule for `echo`, command `"echo hi"` → assert `updatedInput.command` contains the substring `"LACON_TOOL_USE_ID=toolu_01abc"`. RESEARCH Q3 RESOLVED 2026-05-16 — D-26 extended. Combined with test 8, the three env-var substrings (LACON_ASSISTANT, LACON_SESSION_ID, LACON_TOOL_USE_ID) are all locked.

    Each test must:
    - Use `tempdir()` for `.lacon/rules/` to avoid cross-test contamination.
    - Set `cwd` in the JSON payload to `dir.path().to_string_lossy()` so the hook's `RuleLoader::new(Some(cwd))` finds the test rule.
    - Use `serde_json::from_slice(&output.stdout)` for shape assertions (not `predicates::str::contains` on the raw JSON — too fragile).
    - Document with a comment which REQ-* and threat-ID the test gates.
  </action>
  <verify>
    <automated>cd /home/maurice/Projects/gherrink-lacon && cargo test -p lacon-adapter-claudecode --test hook_e2e 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lacon-adapter-claudecode --test hook_e2e` reports at least 11 passing tests.
    - `grep -c '#\[test\]' crates/lacon-adapter-claudecode/tests/hook_e2e.rs` returns at least 11.
    - The test `bypass_via_bang_prefix_emits_empty_stdout` exists and passes.
    - The test `tui_segment_in_chain_triggers_whole_chain_bypass` exists and passes.
    - The test `rewritten_command_contains_lacon_assistant_and_session_id` exists and asserts both LACON_ASSISTANT and LACON_SESSION_ID substrings appear in `updatedInput.command`.
    - The test `description_and_timeout_echoed_back_in_updated_input` exists and asserts description == "say hi" AND timeout == 30000 in updatedInput.
    - The test `non_bash_tool_passes_through` exists and asserts empty stdout + exit 0 for a fixture with `tool_name: "Write"` (RESEARCH Q4 RESOLVED 2026-05-16).
    - The test `rewritten_command_contains_lacon_tool_use_id` exists and asserts the `LACON_TOOL_USE_ID=<id>` substring appears in `updatedInput.command` (RESEARCH Q3 RESOLVED 2026-05-16).
    - `grep -c 'cargo_bin("lacon-claude-hook")' crates/lacon-adapter-claudecode/tests/hook_e2e.rs` returns at least 1.
    - All previous tests (chain_split, tui_heuristic, lib + protocol unit tests) remain green.
  </acceptance_criteria>
  <done>
    End-to-end behavior is locked. REQ-adapter-pretooluse-only, REQ-adapter-bypass-detection, REQ-adapter-chained-commands, REQ-adapter-tui-bypass, REQ-adapter-pipes-passthrough all have orchestration-level gates green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend benches/cold_start.rs with hook scenarios</name>
  <files>benches/cold_start.rs</files>
  <read_first>
    - benches/cold_start.rs (current — full file, lines 1–95)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md (lines 1142–1178 — `measure_hook` helper + two new scenarios)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-CONTEXT.md (Implementation-time benchmarks section — hook-passthrough target ≤2ms, hook-rewrite target ≤5ms; T-cold-start-regression)
    - .planning/intel/constraints.md (CON-nfr-cold-start-budget)
    - .planning/phases/02-local-tracking/02-06-SUMMARY.md (Phase 1 baseline measurements at the bottom — ~1154µs --version, used to set context for Plan 4's hook targets)
  </read_first>
  <behavior>
    - `cargo run --release --bin cold_start_probe` produces a markdown table including TWO new rows: `lacon-claude-hook passthrough` and `lacon-claude-hook rewrite`.
    - Each scenario reports min/median/p95/max in µs. The targets (2ms, 5ms) are documented in the probe's println output; the probe does NOT panic-gate on breach in v1 (T-cold-start-regression mitigation is soft — flagged in PHASE-BENCH.md per Phase 6).
    - The probe builds + finds `target/release/lacon-claude-hook` (mirror the existing `LACON_BIN` constant pattern).
    - Probe still runs the existing `--version` and `validate` scenarios; no regression.
  </behavior>
  <action>
    Edit `benches/cold_start.rs` per 03-RESEARCH.md lines 1142–1178:

    - Add `const HOOK_BIN: &str = "target/release/lacon-claude-hook";` next to the existing `LACON_BIN`.

    - Add `fn measure_hook(stdin_json: &str) -> std::time::Duration` per 03-RESEARCH.md:1156-1168: spawn the hook binary with `stdin = piped, stdout = piped`, write the fixture JSON, drop the stdin handle, wait for output, measure wall-clock from `Instant::now()` to elapsed. Mirror `measure_one`'s shape exactly so warm-up + percentile logic can reuse `measure_cold_start`.

    - Add `fn measure_cold_start_hook(stdin_json: &str) -> Vec<u128>` — same shape as the existing `measure_cold_start` but calling `measure_hook` instead of `measure_one`. (Alternatively, refactor `measure_cold_start` to take a closure — Claude's discretion. The simpler path is a parallel function.)

    - Add `fn run_hook_scenario(name: &str, stdin_json: &str)` mirroring `run_scenario` shape: warm up, sample 50 runs, compute min/median/p95/max, println the markdown row.

    - In `main()`, after the existing `validate <rule>` scenario, check `Path::new(HOOK_BIN).exists()` (warn + skip if not). Then call:
      - `run_hook_scenario("hook passthrough (no rule)", PASSTHROUGH_PAYLOAD)` with a minimal valid PreToolUse JSON for `echo hi` and no rule in cwd.
      - `run_hook_scenario("hook rewrite (matched)", REWRITE_PAYLOAD)` — needs a tempdir with a rule pre-written so the hook hits the rewrite path. Use `tempfile::tempdir()` to create a tempdir + write `.lacon/rules/echo.yaml`, then point the JSON payload's `cwd` field at that tempdir's path.
      - Target documentation: `println!("\nHook targets: passthrough ≤2000 µs median, rewrite ≤5000 µs median. Real measurements above. Phase 6 owns the formal gate.")`.

    - Constants for the two fixture payloads. Use simple multi-line string constants OR build via `serde_json::json!()` macro + `.to_string()` — the latter is cleaner. The rewrite payload must point `cwd` at the tempdir; build it inside `main()` after `tempdir()`.

    Tempfile is a workspace dev-dep (per Cargo.toml line 32); the bench package is `benches/Cargo.toml` — add `tempfile = { workspace = true }` to its `[dependencies]` if it isn't already there (read benches/Cargo.toml to confirm; if missing, add it). serde_json is needed for `json!()` macro usage — also add to benches/Cargo.toml dependencies if missing.

    The hook bench scenarios SHALL NOT be a gating regression test in v1. If the measured median exceeds the target, the probe still exits 0; the operator records the finding in a PHASE-BENCH.md (delegated to Phase 6 acceptance gate per the existing Phase 2 PHASE-BENCH.md precedent). This is T-cold-start-regression's accepted disposition: visibility, not blocking.
  </action>
  <verify>
    <automated>cd /home/maurice/Projects/gherrink-lacon && cargo build --release --bin lacon --bin lacon-claude-hook --bin cold_start_probe 2>&amp;1 | tail -5; cargo run --release --bin cold_start_probe 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run --release --bin cold_start_probe` exits 0.
    - Stdout from the probe contains a line beginning `| \`lacon hook passthrough` (or similar — the scenario row).
    - Stdout from the probe contains a line beginning `| \`lacon hook rewrite`.
    - `grep -c 'measure_hook' benches/cold_start.rs` returns at least 1.
    - `grep -c 'HOOK_BIN' benches/cold_start.rs` returns at least 1.
    - `grep -E '^tempfile' benches/Cargo.toml` succeeds (tempfile workspace dep present).
    - `grep -E '^serde_json' benches/Cargo.toml` succeeds (serde_json workspace dep present).
    - The existing `--version` and `validate <rule>` scenario rows still print (no regression).
    - `cargo build --release --workspace` exits 0.
  </acceptance_criteria>
  <done>
    Cold-start probe instrumented for the hook hot path. Measurements visible; v1 has telemetry, Phase 6 owns the formal gate. T-cold-start-regression has accepted-with-monitoring disposition.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code stdin JSON → run_hook | Untrusted JSON; struct deserialize from Plan 1 mitigates schema drift; the command field carries adversarial content for both injection and TUI-detection tests. |
| run_hook output → Claude Code parser | The hookSpecificOutput shape must match D-03 exactly or Claude Code silently drops the rewrite (T-hook-output-shape). |
| Wrapped command → user's bash exec | The wrapped string `LACON_ASSISTANT=... lacon run --rule X -- <quoted argv>` undergoes ONE shell parse. quote_for_shell must survive that parse (T-quote-injection — mitigated in Plan 3, exercised in Plan 4 hook_e2e). |
| Bypass paths → hook hot path | `!!` prefix and LACON_DISABLE=1 must return empty stdout fast (T-bypass-failsafe — empty stdout is the cheapest hot path). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | T (Tampering) | hookSpecificOutput shape | mitigate | hook_e2e tests assert `hookEventName == "PreToolUse"` AND `permissionDecision == "allow"` AND all `tool_input` fields are echoed back. Mitigates T-hook-output-shape from planning guidance. |
| T-03-04-02 | E (Elevation of privilege) | Bypass failsafe (!!/LACON_DISABLE) | mitigate | Two e2e tests prove bypass produces empty stdout + exit 0. The LACON_DISABLE precedent at `crates/lacon-core/src/runtime/mod.rs:175` is mirrored exactly (`as_deref() == Ok("1")`) so other values (empty, "0", "true") do NOT bypass. Mitigates T-bypass-failsafe. |
| T-03-04-03 | T (Tampering) | TUI-segment-in-chain (T-tui-whole-chain) | mitigate | hook_e2e test `tui_segment_in_chain_triggers_whole_chain_bypass` — `vim file && ls -la` with an ls rule → empty stdout. TUI check runs per-segment BEFORE resolve (D-15). |
| T-03-04-04 | T (Tampering) | Byte-exact reassembly with operators preserved | mitigate | Plan 2's splitter records `trailing_op_span`. Plan 4's reassembly walks segments and joins each segment's output text (wrapped or original) with `trailing_op_span.unwrap_or("")`. hook_e2e test `chain_with_one_matched_one_unmatched_emits_chain_rewrite` asserts ` && ls -la` substring appears in the rewritten command. |
| T-03-04-05 | I (Information disclosure) | description/timeout/run_in_background dropped | mitigate | hook_e2e test asserts all three fields echo back. Plan 1 protocol.rs's `skip_serializing_if = "Option::is_none"` ensures absent fields stay absent (not emitted as null). |
| T-03-04-06 | D (Denial of service) | Cold-start regression | accept | T-cold-start-regression disposition. Hook bench measures the budget but does not panic-gate. Phase 6 owns the formal acceptance gate. Soft threshold targets (2ms passthrough, 5ms rewrite) documented in probe output. |
| T-03-04-07 | E (Elevation of privilege) | Adapter ↔ Phase 2 tracker env-var contract | mitigate | hook_e2e test `rewritten_command_contains_lacon_assistant_and_session_id` asserts the LACON_ASSISTANT=claude-code and LACON_SESSION_ID=<id> prefix is present. Phase 2's tracker at `crates/lacon-cli/src/commands/run.rs:270-272` already consumes these vars; the test gate prevents drift. |
</threat_model>

<verification>
- `cargo build --workspace` clean.
- `cargo test -p lacon-adapter-claudecode --lib` green (all module unit tests + protocol + lib's new run_hook unit tests).
- `cargo test -p lacon-adapter-claudecode --test chain_split` still green (no regression from Plan 2).
- `cargo test -p lacon-adapter-claudecode --test tui_heuristic` still green (no regression from Plan 3).
- `cargo test -p lacon-adapter-claudecode --test hook_e2e` green (≥9 e2e tests).
- `cargo test -p lacon-core --lib rules::rewrite::tests` still green (no regression).
- `cargo test --workspace --tests` shows no regression in Phase 1/2 test suites.
- `cargo run --release --bin cold_start_probe` exits 0 and emits hook passthrough + rewrite scenario rows.
- `cargo test -p lacon-cli --test cli_surface` continues to pass (no new subcommand — REQ-cli-surface-cap preserved).
</verification>

<success_criteria>
- `run_hook` composes the full pipeline (bypass → split → tui → resolve → rewrite → quote → wrap → reassemble → emit).
- 9 hook_e2e tests cover the full requirement+threat matrix.
- Cold-start probe extended to the hook hot path.
- Phase 2 env-var contract (D-26) satisfied via the inline LACON_ASSISTANT/LACON_SESSION_ID prefix in every wrapped command.
- REQ-adapter-pretooluse-only, REQ-adapter-bypass-detection, REQ-adapter-chained-commands, REQ-adapter-tui-bypass, REQ-adapter-pipes-passthrough all closed at the orchestration level.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-code-adapter-lacon-init/03-04-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Plan 5 (lacon init) begins Wave 4 after this Plan's commit.
</output>
