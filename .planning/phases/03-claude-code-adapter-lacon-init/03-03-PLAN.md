---
phase: 03-claude-code-adapter-lacon-init
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - crates/lacon-adapter-claudecode/src/tui.rs
  - crates/lacon-adapter-claudecode/src/quote.rs
  - crates/lacon-adapter-claudecode/src/lib.rs
  - crates/lacon-adapter-claudecode/tests/tui_heuristic.rs
  - crates/lacon-core/src/rules/rewrite.rs
  - crates/lacon-core/src/rules/mod.rs
autonomous: true
requirements:
  - REQ-adapter-tui-bypass
tags: [phase-03, adapter, tui, rewrite, quote, pure-functions]

must_haves:
  truths:
    - "D-15: `is_tui(command, args) -> bool` is a public function in `crates/lacon-adapter-claudecode/src/tui.rs` (NOT in lacon-core — YAGNI on cross-adapter reuse per the spec's adapter-code locality rule)."
    - "D-16: Pure-TUI list is a `const PURE_TUI: &[&str]` of the 22 basenames from `docs/specs/chained-commands.md:85-89`; lookup is by `basename(args[0])` via `std::path::Path::file_name`."
    - "D-17: Conditional patterns dispatched via `match command { \"git\" => ..., \"npm\"|\"yarn\"|\"pnpm\" => ..., \"node\"|\"python\"|\"python3\" => is_repl(args), \"mysql\"|\"psql\"|\"sqlite3\" => is_repl(args), _ => false }` exactly matching the spec table at `docs/specs/chained-commands.md:91-101`."
    - "D-18: Test layout: `tests/tui_heuristic.rs` covers 22 pure-TUI rows + 8 conditional patterns + negative tests for non-TUI lookalikes; chain_split (Plan 2) and hook_e2e (Plan 4) round out adapter coverage."
    - "is_tui(command, args) returns true for all 22 pure-TUI basenames per docs/specs/chained-commands.md:85-87."
    - "is_tui returns true exactly for the 8 conditional patterns (git rebase -i, git commit without -m/-F, git add -p, etc.) per docs/specs/chained-commands.md:91-101."
    - "is_tui returns false for negative-tests like `ls`, `git status`, `mysql -e 'SELECT 1'`, `pnpm run dev`."
    - "D-19: `apply_rewrite(argv, &RewriteSpec) -> Vec<String>` lives in `crates/lacon-core/src/rules/rewrite.rs`; apply order is remove_flags → replace_flags → add_flags; `add_flags` is idempotent (skip if already present); argv[0] is NEVER touched; `apply(apply(x)) == apply(x)` invariant locked by regression test."
    - "apply_rewrite(argv, rewrite) is idempotent: apply(apply(x)) == apply(x) for every rewrite block tested."
    - "apply_rewrite never touches argv[0]."
    - "D-20: `quote_for_shell(arg) -> Cow<str>` lives in `crates/lacon-adapter-claudecode/src/quote.rs`; returns `Cow::Borrowed` when no metachars/whitespace are present, otherwise single-quote-wraps and replaces embedded `'` with `'\\''`. POSIX-portable (sh/bash/dash/zsh). Metachar set: `|&;<>()$\\`\\\\\\\"'\\n\\t *?[#~=%!`."
    - "D-21: Adapter emits rewritten segments as `lacon run --rule <id> -- <quoted argv joined with single spaces>`; unchanged segments preserve `segment.text` byte-exact with no quote round-trip."
    - "D-22: `quote_for_shell` only has to survive ONE shell parse (Claude Code's bash invocation of `lacon run`); downstream Runner uses `Command::new(&argv[0]).args(&argv[1..])` so there is no second shell-interpret hop. Round-trip tests prove `--reporter='custom reporter'`, `$(...)`, embedded quotes survive intact."
    - "quote_for_shell round-trips through `/bin/sh` for: plain string, embedded space, $(rm -rf /), backticks, embedded single quote, embedded newline, embedded tab, empty string, --reporter=val, --reporter=custom reporter, (group)."
  artifacts:
    - path: "crates/lacon-adapter-claudecode/src/tui.rs"
      provides: "is_tui(command, args) + PURE_TUI const + conditional dispatchers"
      exports: ["is_tui"]
      contains: "PURE_TUI"
      min_lines: 100
    - path: "crates/lacon-adapter-claudecode/src/quote.rs"
      provides: "quote_for_shell(arg) -> Cow<str> + 11 round-trip tests"
      exports: ["quote_for_shell"]
      min_lines: 80
    - path: "crates/lacon-core/src/rules/rewrite.rs"
      provides: "apply_rewrite(argv, &RewriteSpec) -> Vec<String> + 10 regression tests"
      exports: ["apply_rewrite"]
      min_lines: 80
    - path: "crates/lacon-adapter-claudecode/tests/tui_heuristic.rs"
      provides: "22 pure-TUI tests + 8 conditional pattern tests + negative tests"
      contains: "fn pure_tui_vim_is_tui"
      min_lines: 150
  key_links:
    - from: "crates/lacon-adapter-claudecode/src/lib.rs"
      to: "crates/lacon-adapter-claudecode/src/tui.rs"
      via: "pub mod tui;"
      pattern: "pub mod tui"
    - from: "crates/lacon-adapter-claudecode/src/lib.rs"
      to: "crates/lacon-adapter-claudecode/src/quote.rs"
      via: "pub mod quote;"
      pattern: "pub mod quote"
    - from: "crates/lacon-core/src/rules/mod.rs"
      to: "crates/lacon-core/src/rules/rewrite.rs"
      via: "pub mod rewrite; pub use rewrite::apply_rewrite;"
      pattern: "pub mod rewrite"
---

<objective>
Ship three pure functions that Plan 4's orchestration consumes: `is_tui(command, args)` (D-15/16/17) lives in `lacon-adapter-claudecode::tui`, `quote_for_shell(arg)` (D-20) lives in `lacon-adapter-claudecode::quote`, and `apply_rewrite(argv, &RewriteSpec)` (D-19) lives in `lacon-core::rules::rewrite`. Each is independently testable; together they let Plan 4 build the rewrite path with zero algorithmic risk.

Purpose: TUI bypass is per-segment + before rule resolution (D-15 + REQ-adapter-tui-bypass). Quote_for_shell is the trust boundary against shell injection (T-quote-injection); it MUST survive ONE shell parse round-trip per D-22. Apply_rewrite is idempotent (D-19 — `apply(apply(x)) == apply(x)`) so re-running the hook on a previously-rewritten command doesn't duplicate flags.

Output: three pure-function modules (tui.rs, quote.rs, rewrite.rs) with their tests, plus the lib.rs `pub mod` declarations and the lacon-core `pub mod rewrite` + re-export. No orchestration glue — Plan 4 calls these.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-CONTEXT.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-PATTERNS.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-VALIDATION.md
@.planning/phases/03-claude-code-adapter-lacon-init/03-01-PLAN.md
@.planning/intel/constraints.md
@docs/specs/chained-commands.md
@docs/specs/filter-rule-schema.md
@crates/lacon-core/src/rules/schema.rs
@crates/lacon-core/src/rules/mod.rs
@crates/lacon-adapter-claudecode/src/lib.rs
@crates/lacon-core/src/tracking/normalize.rs

<interfaces>
<!-- Three independent pure functions. -->

In crates/lacon-adapter-claudecode/src/tui.rs (new):
```rust
/// True if `command` is a pure TUI tool, or if (command, args) matches one
/// of the conditional patterns per docs/specs/chained-commands.md:91-101.
/// Lookup is via basename(command).
pub fn is_tui(command: &str, args: &[String]) -> bool;

pub const PURE_TUI: &[&str] = &[ /* 22 entries */ ];
```

In crates/lacon-adapter-claudecode/src/quote.rs (new):
```rust
/// POSIX-portable shell-quote (sh/bash/dash/zsh).
/// Returns Cow::Borrowed when no quoting needed; Cow::Owned otherwise.
pub fn quote_for_shell(arg: &str) -> std::borrow::Cow<'_, str>;
```

In crates/lacon-core/src/rules/rewrite.rs (new):
```rust
/// Apply a RewriteSpec to argv. Pure, idempotent: apply(apply(x)) == apply(x).
/// argv[0] is NEVER touched.
pub fn apply_rewrite(argv: &[String], rewrite: &crate::rules::schema::RewriteSpec)
    -> Vec<String>;
```

From crates/lacon-core/src/rules/schema.rs (consumed by apply_rewrite):
```rust
pub struct RewriteSpec {
    pub add_flags: Vec<String>,
    pub remove_flags: Vec<String>,
    pub replace_flags: BTreeMap<String, String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tui.rs — pure-TUI table + conditional dispatchers + 30-row test matrix</name>
  <files>
    crates/lacon-adapter-claudecode/src/tui.rs,
    crates/lacon-adapter-claudecode/src/lib.rs,
    crates/lacon-adapter-claudecode/tests/tui_heuristic.rs
  </files>
  <read_first>
    - docs/specs/chained-commands.md (lines 83–108 — the v1 TUI list and conditional patterns table, authoritative)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-CONTEXT.md (D-15, D-16, D-17, D-18)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md (lines 546–613 — PURE_TUI const, conditional dispatch match, conditional dispatcher specs table)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-PATTERNS.md (`tui.rs` section — pure-fn module shape from normalize.rs + ALLOWED_SUBCOMMANDS const slice analog)
    - crates/lacon-core/src/tracking/normalize.rs (canonical module shape with const + tests inline OR separate)
    - .planning/intel/constraints.md (CON-chained-tui-bypass-whole-chain, CON-chained-tui-list-v1)
  </read_first>
  <behavior>
    - `is_tui("vim", &[])` returns true. Same for the other 21 pure-TUI basenames including path-prefixed forms (e.g. `is_tui("/usr/bin/vim", &[])` → true via basename extraction).
    - `is_tui("git", &["rebase", "-i", "HEAD~5"])` returns true. `is_tui("git", &["rebase", "HEAD~5"])` returns false.
    - `is_tui("git", &["commit"])` returns true (no -m/-F). `is_tui("git", &["commit", "-m", "msg"])` returns false.
    - `is_tui("git", &["add", "-p"])` returns true. `is_tui("git", &["add", "file.txt"])` returns false.
    - `is_tui("npm", &["init"])` returns true. `is_tui("npm", &["init", "-y"])` returns false.
    - `is_tui("python3", &[])` returns true (REPL). `is_tui("python3", &["script.py"])` returns false.
    - `is_tui("psql", &[])` returns true. `is_tui("psql", &["-c", "SELECT 1"])` returns false.
    - Negative tests: `is_tui("ls", &[])` returns false. `is_tui("git", &["status"])` returns false. `is_tui("pnpm", &["run", "dev"])` returns false. `is_tui("cargo", &["test"])` returns false.
    - 22 pure-TUI tests + 8 conditional tests + ≥4 negative tests = ≥34 #[test] functions in `tests/tui_heuristic.rs`.
  </behavior>
  <action>
    Update `crates/lacon-adapter-claudecode/src/lib.rs` to declare `pub mod tui;` alongside `pub mod chain;` from Plan 2.

    Create `crates/lacon-adapter-claudecode/src/tui.rs` per 03-RESEARCH.md lines 546–613 and 03-PATTERNS.md `tui.rs` section:
    - `//!` docstring citing `docs/specs/chained-commands.md:83-108` as the authoritative TUI spec.
    - `pub const PURE_TUI: &[&str] = &[ ... ]` listing the 22 basenames in five comment-grouped sections (Editors / Pagers / System monitors / Multiplexers and remote shells / REPLs always interactive / Tools that take over terminal). Use the ALLOWED_SUBCOMMANDS const-slice analog from `crates/lacon-cli/tests/cli_surface.rs:6-8` for shape (one entry per line, grouped by category-comment).
    - `pub fn is_tui(command: &str, args: &[String]) -> bool` — step 1: extract basename via `std::path::Path::new(command).file_name().and_then(|s| s.to_str()).unwrap_or(command)` (per 03-PATTERNS.md "Argv basename extraction" — uses Path API not `rsplit('/')`). Step 2: pure-TUI table lookup via `PURE_TUI.contains(&basename)`. Step 3: dispatch via `match basename { "git" => is_git_interactive(args), "npm" | "yarn" | "pnpm" => is_pkg_init_interactive(args), "node" | "python" | "python3" => is_repl(args), "mysql" | "psql" | "sqlite3" => is_db_interactive(args, basename), _ => false }`.
    - Four private functions implementing the conditional patterns per 03-RESEARCH.md lines 600–606:
      - `fn is_git_interactive(args: &[String]) -> bool` — match on `args[0]`: `rebase` → contains -i OR --interactive; `commit` → none of -m / --message / --message=* (starts_with `--message=`) / -F / --file present; `add` → contains -p OR --patch OR -i OR --interactive; `checkout` → contains -p OR --patch; `stash` → contains -p OR --patch; else false.
      - `fn is_pkg_init_interactive(args: &[String]) -> bool` — true if `args[0] == "init"` AND neither `-y` nor `--yes` present.
      - `fn is_repl(args: &[String]) -> bool` — true if every element in args starts with `-` (no positional arg) OR args is empty. Note: per 03-RESEARCH.md:607 the conservative form (treats `python --version` as TUI) is intentional v1 behavior; document this in the function doc comment.
      - `fn is_db_interactive(args: &[String], _basename: &str) -> bool` — true if NO positional argument (every arg starts with `-`) AND no `-c` / `-e` / `-f` / `--command` / `--execute` / `--file` flag present.
    - Inline `#[cfg(test)] mod tests` with 3–5 smoke tests (basename extraction, one pure-TUI happy path, one negative).

    Create `crates/lacon-adapter-claudecode/tests/tui_heuristic.rs` with the `s(&[...])` helper convention and one #[test] per row:
    - 22 pure-TUI tests (one per basename in PURE_TUI) — `pure_tui_vim_is_tui` through `pure_tui_visudo_is_tui`.
    - 8 conditional tests covering each row of docs/specs/chained-commands.md:91-101:
      - `git_rebase_interactive_flag_true` + `git_rebase_no_interactive_flag_false`
      - `git_commit_no_message_true` + `git_commit_with_dash_m_false`
      - `git_add_patch_true` + `git_add_filename_false`
      - `git_checkout_patch_true`
      - `git_stash_patch_true`
      - `npm_init_no_yes_true` + `npm_init_with_yes_false` (one row per package manager — npm/yarn/pnpm)
      - `python3_no_args_true` (REPL) + `python3_with_script_false`
      - `psql_no_positional_true` + `psql_with_dash_c_false`
    - 4–6 negative tests: `ls_not_tui`, `git_status_not_tui`, `pnpm_run_dev_not_tui`, `cargo_test_not_tui`.
    - 1 path-stripping test: `vim_with_absolute_path_still_tui` using `/usr/bin/vim`.
  </action>
  <verify>
    <automated>cd /home/maurice/Projects/gherrink-lacon && cargo test -p lacon-adapter-claudecode --test tui_heuristic 2>&amp;1 | tail -10; cargo test -p lacon-adapter-claudecode --lib tui::tests 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lacon-adapter-claudecode --test tui_heuristic` reports at least 34 passing tests.
    - `cargo test -p lacon-adapter-claudecode --lib tui::tests` passes.
    - `grep -cE 'pure_tui_[a-z]+_is_tui' crates/lacon-adapter-claudecode/tests/tui_heuristic.rs` returns 22.
    - `grep -c 'PURE_TUI' crates/lacon-adapter-claudecode/src/tui.rs` returns at least 2 (the const declaration + the lookup).
    - `grep -cE '"vim"|"vi"|"nvim"|"nano"|"emacs"' crates/lacon-adapter-claudecode/src/tui.rs` returns at least 5 (editors section present).
    - `grep -cE '"ipython"|"irb"|"pry"|"redis-cli"|"crontab"|"visudo"' crates/lacon-adapter-claudecode/src/tui.rs` returns at least 6.
    - The `is_git_interactive` function exists and the regression-guard test `git_commit_no_message_true` returns true for `["commit"]`.
    - `grep -c 'pub mod tui' crates/lacon-adapter-claudecode/src/lib.rs` returns 1.
  </acceptance_criteria>
  <done>
    `is_tui` is correct against the spec's full 30-row table. Plan 4's TUI-before-resolve dispatch can call it directly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: quote.rs — POSIX shell-quote + 11 round-trip tests</name>
  <files>
    crates/lacon-adapter-claudecode/src/quote.rs,
    crates/lacon-adapter-claudecode/src/lib.rs
  </files>
  <read_first>
    - .planning/phases/03-claude-code-adapter-lacon-init/03-CONTEXT.md (D-20, D-22 — single-quote algorithm + the one-shell-parse trust property)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md (lines 692–773 — full algorithm + 11 round-trip test fixtures)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-PATTERNS.md (`quote.rs` section — Cow<str> pattern is novel; module shape from normalize.rs)
    - crates/lacon-core/src/tracking/normalize.rs (pure-fn + inline-tests analog)
    - crates/lacon-core/src/runtime/mod.rs (lines 138–141 — the subprocess argument injection mitigation that bounds the trust scope of quote_for_shell to "must survive ONE shell parse")
  </read_first>
  <behavior>
    - `quote_for_shell("hello")` returns `Cow::Borrowed("hello")` (no allocation).
    - `quote_for_shell("hello world")` returns `Cow::Owned("'hello world'")`.
    - `quote_for_shell("$(rm -rf /)")` returns a quoted string that, fed through `/bin/sh -c "printf '%s\n' <quoted>"`, prints `$(rm -rf /)` literally (NOT executing the `rm` — proves T-quote-injection is mitigated).
    - `quote_for_shell("it's")` correctly encodes the embedded single quote via the `'\''` close-escape-reopen idiom.
    - `quote_for_shell("")` returns `Cow::Owned("''")`.
    - `quote_for_shell("a\nb")` round-trips through `sh -c "printf '%s\n' <q>"` and produces two output lines (the newline survives).
    - The metachar set is verbatim from D-20: `|&;<>()$\`\\\"'\n\t *?[#~=%!`.
    - All 11 round-trip tests pass.
  </behavior>
  <action>
    Update `crates/lacon-adapter-claudecode/src/lib.rs` to add `pub mod quote;` alongside `pub mod chain; pub mod tui;`.

    Create `crates/lacon-adapter-claudecode/src/quote.rs` per 03-RESEARCH.md lines 696–725:
    - `//!` docstring citing D-20 (algorithm) + D-22 (one-shell-parse trust scope) + `crates/lacon-core/src/runtime/mod.rs:138-141` (downstream Runner does NOT re-shell-parse — so this quoter only has to survive ONE bash invocation).
    - `pub fn quote_for_shell(arg: &str) -> std::borrow::Cow<'_, str>` — body per 03-RESEARCH.md lines 707–725:
      - Const METACHARS byte array (verbatim D-20 set).
      - `needs_quote` flag: true if arg is empty OR any byte is in METACHARS.
      - If !needs_quote → return `Cow::Borrowed(arg)`.
      - Else: build a String with capacity `arg.len() + 2`, push `'`, iterate chars: if `'` push `'\''` (4 bytes — close, escape, reopen); else push char verbatim. Push trailing `'`. Return `Cow::Owned`.
    - Inline `#[cfg(test)] mod tests` with 11 tests covering 03-RESEARCH.md lines 759–770 verbatim:
      - `quote_plain_no_quote` — assertion is exact-equality, not round-trip (the borrow case).
      - `quote_with_space` — round-trip via `roundtrip_via_sh(&["a b"])` returns vec!["a b"].
      - `quote_with_dollar` — `"$(rm -rf /)"` survives literally (CRITICAL T-quote-injection test).
      - `quote_with_backtick` — `"`whoami`"`.
      - `quote_with_single_q` — `"it's"`.
      - `quote_with_newline` — `"a\nb"` becomes 2 output lines.
      - `quote_with_tab` — `"a\tb"`.
      - `quote_empty` — assert_eq!(quote_for_shell(""), "''").
      - `quote_eq_value` — `"--reporter=val"`.
      - `quote_eq_with_space` — `"--reporter=custom reporter"`.
      - `quote_paren` — `"(group)"`.
    - The `roundtrip_via_sh` helper (per 03-RESEARCH.md lines 747–757) builds `printf '%s\n' <quoted args...>`, runs through `std::process::Command::new("/bin/sh").arg("-c").arg(&cmd).output()`, parses stdout via `String::from_utf8(...).unwrap()` and splits on lines. Document with a comment that this is the first test that shells out for verification — justified by D-22 security property.

    The round-trip tests are required to run as part of `cargo test` (not gated behind a feature flag) — the planner accepts the small overhead because the test surface is the trust boundary. CI hermeticity is not violated: `/bin/sh` is present on all v1 targets (macOS + Linux).
  </action>
  <verify>
    <automated>cd /home/maurice/Projects/gherrink-lacon && cargo test -p lacon-adapter-claudecode --lib quote::tests 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lacon-adapter-claudecode --lib quote::tests` reports at least 11 passing tests.
    - `grep -c 'pub mod quote' crates/lacon-adapter-claudecode/src/lib.rs` returns 1.
    - The METACHARS byte slice in quote.rs contains all of: `|`, `&`, `;`, `<`, `>`, `(`, `)`, `$`, `` ` ``, `\\`, `"`, `'`, space, tab, newline, `*`, `?`, `[`, `#`, `~`, `=`, `%`, `!`.
    - The dollar-sign test (`quote_with_dollar`) passes — proves T-quote-injection is mitigated.
    - The `'\''` close-escape-reopen idiom is present in quote.rs source: `grep -c "'\\\\''"` returns at least 1 (note: the literal in Rust source is `"'\\''"` — match accordingly).
    - The function signature uses `Cow<'_, str>` (zero-alloc borrow path on no-quote-needed inputs).
  </acceptance_criteria>
  <done>
    `quote_for_shell` is correct across the 11 POSIX round-trip cases. Plan 4 uses it on every quoted-and-rewritten segment.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lacon-core/rules/rewrite.rs — apply_rewrite + 10 idempotency tests</name>
  <files>
    crates/lacon-core/src/rules/rewrite.rs,
    crates/lacon-core/src/rules/mod.rs
  </files>
  <read_first>
    - crates/lacon-core/src/rules/schema.rs (lines 118–133 — RewriteSpec definition is consumed verbatim)
    - crates/lacon-core/src/rules/mod.rs (current 12-line re-export block — needs `pub mod rewrite; pub use rewrite::apply_rewrite;`)
    - crates/lacon-core/src/tracking/normalize.rs (canonical pure-fn + inline-tests module shape)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-CONTEXT.md (D-19 — apply order, idempotency, argv[0] never touched)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-RESEARCH.md (lines 619–690 — full implementation + 10-row test matrix including the T9 multi-arg-flag ambiguity)
    - .planning/phases/03-claude-code-adapter-lacon-init/03-PATTERNS.md (`crates/lacon-core/src/rules/rewrite.rs` section — module location precedent + mod.rs re-export pattern)
    - docs/specs/filter-rule-schema.md (the rewrite block schema contract — add_flags is idempotent guarantee)
  </read_first>
  <behavior>
    - `apply_rewrite(&["cargo".into(), "test".into()], &spec_with_add_no_color)` returns `["cargo", "test", "--no-color"]`.
    - Re-applying the same spec to the result returns the SAME result (no duplication of `--no-color`).
    - `apply_rewrite(argv, spec)` never modifies `argv[0]` even when `spec.replace_flags` contains a mapping for `argv[0]`.
    - Apply order is `remove_flags` → `replace_flags` → `add_flags` (the post-remove/post-replace argv is what `add_flags` checks against for idempotency).
    - `apply_rewrite(&[], spec)` returns `Vec::new()`.
    - 10 regression tests in the inline test module cover the 10 cases in 03-RESEARCH.md lines 668–680.
  </behavior>
  <action>
    Update `crates/lacon-core/src/rules/mod.rs` to declare `pub mod rewrite;` and add `pub use rewrite::apply_rewrite;` to the existing re-export block (next to `pub use loader::*` and `pub use schema::*`).

    Create `crates/lacon-core/src/rules/rewrite.rs` per 03-RESEARCH.md lines 619–664:
    - `//!` docstring citing D-19 + the idempotency invariant `apply(apply(x)) == apply(x)` + the apply order (remove → replace → add) + the argv[0] never-touched contract.
    - `use crate::rules::schema::RewriteSpec;`.
    - `pub fn apply_rewrite(argv: &[String], rewrite: &RewriteSpec) -> Vec<String>` — body per 03-RESEARCH.md:
      - Early-return empty Vec for empty argv.
      - Pre-allocate output Vec with capacity `argv.len() + rewrite.add_flags.len()`.
      - Push `argv[0].clone()` first (argv[0] preserved verbatim — D-19 invariant).
      - `after_remove`: filter argv[1..] dropping any element matching any string in `rewrite.remove_flags`.
      - `after_replace`: walk `after_remove`, for each element check `rewrite.replace_flags.get(elem.as_str())`; if Some(new), push new.clone(); else push the original.
      - Extend output with `after_replace`.
      - `add_flags` loop: for each flag in `rewrite.add_flags`, push only if NOT already present in `out[1..]` (string-equal check — idempotency).
      - Return `out`.
    - Inline `#[cfg(test)] mod tests` with 10 tests covering 03-RESEARCH.md lines 668–680 exactly:
      - T1 `add_to_clean_argv` — `["cargo","test"]` + add `["--no-color"]` → `["cargo","test","--no-color"]`.
      - T2 `add_already_present_is_noop` — `["cargo","test","--no-color"]` + add `["--no-color"]` → unchanged.
      - T3 `apply_twice_is_idempotent` — apply T1's spec twice, output equals one application. This is the `apply(apply(x)) == apply(x)` invariant check — must include explicit comment citing D-19.
      - T4 `remove_present_flag` — `["cargo","test","--verbose"]` + remove `["--verbose"]` → `["cargo","test"]`.
      - T5 `remove_absent_flag_noop` — `["cargo","test"]` + remove `["--verbose"]` → unchanged.
      - T6 `remove_removes_all_occurrences` — `["cargo","test","--verbose","--verbose"]` + remove → `["cargo","test"]`.
      - T7 `replace_basic` — `["pnpm","install","--progress"]` + replace `{"--progress":"--no-progress"}` → `["pnpm","install","--no-progress"]`.
      - T8 `replace_already_replaced_noop` — `["pnpm","install","--no-progress"]` + same spec → unchanged (the old form is absent, so replace is no-op; this is the idempotency check on the replace path).
      - T9 `multi_arg_flag_literal_handling` — `["vitest","--reporter","verbose"]` + add_flags `["--reporter","silent"]` → `["vitest","--reporter","verbose","silent"]` (per 03-RESEARCH.md:686 — `--reporter` is already present so only `silent` is appended; document with comment that this is intentional per D-19 literal-element semantics and the alternative `--reporter=silent` style works as a single-element add).
      - T10 `argv0_never_touched` — `["cargo","build"]` + replace `{"cargo":"evil"}` → `["cargo","build"]` (D-19 invariant).
    - Use the `s(&[...])` helper convention from `crates/lacon-core/src/tracking/normalize.rs:33-75`. Use `BTreeMap::from([("k".to_string(), "v".to_string())])` (or `BTreeMap::new() + insert`) for replace_flags fixtures.
  </action>
  <verify>
    <automated>cd /home/maurice/Projects/gherrink-lacon && cargo test -p lacon-core --lib rules::rewrite::tests 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lacon-core --lib rules::rewrite::tests` reports at least 10 passing tests.
    - `grep -c 'pub use rewrite::apply_rewrite' crates/lacon-core/src/rules/mod.rs` returns 1.
    - `grep -c 'pub mod rewrite' crates/lacon-core/src/rules/mod.rs` returns 1.
    - `grep -c 'pub fn apply_rewrite' crates/lacon-core/src/rules/rewrite.rs` returns 1.
    - The T3 idempotency test exists and asserts `apply_rewrite(&apply_rewrite(argv, &spec), &spec) == apply_rewrite(argv, &spec)`.
    - The T10 argv0-never-touched test exists and asserts the first element is preserved when replace_flags maps it.
    - `cargo check --workspace` is clean — no warnings introduced.
  </acceptance_criteria>
  <done>
    `apply_rewrite` is in `lacon-core` and idempotent. Plan 4's per-segment rewrite step consumes it via `lacon_core::rules::apply_rewrite`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User command (incl. malicious args) → `quote_for_shell` → bash exec | `quote_for_shell` is the only thing standing between rule-author / model-generated argv and a shell. A quoting bug = command injection. |
| Rewritten argv → re-application (Plan 4 may re-rewrite a previously rewritten argv) | Apply_rewrite must be idempotent so the hook can be safely re-invoked on its own output. |
| Per-segment TUI check (Plan 4 calls is_tui before resolve) | is_tui MUST NOT have side effects (it's a pure predicate). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | E (Elevation of privilege) | `quote_for_shell` | mitigate | 11 round-trip tests through `/bin/sh` — `$(rm -rf /)`, backticks, embedded quotes, newlines, tabs. The dollar-sign test is the critical regression guard (T-quote-injection in the planning guidance). Algorithm is the POSIX-portable single-quote-wrap (sh/bash/dash/zsh). |
| T-03-03-02 | T (Tampering) | `apply_rewrite` idempotency | mitigate | T3 test asserts `apply(apply(x)) == apply(x)`. Without this, re-running the hook on a previously-rewritten command could duplicate flags or corrupt argv. |
| T-03-03-03 | T (Tampering) | `apply_rewrite` argv[0] preservation | mitigate | T10 test asserts argv[0] is preserved even when replace_flags maps it. argv[0] integrity is required for the command to execute correctly. |
| T-03-03-04 | I (Information disclosure) | TUI-segment-in-chain detection | mitigate | T-tui-whole-chain (planning guidance): a TUI segment in a chain MUST cause whole-chain bypass. `is_tui` correctness is the precondition; Plan 4 enforces the whole-chain decision. The 30+ tui_heuristic tests lock the precondition. |
| T-03-03-05 | T (Tampering) | TUI heuristic false negatives | accept | Conservative form per docs/specs/chained-commands.md — false positives (wrap non-TUI command) only lose filtering; false negatives (miss a TUI) hang the user's terminal. Negative tests (`ls`, `git status`, `pnpm run dev`, `cargo test`) lock against over-aggressive matching; positive tests cover the 30 cases. Beyond v1, user-overridable TUI list (deferred to v2 backlog) further addresses false-positive risk. |
</threat_model>

<verification>
- `cargo test -p lacon-adapter-claudecode --test tui_heuristic` is green (≥34 tests).
- `cargo test -p lacon-adapter-claudecode --lib tui::tests` is green.
- `cargo test -p lacon-adapter-claudecode --lib quote::tests` is green (11 tests).
- `cargo test -p lacon-core --lib rules::rewrite::tests` is green (10 tests).
- `cargo build --workspace` clean.
- `cargo test --workspace --tests` shows no regression in upstream Phase 1/2 test suites.
</verification>

<success_criteria>
- All three pure functions (is_tui, quote_for_shell, apply_rewrite) implemented and tested.
- 30+ TUI table-driven tests covering 22 pure-TUI + 8 conditional + ≥4 negative cases.
- 11 POSIX shell-quote round-trip tests through `/bin/sh`.
- 10 apply_rewrite idempotency / argv[0]-preservation tests.
- Plan 4 can call each function directly with no integration risk.
- REQ-adapter-tui-bypass has its full positive+negative test gate (the orchestration whole-chain bypass enforcement lands in Plan 4).
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-code-adapter-lacon-init/03-03-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Plan 3 and Plan 2 are file-disjoint Wave 2 plans and may run in parallel.
</output>
